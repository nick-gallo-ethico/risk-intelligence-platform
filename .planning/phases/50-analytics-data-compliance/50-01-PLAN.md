---
phase: 50-analytics-data-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/prisma/schema.prisma
  - apps/backend/src/modules/analytics/facts/fact.module.ts
  - apps/backend/src/modules/analytics/facts/entities/fact-case-daily.entity.ts
  - apps/backend/src/modules/analytics/facts/entities/fact-riu-daily.entity.ts
  - apps/backend/src/modules/analytics/facts/entities/fact-campaign-daily.entity.ts
  - apps/backend/src/modules/analytics/facts/entities/index.ts
  - apps/backend/src/modules/analytics/facts/index.ts
  - apps/backend/src/modules/analytics/analytics.module.ts
autonomous: true

must_haves:
  truths:
    - "Fact tables exist in PostgreSQL for CaseDaily, RiuDaily, and CampaignDaily aggregations"
    - "Each fact table includes organizationId for tenant isolation"
    - "Fact tables have all dimensions (category, severity, location, business_unit) and metrics (counts, averages)"
    - "Fact tables have refreshedAt timestamp for staleness monitoring"
  artifacts:
    - path: "apps/backend/prisma/schema.prisma"
      provides: "FactCaseDaily, FactRiuDaily, FactCampaignDaily Prisma models"
      contains: "model FactCaseDaily"
    - path: "apps/backend/src/modules/analytics/facts/fact.module.ts"
      provides: "FactModule NestJS module"
      exports: ["FactModule"]
    - path: "apps/backend/src/modules/analytics/facts/entities/fact-case-daily.entity.ts"
      provides: "TypeScript entity for CaseDaily fact"
      min_lines: 50
  key_links:
    - from: "apps/backend/src/modules/analytics/analytics.module.ts"
      to: "FactModule"
      via: "imports array"
      pattern: "FactModule"
---

<objective>
Create Prisma models and NestJS entities for fact tables (FACT_CASE_DAILY, FACT_RIU_DAILY, FACT_CAMPAIGN_DAILY).

Purpose: Fact tables provide pre-aggregated analytics data for fast dashboard queries. This is the foundation for all analytics performance improvements.

Output: Three fact table models in Prisma schema with corresponding TypeScript entities, ready for aggregation services.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-analytics-data-compliance/50-RESEARCH.md
@01-SHARED-INFRASTRUCTURE/ANALYTICS-DATA-MODEL.md
@apps/backend/prisma/schema.prisma
@apps/backend/src/modules/analytics/analytics.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fact table Prisma models to schema</name>
  <files>apps/backend/prisma/schema.prisma</files>
  <action>
Add three fact table models to the Prisma schema following the specifications from ANALYTICS-DATA-MODEL.md:

**FactCaseDaily model:**

- id (UUID), organizationId (String), aggregationDate (DateTime @db.Date)
- Dimension fields: categoryId, severityLevel, sourceChannel, locationId, businessUnitId
- Metric fields: casesCreated (Int), casesClosed (Int), casesOpen (Int), avgDaysToClose (Float?), slaBreaches (Int)
- Metadata: refreshedAt (DateTime @default(now()))
- Unique constraint on [organizationId, aggregationDate, categoryId, severityLevel, sourceChannel, locationId, businessUnitId]
- Index on [organizationId, aggregationDate], [organizationId, categoryId], [organizationId, severityLevel]

**FactRiuDaily model:**

- id (UUID), organizationId, aggregationDate
- Dimension fields: riuType, sourceChannel, categoryId, severityLevel, locationId
- Metric fields: riusCreated (Int), riusPending (Int), riusReleased (Int), avgTimeToRelease (Float?)
- Same metadata and indexing pattern

**FactCampaignDaily model:**

- id (UUID), organizationId, aggregationDate
- Dimension fields: campaignId, campaignType, status
- Metric fields: totalAssignments (Int), completedAssignments (Int), overdueAssignments (Int), completionRate (Float?)
- Same metadata and indexing pattern

Add Organization relation to each fact table. Use @map("snake_case") for column names.
</action>
<verify>Run `cd apps/backend && npx prisma validate` - should pass with no errors</verify>
<done>Three fact table models exist in schema.prisma with all dimensions, metrics, and indexes</done>
</task>

<task type="auto">
  <name>Task 2: Generate Prisma client and create migration</name>
  <files>apps/backend/prisma/migrations/</files>
  <action>
Run Prisma migration to create the fact tables in the database:

1. Generate the migration: `npx prisma migrate dev --name add_fact_tables`
2. If migration fails, check for syntax errors in schema.prisma

The migration should create:

- fact_case_daily table
- fact_riu_daily table
- fact_campaign_daily table

With all indexes and unique constraints as specified.
</action>
<verify>Run `cd apps/backend && npx prisma migrate status` - should show no pending migrations</verify>
<done>Database has fact tables created with all columns and indexes</done>
</task>

<task type="auto">
  <name>Task 3: Create FactModule with TypeScript entities</name>
  <files>
apps/backend/src/modules/analytics/facts/fact.module.ts
apps/backend/src/modules/analytics/facts/entities/fact-case-daily.entity.ts
apps/backend/src/modules/analytics/facts/entities/fact-riu-daily.entity.ts
apps/backend/src/modules/analytics/facts/entities/fact-campaign-daily.entity.ts
apps/backend/src/modules/analytics/facts/entities/index.ts
apps/backend/src/modules/analytics/facts/index.ts
apps/backend/src/modules/analytics/analytics.module.ts
  </files>
  <action>
Create the FactModule structure:

**fact.module.ts:**

```typescript
@Module({
  imports: [],
  providers: [],
  exports: [],
})
export class FactModule {}
```

**entities/fact-case-daily.entity.ts:**

- Interface matching Prisma model fields
- Type for dimension fields (categoryId, severityLevel, sourceChannel, locationId, businessUnitId)
- Type for metric fields (casesCreated, casesClosed, casesOpen, avgDaysToClose, slaBreaches)
- Utility types for aggregation queries

**entities/fact-riu-daily.entity.ts:**

- Same pattern for RIU dimensions and metrics

**entities/fact-campaign-daily.entity.ts:**

- Same pattern for Campaign dimensions and metrics

**index.ts files:**

- Barrel exports for all entities

**analytics.module.ts:**

- Import FactModule in the imports array

Follow existing entity patterns in the codebase (see dashboard-config.entity.ts for reference).
</action>
<verify>Run `cd apps/backend && npm run build` - should compile with no TypeScript errors</verify>
<done>FactModule exists with three entity files, properly exported and imported in AnalyticsModule</done>
</task>

</tasks>

<verification>
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate status` shows no pending migrations
- [ ] `npm run build` compiles successfully
- [ ] Fact table models have organizationId field (tenant isolation)
- [ ] Each fact table has refreshedAt timestamp
- [ ] Unique constraints prevent duplicate aggregation rows
- [ ] Indexes exist for common query patterns (org+date, org+category)
</verification>

<success_criteria>

- Three fact table Prisma models (FactCaseDaily, FactRiuDaily, FactCampaignDaily) exist in schema
- Database migration created and applied successfully
- TypeScript entities match Prisma models with proper types
- FactModule registered in AnalyticsModule
- All dimensions and metrics from ANALYTICS-DATA-MODEL.md are included
  </success_criteria>

<output>
After completion, create `.planning/phases/50-analytics-data-compliance/50-01-SUMMARY.md`
</output>
