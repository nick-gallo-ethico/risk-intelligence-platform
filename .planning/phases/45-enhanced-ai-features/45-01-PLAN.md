---
phase: 45-enhanced-ai-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/ai/note-cleanup-diff.tsx
  - apps/frontend/src/components/operator/ai-note-cleanup.tsx
  - apps/frontend/src/hooks/useAiNoteCleanup.ts
autonomous: true

must_haves:
  truths:
    - "Note cleanup shows word-level diff between original and cleaned content"
    - "Added words highlighted green, removed words highlighted red with strikethrough"
    - "User can toggle between diff view and side-by-side view"
  artifacts:
    - path: "apps/frontend/src/components/ai/note-cleanup-diff.tsx"
      provides: "Word-level diff component using diff library"
      exports: ["NoteCleanupDiff"]
    - path: "apps/frontend/src/components/operator/ai-note-cleanup.tsx"
      provides: "Enhanced cleanup component with diff view toggle"
      contains: "NoteCleanupDiff"
  key_links:
    - from: "apps/frontend/src/components/operator/ai-note-cleanup.tsx"
      to: "apps/frontend/src/components/ai/note-cleanup-diff.tsx"
      via: "import and render"
      pattern: "import.*NoteCleanupDiff"
---

<objective>
Add word-level diff preview to the AI note cleanup component.

Purpose: AIEX-01 requires before/after preview for note cleanup (bullet points to formal narrative). The existing AiNoteCleanup component shows side-by-side comparison but lacks word-level diff highlighting.

Output: Enhanced note cleanup UI with word-level diff view using the existing `diff` library pattern from policy-version-diff.tsx.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-enhanced-ai-features/45-RESEARCH.md

# Reference implementation for diff pattern:

@apps/frontend/src/components/policies/policy-version-diff.tsx

# Current note cleanup component to enhance:

@apps/frontend/src/components/operator/ai-note-cleanup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NoteCleanupDiff component</name>
  <files>apps/frontend/src/components/ai/note-cleanup-diff.tsx</files>
  <action>
Create a new component that displays word-level diff between original and cleaned note content.

Follow the exact pattern from policy-version-diff.tsx:

1. Import `diffWords` and `Change` type from 'diff' library (already installed)
2. Create `NoteCleanupDiff` component with props: `originalContent: string`, `cleanedContent: string`
3. Use `useMemo` to compute diff: `diffWords(originalContent, cleanedContent)`
4. Render each diff segment:
   - Added text: green background (`bg-green-100 dark:bg-green-900/30`), no strikethrough
   - Removed text: red background (`bg-red-100 dark:bg-red-900/30`), strikethrough
   - Unchanged text: no styling
5. Wrap in prose container for readability

Use the exact DiffSegment pattern from policy-version-diff.tsx but WITHOUT the stripHtml logic (notes are plain text, not HTML).

Export `NoteCleanupDiff` as named export.
</action>
<verify>
File exists at apps/frontend/src/components/ai/note-cleanup-diff.tsx.
Run: `npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -30`
No TypeScript errors.
</verify>
<done>
NoteCleanupDiff component exists with word-level diff using diffWords from diff library, following the established policy-version-diff.tsx pattern.
</done>
</task>

<task type="auto">
  <name>Task 2: Add diff view mode to AiNoteCleanup</name>
  <files>apps/frontend/src/components/operator/ai-note-cleanup.tsx</files>
  <action>
Enhance the existing AiNoteCleanup component to support diff view mode:

1. Add import for NoteCleanupDiff from '@/components/ai/note-cleanup-diff'
2. Add state for view mode: `const [viewMode, setViewMode] = useState<'diff' | 'side-by-side'>('diff')`
3. Add view mode toggle buttons in the comparison view header (next to Edit/Redo buttons):
   - Use a ToggleGroup or two Buttons with variant based on active state
   - "Diff" and "Side-by-Side" labels with appropriate icons (Diff, Columns)
4. In the comparison view (when `showComparison && cleanedContent`):
   - If viewMode is 'diff': render `<NoteCleanupDiff originalContent={originalContent} cleanedContent={cleanedContent} />`
   - If viewMode is 'side-by-side': render the existing two-column grid layout
5. Default to 'diff' view as it's more useful for reviewing changes

Keep the existing side-by-side layout code but conditionally render based on viewMode.
</action>
<verify>
Run: `npx tsc --noEmit -p apps/frontend/tsconfig.json 2>&1 | head -30`
No TypeScript errors.
</verify>
<done>
AiNoteCleanup component has view mode toggle between diff view and side-by-side view. Diff view shows word-level changes highlighted in green/red.
</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npm run typecheck -w apps/frontend`
2. ESLint passes: `npm run lint -w apps/frontend -- --max-warnings 0`
3. Manual verification: Note cleanup shows diff view by default with word-level highlighting
</verification>

<success_criteria>

- NoteCleanupDiff component renders word-level diff with green additions and red deletions
- AiNoteCleanup has toggle between 'diff' and 'side-by-side' view modes
- Default view mode is 'diff' showing inline changes
- Side-by-side mode preserves existing functionality
- No TypeScript or ESLint errors
  </success_criteria>

<output>
After completion, create `.planning/phases/45-enhanced-ai-features/45-01-SUMMARY.md`
</output>
