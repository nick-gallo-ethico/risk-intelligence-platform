---
phase: 39-frontend-test-repair
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx
autonomous: true

must_haves:
  truths:
    - "All 27 CaseActivityTimeline tests pass (23 failures fixed)"
    - "Tests use renderWithProviders for QueryClient context"
    - "Skeleton structure assertions match Phase 25.1 component"
  artifacts:
    - path: "apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx"
      provides: "CaseActivityTimeline component tests"
      contains: "renderWithProviders"
  key_links:
    - from: "case-activity-timeline.test.tsx"
      to: "renderWithProviders"
      via: "import statement"
      pattern: "import.*renderWithProviders.*from.*@/test/renderWithProviders"
---

<objective>
Fix 23 failing tests in case-activity-timeline.test.tsx.

Purpose: CaseActivityTimeline component uses useActivities hook which requires QueryClientProvider. Tests fail with "No QueryClient set" error. This plan updates all render calls to use renderWithProviders.

Output: All 27 CaseActivityTimeline tests pass.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-frontend-test-repair/39-RESEARCH.md
@.planning/phases/39-frontend-test-repair/39-01-SUMMARY.md
@apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx
@apps/frontend/src/test/renderWithProviders.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace render with renderWithProviders</name>
  <files>apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx</files>
  <action>
1. **Update imports:**
   - Remove `render` from @testing-library/react import
   - Add import: `import { renderWithProviders } from '@/test/renderWithProviders';`
   - Keep other RTL imports (screen, fireEvent, waitFor, act) from original import

2. **Find and replace all render() calls:**
   - Search for `render(<CaseActivityTimeline` and replace with `renderWithProviders(<CaseActivityTimeline`
   - Search for `render(<CaseActivityTimelineSkeleton` and replace with `renderWithProviders(<CaseActivityTimelineSkeleton`

3. **Verify mock patterns:**
   - Keep existing vi.mock for @/lib/api
   - Keep existing vi.mock for next/navigation
   - Check if component imports from 'sonner' - if so, update any toast mocks

4. **Handle skeleton structure changes (2 failures):**
   - The Phase 25.1 skeleton no longer has `.bg-gray-50.border-b` action bar
   - If tests assert on specific skeleton DOM structure, update selectors to match current implementation
   - Read the actual CaseActivityTimelineSkeleton component to understand current structure
     </action>
     <verify>
     Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-activity-timeline.test.tsx`

Expected: All 27 tests pass (0 failures)
</verify>
<done>

- All render() calls replaced with renderWithProviders()
- Import added for renderWithProviders
- Skeleton structure assertions updated if needed
- All 27 tests pass
  </done>
  </task>

<task type="auto">
  <name>Task 2: Update CSS token assertions if present</name>
  <files>apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx</files>
  <action>
Search for any hardcoded Tailwind color class assertions and update to semantic tokens:

| Old Class          | New Class               |
| ------------------ | ----------------------- |
| `text-gray-500`    | `text-muted-foreground` |
| `bg-gray-100`      | `bg-muted`              |
| `text-gray-600`    | `text-muted-foreground` |
| `hover:bg-gray-50` | `hover:bg-muted/50`     |
| `bg-white`         | `bg-card`               |
| `border-gray-200`  | `border-border`         |

Search patterns to find:

- `toHaveClass.*gray`
- `toHaveClass.*bg-white`
- Any hardcoded color in expect statements

If no color assertions exist, no changes needed.
</action>
<verify>
Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-activity-timeline.test.tsx`

Expected: All tests still pass after CSS token updates
</verify>
<done>

- Any hardcoded color assertions updated to semantic tokens
- Tests continue to pass
  </done>
  </task>

</tasks>

<verification>
After completing all tasks:

1. Run test file: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-activity-timeline.test.tsx`
2. Expected output: `Tests: 27 passed, 27 total`
3. No test functionality removed - all original test intents preserved
   </verification>

<success_criteria>

- [ ] All render() calls use renderWithProviders
- [ ] Import statement added for test utility
- [ ] Skeleton assertions match current component structure
- [ ] CSS token assertions use semantic tokens
- [ ] All 27 tests pass
      </success_criteria>

<output>
After completion, create `.planning/phases/39-frontend-test-repair/39-02-SUMMARY.md`
</output>
