---
phase: 39-frontend-test-repair
plan: 04
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx
  - apps/frontend/src/components/cases/__tests__/property-section.test.tsx
  - apps/frontend/src/components/cases/__tests__/case-header.test.tsx
  - apps/frontend/src/components/cases/__tests__/create-investigation-dialog.test.tsx
autonomous: true

must_haves:
  truths:
    - "All 14 activity-filters tests pass (2 failures fixed)"
    - "All 10 property-section tests pass (2 failures fixed)"
    - "All 17 case-header tests pass (1 failure fixed)"
    - "All 17 create-investigation-dialog tests pass (3 failures fixed)"
  artifacts:
    - path: "apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx"
      provides: "ActivityFilters component tests"
    - path: "apps/frontend/src/components/cases/__tests__/property-section.test.tsx"
      provides: "PropertySection component tests"
    - path: "apps/frontend/src/components/cases/__tests__/case-header.test.tsx"
      provides: "CaseHeader component tests"
    - path: "apps/frontend/src/components/cases/__tests__/create-investigation-dialog.test.tsx"
      provides: "CreateInvestigationDialog component tests"
      contains: "vi.mock.*sonner"
  key_links:
    - from: "create-investigation-dialog.test.tsx"
      to: "sonner"
      via: "vi.mock"
      pattern: "vi\\.mock\\(['\"]sonner['\"]"
---

<objective>
Fix remaining 8 test failures across 4 test files.

Purpose: These files have CSS token migration issues (activity-filters, property-section, case-header) and mock path mismatches (create-investigation-dialog). Simpler fixes than QueryClient issues.

Output: All 58 tests pass across 4 files.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-frontend-test-repair/39-RESEARCH.md
@.planning/phases/39-frontend-test-repair/39-01-SUMMARY.md
@apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx
@apps/frontend/src/components/cases/__tests__/property-section.test.tsx
@apps/frontend/src/components/cases/__tests__/case-header.test.tsx
@apps/frontend/src/components/cases/__tests__/create-investigation-dialog.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix activity-filters.test.tsx (2 failures)</name>
  <files>apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx</files>
  <action>
CSS token migration failures. Update class assertions:

1. Search for `toHaveClass` assertions with hardcoded colors
2. Update mappings:
   - `text-gray-500` -> `text-muted-foreground`
   - `bg-gray-100` -> `bg-muted`
   - `hover:bg-gray-50` -> `hover:bg-muted/50`

Example fix:

```typescript
// Before
expect(inactiveTab).toHaveClass("text-gray-500");

// After
expect(inactiveTab).toHaveClass("text-muted-foreground");
```

Note: This component likely does NOT need QueryClient - verify by checking if it uses any React Query hooks.
</action>
<verify>
Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/activity-filters.test.tsx`

Expected: All 14 tests pass
</verify>
<done>

- CSS token assertions updated
- All 14 tests pass
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix property-section.test.tsx (2 failures)</name>
  <files>apps/frontend/src/components/cases/__tests__/property-section.test.tsx</files>
  <action>
Two issues: CSS token migration + chevron rotation change.

1. **Chevron rotation fix (1 failure):**
   - Phase 25.1 changed chevron from `rotate-180` to `rotate-90` for expanded state

   ```typescript
   // Before
   expect(chevron).toHaveClass("rotate-180");

   // After
   expect(chevron).toHaveClass("rotate-90");
   ```

2. **CSS token fix (1 failure):**
   - Update any hardcoded color assertions to semantic tokens

Search for: `rotate-180`, `text-gray`, `bg-gray`, `bg-white`
</action>
<verify>
Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/property-section.test.tsx`

Expected: All 10 tests pass
</verify>
<done>

- Chevron rotation assertion updated (rotate-180 -> rotate-90)
- CSS token assertions updated
- All 10 tests pass
  </done>
  </task>

<task type="auto">
  <name>Task 3: Fix case-header.test.tsx (1 failure)</name>
  <files>apps/frontend/src/components/cases/__tests__/case-header.test.tsx</files>
  <action>
CSS token migration failure. Update class assertions:

1. Search for `toHaveClass` assertions with hardcoded colors
2. Apply standard mapping:
   - `text-gray-*` -> `text-muted-foreground`
   - `bg-gray-*` -> `bg-muted`
   - `border-gray-*` -> `border-border`

This file might also have skeleton structure changes - check if assertions test specific skeleton DOM structure.
</action>
<verify>
Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-header.test.tsx`

Expected: All 17 tests pass
</verify>
<done>

- CSS token assertions updated
- Skeleton assertions updated if applicable
- All 17 tests pass
  </done>
  </task>

<task type="auto">
  <name>Task 4: Fix create-investigation-dialog.test.tsx (3 failures)</name>
  <files>apps/frontend/src/components/cases/__tests__/create-investigation-dialog.test.tsx</files>
  <action>
Mock path mismatch - tests mock wrong toast import.

1. **Find the mock:**
   Search for `vi.mock.*toaster` or `vi.mock.*toast`

2. **Check component imports:**
   Read CreateInvestigationDialog component to verify it imports from `sonner`

3. **Update mock path:**

   ```typescript
   // Before
   vi.mock("@/components/ui/toaster", () => ({
     toast: { success: vi.fn(), error: vi.fn() },
   }));

   // After
   vi.mock("sonner", () => ({
     toast: { success: vi.fn(), error: vi.fn() },
   }));
   ```

4. **Verify toast.success and toast.error calls in tests** match the mocked structure.

This component might need renderWithProviders if it uses React Query - check imports.
</action>
<verify>
Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/create-investigation-dialog.test.tsx`

Expected: All 17 tests pass
</verify>
<done>

- Mock path corrected to match component imports
- renderWithProviders added if component uses React Query
- All 17 tests pass
  </done>
  </task>

</tasks>

<verification>
After completing all tasks:

1. Run all 4 test files:
   ```bash
   cd apps/frontend && npx vitest run \
     src/components/cases/__tests__/activity-filters.test.tsx \
     src/components/cases/__tests__/property-section.test.tsx \
     src/components/cases/__tests__/case-header.test.tsx \
     src/components/cases/__tests__/create-investigation-dialog.test.tsx
   ```
2. Expected output: All 58 tests pass (14 + 10 + 17 + 17)
3. No test functionality removed
   </verification>

<success_criteria>

- [ ] activity-filters.test.tsx: All 14 tests pass
- [ ] property-section.test.tsx: All 10 tests pass
- [ ] case-header.test.tsx: All 17 tests pass
- [ ] create-investigation-dialog.test.tsx: All 17 tests pass
- [ ] CSS assertions use semantic tokens
- [ ] Chevron rotation uses rotate-90
- [ ] Mock paths match component imports
      </success_criteria>

<output>
After completion, create `.planning/phases/39-frontend-test-repair/39-04-SUMMARY.md`
</output>
