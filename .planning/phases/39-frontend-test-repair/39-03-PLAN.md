---
phase: 39-frontend-test-repair
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - apps/frontend/src/components/cases/__tests__/case-properties-panel.test.tsx
  - apps/frontend/src/components/cases/__tests__/case-investigations-panel.test.tsx
autonomous: true

must_haves:
  truths:
    - "All 20 CasePropertiesPanel tests pass (15 failures fixed)"
    - "All 18 CaseInvestigationsPanel tests pass (15 failures fixed)"
    - "Tests use renderWithProviders for QueryClient context"
    - "Mock imports match actual component imports (sonner not @/components/ui/toaster)"
  artifacts:
    - path: "apps/frontend/src/components/cases/__tests__/case-properties-panel.test.tsx"
      provides: "CasePropertiesPanel component tests"
      contains: "renderWithProviders"
    - path: "apps/frontend/src/components/cases/__tests__/case-investigations-panel.test.tsx"
      provides: "CaseInvestigationsPanel component tests"
      contains: "renderWithProviders"
  key_links:
    - from: "case-properties-panel.test.tsx"
      to: "renderWithProviders"
      via: "import statement"
      pattern: "import.*renderWithProviders"
    - from: "case-investigations-panel.test.tsx"
      to: "renderWithProviders"
      via: "import statement"
      pattern: "import.*renderWithProviders"
---

<objective>
Fix 30 failing tests across CasePropertiesPanel and CaseInvestigationsPanel test files.

Purpose: Both components use hooks requiring QueryClient context. Tests fail with "No QueryClient set" error plus some mock path mismatches.

Output: All 38 tests pass across both files.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-frontend-test-repair/39-RESEARCH.md
@.planning/phases/39-frontend-test-repair/39-01-SUMMARY.md
@apps/frontend/src/components/cases/__tests__/case-properties-panel.test.tsx
@apps/frontend/src/components/cases/__tests__/case-investigations-panel.test.tsx
@apps/frontend/src/test/renderWithProviders.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix case-properties-panel.test.tsx (15 failures)</name>
  <files>apps/frontend/src/components/cases/__tests__/case-properties-panel.test.tsx</files>
  <action>
1. **Update imports:**
   - Remove `render` from @testing-library/react import (keep screen, fireEvent, waitFor)
   - Add: `import { renderWithProviders } from '@/test/renderWithProviders';`

2. **Replace all render() calls:**
   - Find all `render(<CasePropertiesPanel` and replace with `renderWithProviders(<CasePropertiesPanel`

3. **Fix CSS token assertions (5 failures):**
   Search for hardcoded color class assertions and update:
   - `text-gray-500` -> `text-muted-foreground`
   - `bg-gray-100` -> `bg-muted`
   - `text-gray-600` -> `text-muted-foreground`
   - `bg-white` -> `bg-card`
   - `border-gray-200` -> `border-border`

4. **Check mock paths:**
   - If test mocks `@/components/ui/toaster`, check if component actually imports from `sonner`
   - Update mock to match actual import path
     </action>
     <verify>
     Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-properties-panel.test.tsx`

Expected: All 20 tests pass
</verify>
<done>

- renderWithProviders replaces all render() calls
- CSS token assertions use semantic tokens
- Mock paths match component imports
- All 20 tests pass
  </done>
  </task>

<task type="auto">
  <name>Task 2: Fix case-investigations-panel.test.tsx (15 failures)</name>
  <files>apps/frontend/src/components/cases/__tests__/case-investigations-panel.test.tsx</files>
  <action>
1. **Update imports:**
   - Remove `render` from @testing-library/react import
   - Add: `import { renderWithProviders } from '@/test/renderWithProviders';`

2. **Replace all render() calls:**
   - Find all `render(<CaseInvestigationsPanel` and replace with `renderWithProviders(<CaseInvestigationsPanel`

3. **Fix mock path mismatch:**
   - Check if test mocks `@/components/ui/toaster`
   - If component imports from `sonner`, update mock:

   ```typescript
   // Before
   vi.mock("@/components/ui/toaster", () => ({
     toast: { success: vi.fn(), error: vi.fn() },
   }));

   // After
   vi.mock("sonner", () => ({
     toast: { success: vi.fn(), error: vi.fn() },
   }));
   ```

4. **Check for additional CSS token assertions** that need updating.
   </action>
   <verify>
   Run: `cd apps/frontend && npx vitest run src/components/cases/__tests__/case-investigations-panel.test.tsx`

Expected: All 18 tests pass
</verify>
<done>

- renderWithProviders replaces all render() calls
- Mock path corrected from @/components/ui/toaster to sonner (if applicable)
- All 18 tests pass
  </done>
  </task>

</tasks>

<verification>
After completing all tasks:

1. Run both test files:
   ```bash
   cd apps/frontend && npx vitest run src/components/cases/__tests__/case-properties-panel.test.tsx src/components/cases/__tests__/case-investigations-panel.test.tsx
   ```
2. Expected output: All 38 tests pass
3. No test functionality removed
   </verification>

<success_criteria>

- [ ] case-properties-panel.test.tsx: All 20 tests pass
- [ ] case-investigations-panel.test.tsx: All 18 tests pass
- [ ] Both files use renderWithProviders
- [ ] Mock paths match component imports
- [ ] CSS assertions use semantic tokens
      </success_criteria>

<output>
After completion, create `.planning/phases/39-frontend-test-repair/39-03-SUMMARY.md`
</output>
