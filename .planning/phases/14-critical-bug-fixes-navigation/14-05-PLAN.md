---
phase: 14-critical-bug-fixes-navigation
plan: 05
type: execute
wave: 2
depends_on: ["14-01", "14-02", "14-03", "14-04"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 11 success criteria from Phase 14 are verified"
    - "No TypeScript compilation errors"
    - "No console errors visible on dashboard, case creation, or search pages"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 14 bug fixes are working end-to-end.

Purpose: Phase 14 fixes 11 distinct issues. This checkpoint verifies all fixes work together before marking the phase complete.

Output: Verification results documenting pass/fail for each success criterion.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-critical-bug-fixes-navigation/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
  Run these automated checks to verify all fixes:

1. **TypeScript compilation:**

   ```bash
   cd apps/frontend && npx tsc --noEmit
   ```

   Must pass with zero errors.

2. **Verify all pages exist (no more 404s):**

   ```bash
   ls apps/frontend/src/app/\(authenticated\)/notifications/page.tsx
   ls apps/frontend/src/app/\(authenticated\)/my-work/page.tsx
   ls apps/frontend/src/app/\(authenticated\)/search/page.tsx
   ls apps/frontend/src/app/\(authenticated\)/profile/page.tsx
   ls apps/frontend/src/app/\(authenticated\)/settings/audit/page.tsx
   ```

   All must exist.

3. **Verify no hardcoded user data:**

   ```bash
   grep -n "Sarah Chen" apps/frontend/src/components/layout/top-nav.tsx
   ```

   Must return zero results.

4. **Verify auth context wired:**

   ```bash
   grep -n "useAuth" apps/frontend/src/components/layout/top-nav.tsx
   ```

   Must find useAuth import and usage.

5. **Verify logout handler exists:**

   ```bash
   grep -n "logout" apps/frontend/src/components/layout/top-nav.tsx
   ```

   Must find logout() call.

6. **Verify SVG logo used:**

   ```bash
   grep -n "ethico-icon" apps/frontend/src/components/layout/app-sidebar.tsx
   ```

   Must find the SVG reference.

7. **Verify dark nav styling:**

   ```bash
   grep -n "hsl(227" apps/frontend/src/components/layout/top-nav.tsx
   ```

   Must find the dark background color.

8. **Verify no empty SelectItem values:**

   ```bash
   grep -rn 'SelectItem.*value=""' apps/frontend/src/components/cases/form-sections/
   ```

   Must return zero results.

9. **Verify reduced dashboard fetch:**

   ```bash
   grep -n "limit.*25" apps/frontend/src/app/\(authenticated\)/dashboard/page.tsx
   ```

   Must find the reduced limit.

10. **Verify sidebar dark theme:**
    ```bash
    grep -n "sidebar-background.*227" apps/frontend/src/app/globals.css
    ```
    Should find the dark sidebar variable.

Record pass/fail for each check.
</action>
<verify>All 10 automated checks pass.</verify>
<done>All automated verification checks pass. TypeScript compiles. All pages exist. No hardcoded data. Auth wired. Dark theme applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Phase 14 fixes all applied:
  1. Top nav shows real user name from auth context (not "Sarah Chen")
  2. Logout button works (logs out and redirects to /login)
  3. Ethico SVG logo in sidebar (not text "E")
  4. Top nav and sidebar both dark navy
  5. /notifications page shows notifications list
  6. /my-work page shows task queue
  7. /search page shows search results
  8. /profile redirects to /settings
  9. Create Case form opens without errors
  10. Dashboard loads quickly
  11. Audit log accessible at /settings/audit
  </what-built>
  <how-to-verify>
  Start the dev server: `cd apps/frontend && npm run dev`

1. Navigate to dashboard — verify it loads within 2 seconds
2. Check top-right: user name should reflect logged-in user (not "Sarah Chen")
3. Check sidebar: Ethico "E" logo should be the gradient SVG (not plain blue box)
4. Check nav colors: top bar and sidebar should both be dark navy
5. Click notification bell → "View All" → should navigate to /notifications (not 404)
6. Click "View All Tasks" on dashboard → should navigate to /my-work (not 404)
7. Type in global search bar, press Enter → should navigate to /search?q=... (not 404)
8. Click user avatar → "My Profile" → should redirect to /settings (not 404)
9. Click user avatar → "Log out" → should log out and show /login page
10. Navigate to /cases/new → form should open without console errors
11. In case creation form, check category dropdowns — "None" option should work without errors
12. Click "My Tasks" items on dashboard → should navigate to case detail pages
13. Check sidebar → Settings → Audit Log should be visible and clickable
    </how-to-verify>
    <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
    </task>

</tasks>

<verification>
All 11 Phase 14 success criteria verified through automated checks and manual testing.
</verification>

<success_criteria>

- All 10 automated checks pass
- Human verification confirms all 11 success criteria are met
- No console errors on any fixed page
  </success_criteria>

<output>
After completion, create `.planning/phases/14-critical-bug-fixes-navigation/14-05-SUMMARY.md`
</output>
