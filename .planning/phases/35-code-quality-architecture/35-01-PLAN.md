---
phase: 35-code-quality-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/ai/schema-introspection.service.ts
  - apps/backend/src/modules/ai/services/entity-schema-registry.service.ts
  - apps/backend/src/modules/ai/services/filter-validator.service.ts
  - apps/backend/src/modules/analytics/migration/mapping-suggestion.service.ts
  - apps/backend/src/modules/analytics/migration/services/field-matcher.service.ts
  - apps/backend/src/modules/analytics/migration/services/transform-applier.service.ts
  - apps/backend/src/modules/analytics/ai-query/query-to-prisma.service.ts
  - apps/backend/src/modules/analytics/ai-query/services/field-whitelist.service.ts
  - apps/backend/src/modules/analytics/ai-query/services/prisma-query-builder.service.ts
  - apps/backend/src/modules/ai/index.ts
  - apps/backend/src/modules/ai/ai.module.ts
  - apps/backend/src/modules/analytics/migration/migration.module.ts
  - apps/backend/src/modules/analytics/ai-query/ai-query.module.ts
autonomous: true

must_haves:
  truths:
    - "schema-introspection.service.ts is under 400 LOC"
    - "mapping-suggestion.service.ts is under 400 LOC"
    - "query-to-prisma.service.ts is under 400 LOC"
    - "All services compile without errors"
    - "All existing functionality continues to work"
  artifacts:
    - path: "apps/backend/src/modules/ai/services/entity-schema-registry.service.ts"
      provides: "Static schema definitions extracted from schema-introspection"
      min_lines: 100
    - path: "apps/backend/src/modules/ai/services/filter-validator.service.ts"
      provides: "Filter validation logic extracted from schema-introspection"
      min_lines: 100
    - path: "apps/backend/src/modules/analytics/migration/services/field-matcher.service.ts"
      provides: "Fuzzy field matching logic extracted from mapping-suggestion"
      min_lines: 150
    - path: "apps/backend/src/modules/analytics/migration/services/transform-applier.service.ts"
      provides: "Value transformation logic extracted from mapping-suggestion"
      min_lines: 100
    - path: "apps/backend/src/modules/analytics/ai-query/services/field-whitelist.service.ts"
      provides: "Field whitelisting/security extracted from query-to-prisma"
      min_lines: 150
    - path: "apps/backend/src/modules/analytics/ai-query/services/prisma-query-builder.service.ts"
      provides: "Prisma query construction extracted from query-to-prisma"
      min_lines: 200
  key_links:
    - from: "schema-introspection.service.ts"
      to: "entity-schema-registry.service.ts"
      via: "constructor injection"
      pattern: "private.*entitySchemaRegistry"
    - from: "mapping-suggestion.service.ts"
      to: "field-matcher.service.ts"
      via: "constructor injection"
      pattern: "private.*fieldMatcher"
    - from: "query-to-prisma.service.ts"
      to: "field-whitelist.service.ts"
      via: "constructor injection"
      pattern: "private.*fieldWhitelist"
---

<objective>
Split Level 0 services (no dependencies) into focused sub-services following the "Thin Coordinator + Focused Helpers" pattern.

Purpose: These three services have no internal dependencies and are used by other services that will be split later. Splitting them first prevents breaking downstream services during later splits.

Output:

- schema-introspection.service.ts split into EntitySchemaRegistryService + FilterValidatorService
- mapping-suggestion.service.ts split into FieldMatcherService + TransformApplierService
- query-to-prisma.service.ts split into FieldWhitelistService + PrismaQueryBuilderService
  </objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-code-quality-architecture/35-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split schema-introspection.service.ts (843 LOC)</name>
  <files>
    apps/backend/src/modules/ai/schema-introspection.service.ts
    apps/backend/src/modules/ai/services/entity-schema-registry.service.ts
    apps/backend/src/modules/ai/services/filter-validator.service.ts
    apps/backend/src/modules/ai/ai.module.ts
    apps/backend/src/modules/ai/index.ts
  </files>
  <action>
    Read schema-introspection.service.ts and identify the two concerns:
    1. Static schema definitions (entity schemas, field metadata, documentation) -> EntitySchemaRegistryService
    2. Dynamic validation (filter validation, query validation) -> FilterValidatorService

    Create EntitySchemaRegistryService in services/ folder:
    - Move all static schema maps (ENTITY_SCHEMAS, FIELD_METADATA, etc.)
    - Move getEntitySchema(), getFieldMetadata(), getDocumentation() methods
    - Export as injectable service

    Create FilterValidatorService in services/ folder:
    - Move validateFilter(), validateFieldAccess(), isValidOperator() methods
    - Inject EntitySchemaRegistryService for schema lookups

    Refactor SchemaIntrospectionService as thin coordinator:
    - Inject both sub-services
    - Delegate to appropriate service
    - Keep public API unchanged (existing callers still work)
    - Target: 150-200 LOC

    Update ai.module.ts:
    - Register both new services as providers
    - Export them if needed by other modules

    Update index.ts barrel export to include new services.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/ai/schema-introspection.service.ts`
    Verify: Under 250 LOC
  </verify>
  <done>
    schema-introspection.service.ts reduced to thin coordinator under 250 LOC.
    EntitySchemaRegistryService and FilterValidatorService exist and are injected.
    Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Split mapping-suggestion.service.ts (957 LOC)</name>
  <files>
    apps/backend/src/modules/analytics/migration/mapping-suggestion.service.ts
    apps/backend/src/modules/analytics/migration/services/field-matcher.service.ts
    apps/backend/src/modules/analytics/migration/services/transform-applier.service.ts
    apps/backend/src/modules/analytics/migration/migration.module.ts
  </files>
  <action>
    Read mapping-suggestion.service.ts and identify the two concerns:
    1. Field matching (synonyms, fuzzy matching, confidence scoring) -> FieldMatcherService
    2. Value transformation (type conversion, formatting, normalization) -> TransformApplierService

    Create FieldMatcherService in services/ folder:
    - Move FIELD_SYNONYMS map, fuzzyMatch(), scoreSimilarity() methods
    - Move suggestFieldMappings(), findBestMatch() methods
    - Export as injectable service (~200-300 LOC)

    Create TransformApplierService in services/ folder:
    - Move transformation functions (transformValue, normalizeDate, etc.)
    - Move applyTransformations(), validateTransformation() methods
    - Export as injectable service (~150-250 LOC)

    Refactor MappingSuggestionService as thin coordinator:
    - Inject both sub-services
    - Keep suggestMappings() as orchestrator that calls fieldMatcher then transformApplier
    - Target: 200-300 LOC

    Update migration.module.ts to register both new services.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/analytics/migration/mapping-suggestion.service.ts`
    Verify: Under 350 LOC
  </verify>
  <done>
    mapping-suggestion.service.ts reduced to thin coordinator under 350 LOC.
    FieldMatcherService and TransformApplierService exist and are injected.
    Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Split query-to-prisma.service.ts (956 LOC)</name>
  <files>
    apps/backend/src/modules/analytics/ai-query/query-to-prisma.service.ts
    apps/backend/src/modules/analytics/ai-query/services/field-whitelist.service.ts
    apps/backend/src/modules/analytics/ai-query/services/prisma-query-builder.service.ts
    apps/backend/src/modules/analytics/ai-query/ai-query.module.ts
  </files>
  <action>
    Read query-to-prisma.service.ts and identify the two concerns:
    1. Security/validation (field whitelisting, access control) -> FieldWhitelistService
    2. Query building (Prisma where/orderBy/include construction) -> PrismaQueryBuilderService

    Create FieldWhitelistService in services/ folder:
    - Move ALLOWED_FIELDS, SENSITIVE_FIELDS maps
    - Move isFieldAllowed(), validateFieldAccess(), sanitizeFieldPath() methods
    - Move role-based field access logic
    - Export as injectable service (~200-300 LOC)

    Create PrismaQueryBuilderService in services/ folder:
    - Move buildWhereClause(), buildOrderBy(), buildInclude() methods
    - Move date range handling, operator mapping
    - Inject FieldWhitelistService for validation checks during build
    - Export as injectable service (~300-400 LOC)

    Refactor QueryToPrismaService as thin coordinator:
    - Inject both sub-services
    - Keep convert() as orchestrator
    - Target: 200-250 LOC

    Update ai-query.module.ts to register both new services.
    Ensure services/ directory exists in ai-query module.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/analytics/ai-query/query-to-prisma.service.ts`
    Verify: Under 300 LOC
  </verify>
  <done>
    query-to-prisma.service.ts reduced to thin coordinator under 300 LOC.
    FieldWhitelistService and PrismaQueryBuilderService exist and are injected.
    Backend compiles without errors.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. LOC Check:

```bash
wc -l apps/backend/src/modules/ai/schema-introspection.service.ts
wc -l apps/backend/src/modules/analytics/migration/mapping-suggestion.service.ts
wc -l apps/backend/src/modules/analytics/ai-query/query-to-prisma.service.ts
```

All three should be under 400 LOC.

2. Compilation Check:

```bash
cd apps/backend && npx tsc --noEmit
```

No errors.

3. New Services Exist:

```bash
ls apps/backend/src/modules/ai/services/
ls apps/backend/src/modules/analytics/migration/services/
ls apps/backend/src/modules/analytics/ai-query/services/
```

All 6 new service files exist.

4. Module Registration:
   Verify each new service is listed in providers array of its module.
   </verification>

<success_criteria>

- [ ] schema-introspection.service.ts under 400 LOC
- [ ] mapping-suggestion.service.ts under 400 LOC
- [ ] query-to-prisma.service.ts under 400 LOC
- [ ] 6 new focused services created
- [ ] All modules updated with new providers
- [ ] Backend compiles without TypeScript errors
- [ ] Existing functionality preserved (services are thin coordinators)
      </success_criteria>

<output>
After completion, create `.planning/phases/35-code-quality-architecture/35-01-SUMMARY.md`
</output>
