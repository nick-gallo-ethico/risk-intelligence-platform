---
phase: 35-code-quality-architecture
plan: 04
type: execute
wave: 3
depends_on: [35-01, 35-02, 35-03]
files_modified:
  - apps/backend/src/modules/policies/associations/policy-case-association.service.ts
  - apps/backend/src/modules/policies/associations/services/association-crud.service.ts
  - apps/backend/src/modules/policies/associations/services/violation-analytics.service.ts
  - apps/backend/src/modules/notifications/services/notification.service.ts
  - apps/backend/src/modules/notifications/services/notification-router.service.ts
  - apps/backend/src/modules/notifications/services/delivery-dispatcher.service.ts
  - apps/backend/src/modules/campaigns/campaign-scheduling.service.ts
  - apps/backend/src/modules/campaigns/services/wave-scheduler.service.ts
  - apps/backend/src/modules/campaigns/services/blackout-manager.service.ts
  - apps/backend/src/modules/policies/policies.module.ts
  - apps/backend/src/modules/notifications/notifications.module.ts
  - apps/backend/src/modules/campaigns/campaigns.module.ts
autonomous: true

must_haves:
  truths:
    - "policy-case-association.service.ts is under 400 LOC"
    - "notification.service.ts is under 400 LOC"
    - "campaign-scheduling.service.ts is under 400 LOC"
    - "All services compile without errors"
    - "Notification delivery continues to work"
    - "Campaign scheduling continues to work"
  artifacts:
    - path: "apps/backend/src/modules/policies/associations/services/association-crud.service.ts"
      provides: "Policy-case association CRUD operations"
      min_lines: 150
    - path: "apps/backend/src/modules/policies/associations/services/violation-analytics.service.ts"
      provides: "Violation statistics and analytics"
      min_lines: 150
    - path: "apps/backend/src/modules/notifications/services/notification-router.service.ts"
      provides: "Notification routing logic"
      min_lines: 150
    - path: "apps/backend/src/modules/notifications/services/delivery-dispatcher.service.ts"
      provides: "Multi-channel delivery dispatch"
      min_lines: 150
    - path: "apps/backend/src/modules/campaigns/services/wave-scheduler.service.ts"
      provides: "Campaign wave scheduling logic"
      min_lines: 150
    - path: "apps/backend/src/modules/campaigns/services/blackout-manager.service.ts"
      provides: "Blackout date management"
      min_lines: 100
  key_links:
    - from: "notification.service.ts"
      to: "notification-router.service.ts"
      via: "constructor injection"
      pattern: "private.*notificationRouter"
    - from: "notification.service.ts"
      to: "delivery-dispatcher.service.ts"
      via: "constructor injection"
      pattern: "private.*deliveryDispatcher"
    - from: "campaign-scheduling.service.ts"
      to: "wave-scheduler.service.ts"
      via: "constructor injection"
      pattern: "private.*waveScheduler"
---

<objective>
Split the final three Level 2 services - these have many dependents so are split last to minimize disruption.

Purpose: notification.service.ts has 13 files importing it. Splitting it last ensures all other refactoring is complete before touching this high-traffic service.

Output:

- policy-case-association.service.ts split into AssociationCrudService + ViolationAnalyticsService
- notification.service.ts split into NotificationRouterService + DeliveryDispatcherService
- campaign-scheduling.service.ts split into WaveSchedulerService + BlackoutManagerService
  </objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/35-code-quality-architecture/35-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split policy-case-association.service.ts (878 LOC)</name>
  <files>
    apps/backend/src/modules/policies/associations/policy-case-association.service.ts
    apps/backend/src/modules/policies/associations/services/association-crud.service.ts
    apps/backend/src/modules/policies/associations/services/violation-analytics.service.ts
    apps/backend/src/modules/policies/policies.module.ts
  </files>
  <action>
    Read policy-case-association.service.ts and identify two concerns:
    1. Association CRUD (create, read, update, delete, events) -> AssociationCrudService
    2. Violation analytics (stats, reporting, trends) -> ViolationAnalyticsService

    Create services/ directory in associations/ if it doesn't exist.

    Create AssociationCrudService:
    - Move create(), findAll(), findByCase(), findByPolicy(), update(), delete() methods
    - Move event emission logic (policy linked, policy unlinked)
    - Move validation logic (prevent duplicates, validate references)
    - Export as injectable service (~250-350 LOC)

    Create ViolationAnalyticsService:
    - Move getViolationStats(), getViolationsByCategory(), getViolationTrends() methods
    - Move aggregation queries
    - Move reporting data formatting
    - Export as injectable service (~200-300 LOC)

    Refactor PolicyCaseAssociationService as thin coordinator:
    - Inject both sub-services
    - Keep linkPolicyToCase() as orchestrator
    - Target: 200-250 LOC

    Update policies.module.ts or associations/index.ts to register new services.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/policies/associations/policy-case-association.service.ts`
    Verify: Under 300 LOC
  </verify>
  <done>
    policy-case-association.service.ts reduced to thin coordinator under 300 LOC.
    AssociationCrudService and ViolationAnalyticsService exist.
    Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Split notification.service.ts (868 LOC) - HIGH IMPACT</name>
  <files>
    apps/backend/src/modules/notifications/services/notification.service.ts
    apps/backend/src/modules/notifications/services/notification-router.service.ts
    apps/backend/src/modules/notifications/services/delivery-dispatcher.service.ts
    apps/backend/src/modules/notifications/notifications.module.ts
  </files>
  <action>
    **CAUTION: This service has 13 files importing it. Preserve the public API.**

    Read notification.service.ts and identify two concerns:
    1. Routing (preference checking, channel selection, OOO handling) -> NotificationRouterService
    2. Dispatch (actual delivery to email/in-app/push) -> DeliveryDispatcherService

    Create NotificationRouterService:
    - Move shouldNotify(), checkPreferences(), handleOutOfOffice() methods
    - Move channel selection logic (which channels to use)
    - Move notification deduplication logic
    - Export as injectable service (~250-350 LOC)

    Create DeliveryDispatcherService:
    - Move dispatchEmail(), dispatchInApp(), dispatchPush() methods
    - Move template rendering calls
    - Move delivery tracking/retry logic
    - Export as injectable service (~250-350 LOC)

    Refactor NotificationService as thin coordinator:
    - Inject both sub-services
    - Keep notify() as main orchestrator (existing public API)
    - Keep send() and sendBatch() for backward compatibility
    - Keep markAsRead(), getUnread() (simple operations)
    - Target: 200-300 LOC

    **CRITICAL: Do not change the method signatures of notify(), send(), sendBatch().**
    The 13 importing files expect these methods to exist with current signatures.

    Update notifications.module.ts to register both new services.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/notifications/services/notification.service.ts`
    Verify: Under 350 LOC
    Run: `grep -r "NotificationService" apps/backend/src/modules/ --include="*.ts" | wc -l`
    Verify: All imports still resolve
  </verify>
  <done>
    notification.service.ts reduced to thin coordinator under 350 LOC.
    NotificationRouterService and DeliveryDispatcherService exist.
    All 13 importing files still compile.
    Backend compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Split campaign-scheduling.service.ts (856 LOC)</name>
  <files>
    apps/backend/src/modules/campaigns/campaign-scheduling.service.ts
    apps/backend/src/modules/campaigns/services/wave-scheduler.service.ts
    apps/backend/src/modules/campaigns/services/blackout-manager.service.ts
    apps/backend/src/modules/campaigns/campaigns.module.ts
  </files>
  <action>
    Read campaign-scheduling.service.ts and identify two concerns:
    1. Wave scheduling (wave timing, distribution, assignment batching) -> WaveSchedulerService
    2. Blackout management (blackout dates, holiday handling) -> BlackoutManagerService

    Create services/ directory in campaigns/ if it doesn't exist.

    Create WaveSchedulerService:
    - Move calculateWaves(), scheduleWave(), distributeAssignments() methods
    - Move wave timing calculations
    - Inject BlackoutManagerService for date validation
    - Export as injectable service (~250-350 LOC)

    Create BlackoutManagerService:
    - Move isBlackoutDate(), getNextAvailableDate(), addBlackout(), removeBlackout() methods
    - Move holiday calendar handling
    - Move org-specific blackout rules
    - Export as injectable service (~150-250 LOC)

    Refactor CampaignSchedulingService as thin coordinator:
    - Inject both sub-services
    - Keep scheduleCampaign() as orchestrator
    - Target: 200-250 LOC

    Update campaigns.module.ts to register both new services.

  </action>
  <verify>
    Run: `cd apps/backend && npx tsc --noEmit`
    Verify: No TypeScript errors
    Run: `wc -l apps/backend/src/modules/campaigns/campaign-scheduling.service.ts`
    Verify: Under 300 LOC
  </verify>
  <done>
    campaign-scheduling.service.ts reduced to thin coordinator under 300 LOC.
    WaveSchedulerService and BlackoutManagerService exist.
    Backend compiles without errors.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. LOC Check - Final 3 Services:

```bash
wc -l apps/backend/src/modules/policies/associations/policy-case-association.service.ts
wc -l apps/backend/src/modules/notifications/services/notification.service.ts
wc -l apps/backend/src/modules/campaigns/campaign-scheduling.service.ts
```

All three should be under 400 LOC.

2. Total Service Split Verification:

```bash
# All 12 original fat services should now be under 400 LOC
find apps/backend/src/modules -name "*.service.ts" -exec wc -l {} \; | awk '$1 > 500 { print }'
```

Should return empty (no service over 500 LOC).

3. Compilation Check:

```bash
cd apps/backend && npx tsc --noEmit
```

No errors.

4. Notification Service Import Check:

```bash
grep -r "from.*notification.service" apps/backend/src/modules/ --include="*.ts"
```

All imports should still resolve.
</verification>

<success_criteria>

- [ ] policy-case-association.service.ts under 400 LOC
- [ ] notification.service.ts under 400 LOC
- [ ] campaign-scheduling.service.ts under 400 LOC
- [ ] 6 new focused services created
- [ ] All 12 original fat services now under 400 LOC
- [ ] NotificationService public API preserved
- [ ] Backend compiles without TypeScript errors
      </success_criteria>

<output>
After completion, create `.planning/phases/35-code-quality-architecture/35-04-SUMMARY.md`
</output>
