---
phase: 29-error-handling-reliability
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/policies/associations/policy-case-association.service.ts
  - apps/backend/src/modules/notifications/services/email-template.service.ts
  - apps/backend/src/modules/reporting/report-template.service.ts
  - apps/backend/src/modules/ai/services/prompt.service.ts
  - apps/backend/src/modules/messaging/messaging.controller.ts
  - apps/backend/src/modules/tables/user-table.controller.ts
  - apps/backend/src/modules/analytics/ai-query/ai-query.service.ts
  - apps/backend/src/modules/analytics/exports/pdf-generator.service.ts
  - apps/backend/src/modules/portals/employee/manager-proxy.service.ts
  - apps/backend/src/modules/rius/riu-access.service.ts
  - apps/backend/src/modules/notifications/services/notification.service.ts
autonomous: true

must_haves:
  truths:
    - "Service and controller files use NestJS HTTP exceptions instead of bare throw new Error()"
    - "Exception types match the error context (BadRequest for validation, NotFound for missing resources, etc.)"
    - "Event class constructors retain bare throw new Error() for internal validation (documented as intentional)"
  artifacts:
    - path: "apps/backend/src/modules/policies/associations/policy-case-association.service.ts"
      provides: "NestJS exceptions for policy-case association errors"
      contains: "BadRequestException|NotFoundException"
    - path: "apps/backend/src/modules/notifications/services/email-template.service.ts"
      provides: "NestJS exceptions for template errors"
      contains: "NotFoundException|BadRequestException"
    - path: "apps/backend/src/modules/messaging/messaging.controller.ts"
      provides: "NestJS exceptions for messaging validation"
      contains: "BadRequestException|UnauthorizedException"
  key_links:
    - from: "service files"
      to: "HttpExceptionFilter"
      via: "NestJS exception classes"
      pattern: "throw new (BadRequestException|NotFoundException|ConflictException)"
---

<objective>
Replace bare `throw new Error()` statements in service and controller files with appropriate NestJS HTTP exceptions. This ensures errors flow through the global exception filter and return proper HTTP status codes.

Purpose: Address ERR-01 from the code review audit. Bare `throw new Error()` bypasses NestJS exception filters, returning generic 500 errors instead of semantic HTTP status codes.

Output: 11 modified service/controller files using typed NestJS exceptions.

**Note on event files:** The RESEARCH file (Pattern 1, Pitfall 2) confirms that event class constructor throws are APPROPRIATE for internal validation. These are not HTTP requests and should remain as bare `throw new Error()`. Plan 29-02 addresses handler-level try-catch for these.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-error-handling-reliability/29-RESEARCH.md

# Source files to modify

@apps/backend/src/modules/policies/associations/policy-case-association.service.ts
@apps/backend/src/modules/notifications/services/email-template.service.ts
@apps/backend/src/modules/reporting/report-template.service.ts
@apps/backend/src/modules/ai/services/prompt.service.ts
@apps/backend/src/modules/messaging/messaging.controller.ts
@apps/backend/src/modules/tables/user-table.controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace bare throws in high-instance service files (19 instances)</name>
  <files>
apps/backend/src/modules/policies/associations/policy-case-association.service.ts
apps/backend/src/modules/notifications/services/email-template.service.ts
apps/backend/src/modules/reporting/report-template.service.ts
apps/backend/src/modules/ai/services/prompt.service.ts
  </files>
  <action>
Replace bare `throw new Error()` with appropriate NestJS HTTP exceptions in the top 4 service files.

**For each file, analyze the error context and choose the appropriate exception:**

| Error Context                             | NestJS Exception               | HTTP Status |
| ----------------------------------------- | ------------------------------ | ----------- |
| Missing required input/validation failure | `BadRequestException`          | 400         |
| Resource not found                        | `NotFoundException`            | 404         |
| Duplicate resource                        | `ConflictException`            | 409         |
| Permission denied                         | `ForbiddenException`           | 403         |
| External service failure                  | `ServiceUnavailableException`  | 503         |
| Semantic validation error                 | `UnprocessableEntityException` | 422         |
| Unexpected internal error                 | `InternalServerErrorException` | 500         |

**Transformation pattern:**

```typescript
// BEFORE
throw new Error("Template not found");

// AFTER
import { NotFoundException } from "@nestjs/common";
throw new NotFoundException("Template not found");
```

```typescript
// BEFORE
throw new Error("Invalid policy ID format");

// AFTER
import { BadRequestException } from "@nestjs/common";
throw new BadRequestException("Invalid policy ID format");
```

**File-specific guidance:**

1. **policy-case-association.service.ts** (7 instances):
   - "Policy not found" -> `NotFoundException`
   - "Case not found" -> `NotFoundException`
   - "Invalid association type" -> `BadRequestException`
   - "Association already exists" -> `ConflictException`

2. **email-template.service.ts** (5 instances):
   - "Template not found" -> `NotFoundException`
   - "Invalid template variables" -> `BadRequestException`
   - "Template rendering failed" -> `InternalServerErrorException`

3. **report-template.service.ts** (4 instances):
   - "Report not found" -> `NotFoundException`
   - "Invalid report configuration" -> `BadRequestException`

4. **prompt.service.ts** (3 instances):
   - "Prompt not found" -> `NotFoundException`
   - "Invalid prompt format" -> `BadRequestException`

**Import additions:** Add missing imports from `@nestjs/common` at the top of each file.

Follow Pattern 1 from 29-RESEARCH.md exactly.
</action>
<verify>

1. `cd apps/backend && npx tsc --noEmit` - TypeScript compiles
2. `grep -c "throw new Error" src/modules/policies/associations/policy-case-association.service.ts` - should return 0
3. `grep -c "throw new Error" src/modules/notifications/services/email-template.service.ts` - should return 0
4. `grep -c "throw new Error" src/modules/reporting/report-template.service.ts` - should return 0
5. `grep -c "throw new Error" src/modules/ai/services/prompt.service.ts` - should return 0
   </verify>
   <done>Top 4 service files use NestJS HTTP exceptions (19 instances replaced)</done>
   </task>

<task type="auto">
  <name>Task 2: Replace bare throws in controllers and remaining services (12 instances)</name>
  <files>
apps/backend/src/modules/messaging/messaging.controller.ts
apps/backend/src/modules/tables/user-table.controller.ts
apps/backend/src/modules/analytics/ai-query/ai-query.service.ts
apps/backend/src/modules/analytics/exports/pdf-generator.service.ts
apps/backend/src/modules/portals/employee/manager-proxy.service.ts
apps/backend/src/modules/rius/riu-access.service.ts
apps/backend/src/modules/notifications/services/notification.service.ts
  </files>
  <action>
Replace bare `throw new Error()` in controllers and remaining service files.

**Controller-specific guidance:**

1. **messaging.controller.ts** (2 instances):
   - Input validation failures -> `BadRequestException`
   - Missing conversation/message -> `NotFoundException`

2. **user-table.controller.ts** (1 instance):
   - Invalid table configuration -> `BadRequestException`

**Service-specific guidance:**

3. **ai-query.service.ts** (1 instance):
   - Query execution failure -> `BadRequestException` (user query issue) or `InternalServerErrorException` (system issue)

4. **pdf-generator.service.ts** (1 instance):
   - PDF generation failure -> `InternalServerErrorException`

5. **manager-proxy.service.ts** (1 instance):
   - Employee not found -> `NotFoundException`
   - Permission denied -> `ForbiddenException`

6. **riu-access.service.ts** (1 instance):
   - RIU not found -> `NotFoundException`
   - Access denied -> `ForbiddenException`

7. **notification.service.ts** (1 instance):
   - Notification not found -> `NotFoundException`

**Import pattern:**

```typescript
import {
  BadRequestException,
  NotFoundException,
  ForbiddenException,
  InternalServerErrorException,
} from "@nestjs/common";
```

Follow Pattern 1 from 29-RESEARCH.md exactly.
</action>
<verify>

1. `cd apps/backend && npx tsc --noEmit` - TypeScript compiles
2. `grep -c "throw new Error" src/modules/messaging/messaging.controller.ts` - should return 0
3. `grep -c "throw new Error" src/modules/tables/user-table.controller.ts` - should return 0
4. `grep "throw new" src/modules/messaging/messaging.controller.ts | head -5` - shows NestJS exceptions
   </verify>
   <done>Controllers and remaining services use NestJS HTTP exceptions (12 instances replaced)</done>
   </task>

</tasks>

<verification>
Run all checks in sequence:

```bash
cd apps/backend
npx tsc --noEmit
npm run lint -- --max-warnings=0
npm test -- --passWithNoTests

# Count remaining bare throws in service/controller files (should be 0)
grep -r "throw new Error" src/modules --include="*.service.ts" | wc -l
grep -r "throw new Error" src/modules --include="*.controller.ts" | wc -l

# Verify NestJS exceptions are used
grep -r "throw new (BadRequestException\|NotFoundException\|ConflictException)" src/modules --include="*.service.ts" | head -10
```

**Expected outcome:**

- 0 bare `throw new Error()` in service files
- 0 bare `throw new Error()` in controller files
- 31+ NestJS exception usages across modified files
  </verification>

<success_criteria>

1. All 11 service/controller files use NestJS HTTP exceptions instead of bare throws
2. Exception types match error context (404 for not found, 400 for validation, etc.)
3. All TypeScript compiles without errors
4. All lint rules pass
5. Event files (.events.ts) remain unchanged (internal validation is intentional)
   </success_criteria>

<output>
After completion, create `.planning/phases/29-error-handling-reliability/29-05-SUMMARY.md`
</output>
