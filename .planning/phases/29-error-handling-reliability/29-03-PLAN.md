---
phase: 29-error-handling-reliability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/(authenticated)/error.tsx
  - apps/frontend/src/app/(authenticated)/analytics/error.tsx
  - apps/frontend/src/app/(authenticated)/campaigns/error.tsx
  - apps/frontend/src/app/(authenticated)/cases/error.tsx
  - apps/frontend/src/app/(authenticated)/dashboard/error.tsx
  - apps/frontend/src/app/(authenticated)/disclosures/error.tsx
  - apps/frontend/src/app/(authenticated)/forms/error.tsx
  - apps/frontend/src/app/(authenticated)/help/error.tsx
  - apps/frontend/src/app/(authenticated)/intake-forms/error.tsx
  - apps/frontend/src/app/(authenticated)/investigations/error.tsx
  - apps/frontend/src/app/(authenticated)/my-work/error.tsx
  - apps/frontend/src/app/(authenticated)/notifications/error.tsx
  - apps/frontend/src/app/(authenticated)/policies/error.tsx
  - apps/frontend/src/app/(authenticated)/profile/error.tsx
  - apps/frontend/src/app/(authenticated)/projects/error.tsx
  - apps/frontend/src/app/(authenticated)/reports/error.tsx
  - apps/frontend/src/app/(authenticated)/search/error.tsx
  - apps/frontend/src/app/(authenticated)/settings/error.tsx
  - apps/frontend/src/app/ethics/[tenant]/error.tsx
  - apps/frontend/src/app/employee/error.tsx
  - apps/frontend/src/app/internal/error.tsx
  - apps/frontend/src/app/operator/error.tsx
  - apps/frontend/src/app/global-error.tsx
autonomous: true

must_haves:
  truths:
    - "Error boundary exists for all top-level route segments under (authenticated)"
    - "Error boundary exists for public portals (ethics, employee, internal, operator)"
    - "Global error boundary exists for root layout errors"
    - "All error boundaries show user-friendly message with Try Again and Go Back buttons"
  artifacts:
    - path: "apps/frontend/src/app/(authenticated)/error.tsx"
      provides: "Catch-all error boundary for authenticated routes"
      contains: "use client"
    - path: "apps/frontend/src/app/global-error.tsx"
      provides: "Root layout error boundary"
      contains: "use client"
  key_links:
    - from: "error.tsx files"
      to: "Next.js error recovery"
      via: "reset() function"
      pattern: "reset\\(\\)"
---

<objective>
Add error boundary components to all top-level route segments, providing graceful error recovery throughout the application. Currently only `/cases/[id]/error.tsx` exists.

Purpose: Address ERR-05 from the code review audit. Without error boundaries, runtime errors crash the entire application instead of showing recovery options.
Output: 22+ new error.tsx files covering all route segments plus a global-error.tsx for root layout failures.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-error-handling-reliability/29-RESEARCH.md

# Existing error boundary for reference

@apps/frontend/src/app/(authenticated)/cases/[id]/error.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable RouteError component and authenticated route boundaries</name>
  <files>
apps/frontend/src/components/route-error.tsx
apps/frontend/src/app/(authenticated)/error.tsx
apps/frontend/src/app/(authenticated)/analytics/error.tsx
apps/frontend/src/app/(authenticated)/campaigns/error.tsx
apps/frontend/src/app/(authenticated)/cases/error.tsx
apps/frontend/src/app/(authenticated)/dashboard/error.tsx
apps/frontend/src/app/(authenticated)/disclosures/error.tsx
apps/frontend/src/app/(authenticated)/forms/error.tsx
apps/frontend/src/app/(authenticated)/help/error.tsx
apps/frontend/src/app/(authenticated)/intake-forms/error.tsx
apps/frontend/src/app/(authenticated)/investigations/error.tsx
apps/frontend/src/app/(authenticated)/my-work/error.tsx
apps/frontend/src/app/(authenticated)/notifications/error.tsx
apps/frontend/src/app/(authenticated)/policies/error.tsx
apps/frontend/src/app/(authenticated)/profile/error.tsx
apps/frontend/src/app/(authenticated)/projects/error.tsx
apps/frontend/src/app/(authenticated)/reports/error.tsx
apps/frontend/src/app/(authenticated)/search/error.tsx
apps/frontend/src/app/(authenticated)/settings/error.tsx
  </files>
  <action>
1. Create a reusable RouteError component at `apps/frontend/src/components/route-error.tsx`:

```typescript
'use client';

import { useEffect } from 'react';
import { AlertCircle, RefreshCw, ArrowLeft } from 'lucide-react';
import { Button } from '@/components/ui/button';

interface RouteErrorProps {
  error: Error & { digest?: string };
  reset: () => void;
  title?: string;
}

/**
 * Reusable error boundary component for Next.js route segments.
 * Displays error message with Try Again and Go Back actions.
 */
export function RouteError({ error, reset, title = 'Something went wrong' }: RouteErrorProps) {
  useEffect(() => {
    // Log to error reporting service
    console.error('Route error:', error);
  }, [error]);

  return (
    <div className="flex flex-col items-center justify-center min-h-[400px] p-8">
      <AlertCircle className="h-12 w-12 text-destructive mb-4" />
      <h2 className="text-xl font-semibold mb-2">{title}</h2>
      <p className="text-muted-foreground mb-6 text-center max-w-md">
        {error.message || 'An unexpected error occurred. Please try again.'}
      </p>
      {error.digest && (
        <p className="text-xs text-muted-foreground mb-4">
          Error ID: {error.digest}
        </p>
      )}
      <div className="flex gap-3">
        <Button onClick={reset} variant="default">
          <RefreshCw className="h-4 w-4 mr-2" />
          Try Again
        </Button>
        <Button variant="outline" onClick={() => window.history.back()}>
          <ArrowLeft className="h-4 w-4 mr-2" />
          Go Back
        </Button>
      </div>
    </div>
  );
}
```

2. Create error.tsx files for each authenticated route segment. Each file follows this pattern:

```typescript
'use client';

import { RouteError } from '@/components/route-error';

export default function PageError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return <RouteError error={error} reset={reset} title="Failed to load [page name]" />;
}
```

Create for all 18 authenticated routes:

- `(authenticated)/error.tsx` - title: "Something went wrong" (catch-all)
- `analytics/error.tsx` - title: "Failed to load analytics"
- `campaigns/error.tsx` - title: "Failed to load campaigns"
- `cases/error.tsx` - title: "Failed to load cases"
- `dashboard/error.tsx` - title: "Failed to load dashboard"
- `disclosures/error.tsx` - title: "Failed to load disclosures"
- `forms/error.tsx` - title: "Failed to load forms"
- `help/error.tsx` - title: "Failed to load help"
- `intake-forms/error.tsx` - title: "Failed to load intake forms"
- `investigations/error.tsx` - title: "Failed to load investigations"
- `my-work/error.tsx` - title: "Failed to load My Work"
- `notifications/error.tsx` - title: "Failed to load notifications"
- `policies/error.tsx` - title: "Failed to load policies"
- `profile/error.tsx` - title: "Failed to load profile"
- `projects/error.tsx` - title: "Failed to load projects"
- `reports/error.tsx` - title: "Failed to load reports"
- `search/error.tsx` - title: "Failed to load search"
- `settings/error.tsx` - title: "Failed to load settings"

Follow Pattern 5 from 29-RESEARCH.md.
</action>
<verify>

1. `cd apps/frontend && npx tsc --noEmit` - TypeScript compiles
2. `find src/app/\\(authenticated\\) -name "error.tsx" | wc -l` - shows 18+ files
3. `grep -l "RouteError" src/app/\\(authenticated\\)/*/error.tsx` - shows RouteError usage
   </verify>
   <done>All authenticated route segments have error boundaries using the reusable RouteError component</done>
   </task>

<task type="auto">
  <name>Task 2: Create portal error boundaries and global-error.tsx</name>
  <files>
apps/frontend/src/app/ethics/[tenant]/error.tsx
apps/frontend/src/app/employee/error.tsx
apps/frontend/src/app/internal/error.tsx
apps/frontend/src/app/operator/error.tsx
apps/frontend/src/app/global-error.tsx
  </files>
  <action>
1. Create error boundaries for public/portal routes:

**apps/frontend/src/app/ethics/[tenant]/error.tsx:**

```typescript
'use client';

import { RouteError } from '@/components/route-error';

export default function EthicsPortalError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return <RouteError error={error} reset={reset} title="Failed to load reporting portal" />;
}
```

**apps/frontend/src/app/employee/error.tsx:**

```typescript
'use client';

import { RouteError } from '@/components/route-error';

export default function EmployeePortalError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return <RouteError error={error} reset={reset} title="Failed to load employee portal" />;
}
```

**apps/frontend/src/app/internal/error.tsx:**

```typescript
'use client';

import { RouteError } from '@/components/route-error';

export default function InternalToolsError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return <RouteError error={error} reset={reset} title="Failed to load internal tools" />;
}
```

**apps/frontend/src/app/operator/error.tsx:**

```typescript
'use client';

import { RouteError } from '@/components/route-error';

export default function OperatorConsoleError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  return <RouteError error={error} reset={reset} title="Failed to load operator console" />;
}
```

2. Create global-error.tsx for root layout errors:

**apps/frontend/src/app/global-error.tsx:**

```typescript
'use client';

import { useEffect } from 'react';
import { AlertCircle, RefreshCw, Home } from 'lucide-react';

/**
 * Global error boundary for root layout errors.
 * Must include its own html/body tags since layout may have failed.
 */
export default function GlobalError({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    console.error('Global error:', error);
  }, [error]);

  return (
    <html>
      <body className="min-h-screen flex items-center justify-center bg-background">
        <div className="flex flex-col items-center justify-center p-8 max-w-md text-center">
          <AlertCircle className="h-16 w-16 text-destructive mb-6" />
          <h1 className="text-2xl font-bold mb-2">Application Error</h1>
          <p className="text-muted-foreground mb-6">
            {error.message || 'A critical error occurred. Please refresh the page.'}
          </p>
          {error.digest && (
            <p className="text-xs text-muted-foreground mb-6">
              Error ID: {error.digest}
            </p>
          )}
          <div className="flex gap-3">
            <button
              onClick={reset}
              className="inline-flex items-center px-4 py-2 bg-primary text-primary-foreground rounded-md hover:bg-primary/90"
            >
              <RefreshCw className="h-4 w-4 mr-2" />
              Try Again
            </button>
            <a
              href="/"
              className="inline-flex items-center px-4 py-2 border border-input rounded-md hover:bg-accent"
            >
              <Home className="h-4 w-4 mr-2" />
              Go Home
            </a>
          </div>
        </div>
      </body>
    </html>
  );
}
```

Note: global-error.tsx uses native HTML/inline styles because shadcn/ui components may not be available if the root layout failed. Keep it minimal and self-contained.
</action>
<verify>

1. `cd apps/frontend && npx tsc --noEmit` - TypeScript compiles
2. `ls -la src/app/global-error.tsx` - global error exists
3. `ls -la src/app/ethics/*/error.tsx src/app/employee/error.tsx src/app/internal/error.tsx src/app/operator/error.tsx` - portal errors exist
   </verify>
   <done>Portal error boundaries and global-error.tsx created for complete error coverage</done>
   </task>

</tasks>

<verification>
Run all checks:

```bash
cd apps/frontend
npx tsc --noEmit
npm run lint -- --max-warnings=0

# Count error boundary files
find src/app -name "error.tsx" | wc -l
# Should be >= 23 (18 authenticated + 1 (authenticated) catch-all + 4 portals = 23, plus the existing [id]/error.tsx = 24)

# Verify global-error exists
ls -la src/app/global-error.tsx
```

</verification>

<success_criteria>

1. RouteError reusable component exists at src/components/route-error.tsx
2. All 18 authenticated route segments have error.tsx files
3. Catch-all error.tsx exists at (authenticated)/error.tsx
4. All 4 portal routes have error.tsx files
5. global-error.tsx exists at src/app/global-error.tsx
6. All files use 'use client' directive
7. All TypeScript compiles without errors
8. All lint rules pass
   </success_criteria>

<output>
After completion, create `.planning/phases/29-error-handling-reliability/29-03-SUMMARY.md`
</output>
