---
phase: 38-dark-mode-gap-closure
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/frontend/src/components/projects/TaskFileList.tsx
  - apps/frontend/src/components/projects/TaskDependencyList.tsx
  - apps/frontend/src/components/projects/TaskActivityLog.tsx
  - apps/frontend/src/components/projects/TaskDetailPanel.tsx
  - apps/frontend/src/components/projects/TaskRow.tsx
  - apps/frontend/src/components/projects/TaskSubscriberList.tsx
  - apps/frontend/src/components/projects/TaskUpdateThread.tsx
autonomous: true

must_haves:
  truths:
    - "Project task components render correctly in dark mode"
    - "Task detail panels and lists have proper contrast"
    - "Activity logs and update threads are readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/projects/TaskFileList.tsx"
      provides: "Task file list with semantic tokens"
      contains: "bg-card"
    - path: "apps/frontend/src/components/projects/TaskActivityLog.tsx"
      provides: "Activity log with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "task components"
      to: "project detail page"
      via: "task panel imports"
      pattern: "TaskDetailPanel|TaskFileList|TaskActivityLog"
---

<objective>
Migrate high-occurrence project task components (7 files, ~60 occurrences) to semantic tokens.

Purpose: Project management is a key module. These task-related components have the highest occurrence counts in projects/.

Output: 7 project task components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TaskFileList.tsx and TaskDependencyList.tsx (21 occurrences)</name>
  <files>
apps/frontend/src/components/projects/TaskFileList.tsx
apps/frontend/src/components/projects/TaskDependencyList.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For TaskFileList.tsx (11 occurrences):

- File list container: `bg-white` -> `bg-card`
- File items: `hover:bg-gray-50` -> `hover:bg-muted`
- File name: `text-gray-900` -> `text-foreground`
- File size/date: `text-gray-500` -> `text-muted-foreground`
- File type icons: `text-gray-400` -> `text-muted-foreground`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- Upload area: `bg-gray-50 border-gray-*` -> `bg-muted border-border`
- Borders: `border-gray-*` -> `border-border`

For TaskDependencyList.tsx (10 occurrences):

- Dependency list: `bg-white` -> `bg-card`
- Dependency items: `hover:bg-gray-50` -> `hover:bg-muted`
- Task name: `text-gray-900` -> `text-foreground`
- Dependency type badge: Add dark: variants
- Status indicator: Ensure visible in dark mode
- Remove button: `text-gray-400 hover:text-gray-600` -> `text-muted-foreground hover:text-foreground`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/projects/TaskFileList.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/projects/TaskDependencyList.tsx
```

Both should return 0 matches.
</verify>
<done>TaskFileList.tsx and TaskDependencyList.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TaskActivityLog.tsx and TaskDetailPanel.tsx (15+ occurrences)</name>
  <files>
apps/frontend/src/components/projects/TaskActivityLog.tsx
apps/frontend/src/components/projects/TaskDetailPanel.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For TaskActivityLog.tsx (9 occurrences):

- Activity log container: `bg-white` -> `bg-card`
- Activity items: `hover:bg-gray-50` -> `hover:bg-muted`
- Activity type icon: `text-gray-400` -> `text-muted-foreground`
- Activity description: `text-gray-700` -> `text-foreground`
- Timestamp: `text-gray-500` -> `text-muted-foreground`
- User avatar fallback: Keep gray for avatar placeholders
- Timeline line: `bg-gray-200` -> `bg-border`
- Borders: `border-gray-*` -> `border-border`

For TaskDetailPanel.tsx:
This is the main task detail slide-over panel with tabs.

- Panel container: `bg-white` -> `bg-card`
- Panel header: `bg-gray-50` -> `bg-muted`
- Task title: `text-gray-900` -> `text-foreground`
- Tab headers: Ensure active/inactive states use semantic tokens
- Tab content areas: `bg-white` -> `bg-card`
- Field labels: `text-gray-500` -> `text-muted-foreground`
- Field values: `text-gray-900` -> `text-foreground`
- Close button: `text-gray-400` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/projects/TaskActivityLog.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/projects/TaskDetailPanel.tsx
```

Both should return 0 matches.
</verify>
<done>TaskActivityLog.tsx and TaskDetailPanel.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate TaskRow.tsx, TaskSubscriberList.tsx, TaskUpdateThread.tsx</name>
  <files>
apps/frontend/src/components/projects/TaskRow.tsx
apps/frontend/src/components/projects/TaskSubscriberList.tsx
apps/frontend/src/components/projects/TaskUpdateThread.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For TaskRow.tsx:

- Row container: `hover:bg-gray-50` -> `hover:bg-muted`
- Task name: `text-gray-900` -> `text-foreground`
- Inline edit: `bg-white` -> `bg-background`
- Status column: Colored badges - add dark: variants
- Priority column: Use priorityColors from theme-colors.ts
- Date columns: `text-gray-600` -> `text-muted-foreground`
- Assignee avatar: Keep as-is for avatars
- Borders: `border-gray-*` -> `border-border`

For TaskSubscriberList.tsx:

- List container: `bg-white` -> `bg-card`
- Subscriber items: `hover:bg-gray-50` -> `hover:bg-muted`
- Subscriber name: `text-gray-900` -> `text-foreground`
- Subscriber email/role: `text-gray-500` -> `text-muted-foreground`
- Remove button: `text-gray-400` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`

For TaskUpdateThread.tsx:

- Thread container: `bg-white` -> `bg-card`
- Update items: May have `bg-gray-50` for alternating - convert to `bg-muted/50`
- Update author: `text-gray-900` -> `text-foreground`
- Update text: `text-gray-700` -> `text-foreground`
- Timestamp: `text-gray-500` -> `text-muted-foreground`
- Reply button: `text-gray-500 hover:text-gray-700` -> `text-muted-foreground hover:text-foreground`
- Nested replies: Maintain visual hierarchy with semantic tokens
  </action>
  <verify>
  Run:

```bash
for f in TaskRow TaskSubscriberList TaskUpdateThread; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/projects/$f.tsx" || echo "CLEAN"
done
```

All 3 should show "CLEAN".
</verify>
<done>TaskRow.tsx, TaskSubscriberList.tsx, and TaskUpdateThread.tsx contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all 7 migrated files:

```bash
for f in TaskFileList TaskDependencyList TaskActivityLog TaskDetailPanel TaskRow TaskSubscriberList TaskUpdateThread; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/projects/$f.tsx" || echo "CLEAN"
done
```

All 7 should show "CLEAN".

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- 7 high-occurrence project task components migrated (~60 occurrences)
- Task detail panel and all tabs render correctly in dark mode
- Activity logs and update threads have proper contrast
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-07-SUMMARY.md`
</output>
