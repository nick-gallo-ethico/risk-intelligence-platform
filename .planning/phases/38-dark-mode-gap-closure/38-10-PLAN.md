---
phase: 38-dark-mode-gap-closure
plan: 10
type: execute
wave: 4
depends_on: []
files_modified:
  - apps/frontend/src/components/settings/role-permissions-table.tsx
  - apps/frontend/src/components/settings/user-list.tsx
  - apps/frontend/src/components/conflicts/ConflictAlert.tsx
  - apps/frontend/src/components/conflicts/EntityTimeline.tsx
  - apps/frontend/src/components/views/DataTable.tsx
  - apps/frontend/src/components/users/users-table.tsx
  - apps/frontend/src/components/help/ticket-list.tsx
  - apps/frontend/src/components/operator/riu-type-selector.tsx
  - apps/frontend/src/components/ethics/status-badge.tsx
  - apps/frontend/src/components/analytics/dashboard-template-picker.tsx
  - apps/frontend/src/components/auth/mfa-setup.tsx
  - apps/frontend/src/components/ui/association-card.tsx
autonomous: true

must_haves:
  truths:
    - "Settings and user management components render correctly in dark mode"
    - "DataTable fallback badges have dark mode variants (THEME-06)"
    - "All remaining component domains have zero hardcoded colors"
  artifacts:
    - path: "apps/frontend/src/components/views/DataTable.tsx"
      provides: "DataTable with dark mode fallback badges"
      contains: "dark:bg-gray-800"
    - path: "apps/frontend/src/components/users/users-table.tsx"
      provides: "Users table with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "DataTable"
      to: "all list views"
      via: "table imports"
      pattern: "DataTable"
---

<objective>
Migrate remaining miscellaneous components (12 files) including the critical DataTable fallback fix (THEME-06).

Purpose: Complete all remaining component domains (settings, conflicts, views, users, help, operator, ethics, analytics, auth, ui).

Output: All remaining components use semantic tokens. DataTable THEME-06 requirement satisfied.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

## THEME-06 DataTable Fix

DataTable has fallback badge colors that need dark: variants:

- Line ~216: `bg-gray-100 text-gray-800` -> add `dark:bg-gray-800 dark:text-gray-300 dark:border-gray-700`
- Line ~240: Same pattern for severity fallback
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DataTable fallbacks (THEME-06) and migrate users/settings (4 files)</name>
  <files>
apps/frontend/src/components/views/DataTable.tsx
apps/frontend/src/components/users/users-table.tsx
apps/frontend/src/components/settings/role-permissions-table.tsx
apps/frontend/src/components/settings/user-list.tsx
  </files>
  <action>
Read all 4 files and migrate hardcoded colors:

For DataTable.tsx (CRITICAL - THEME-06):
This is the main data table component used across the platform.

The research identified 2 fallback badge occurrences:

- Status column fallback: `bg-gray-100 text-gray-800` needs dark: variants
- Severity column fallback: Same pattern

Find and update:

```typescript
// BEFORE
const colorClass =
  statusColors[statusValue] || "bg-gray-100 text-gray-800 border-gray-200";

// AFTER
const colorClass =
  statusColors[statusValue] ||
  "bg-gray-100 text-gray-800 border-gray-200 dark:bg-gray-800 dark:text-gray-300 dark:border-gray-700";
```

Apply to both status and severity fallbacks.

For users-table.tsx:

- Table container: `bg-white` -> `bg-card`
- Row hover: `hover:bg-gray-50` -> `hover:bg-muted`
- User names: `text-gray-900` -> `text-foreground`
- Email: `text-gray-500` -> `text-muted-foreground`
- Role badges: Use ROLE_COLORS pattern from 22-12 with dark: variants

For role-permissions-table.tsx:

- Table: `bg-white` -> `bg-card`
- Headers: `bg-gray-50` -> `bg-muted`
- Permission checkboxes area: `hover:bg-gray-50` -> `hover:bg-muted`
- Labels: `text-gray-*` -> semantic tokens

For user-list.tsx:

- List container: `bg-white` -> `bg-card`
- List items: `hover:bg-gray-50` -> `hover:bg-muted`
- User info: `text-gray-*` -> semantic tokens
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/views/DataTable.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/users/users-table.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/settings/role-permissions-table.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/settings/user-list.tsx
```

All should return 0 matches OR only show lines that now include `dark:` variants.
</verify>
<done>DataTable.tsx has dark: variants on fallback badges (THEME-06 satisfied), users-table.tsx, role-permissions-table.tsx, and user-list.tsx migrated</done>
</task>

<task type="auto">
  <name>Task 2: Migrate conflicts and implementation components (6 files)</name>
  <files>
apps/frontend/src/components/conflicts/ConflictAlert.tsx
apps/frontend/src/components/conflicts/EntityTimeline.tsx
apps/frontend/src/components/implementation/BlockerCard.tsx
apps/frontend/src/components/implementation/ChecklistPanel.tsx
apps/frontend/src/components/implementation/GoLiveChecklist.tsx
apps/frontend/src/components/implementation/ProjectCard.tsx
  </files>
  <action>
Read all 6 files and migrate hardcoded colors:

For ConflictAlert.tsx:

- Alert container: May use warning/error backgrounds
- Alert text: `text-gray-*` -> semantic tokens
- Alert icon: Ensure visible in dark mode
- Borders: `border-gray-*` -> `border-border`

For EntityTimeline.tsx:

- Timeline container: `bg-white` -> `bg-card`
- Timeline line: `bg-gray-200` -> `bg-border`
- Event cards: `bg-gray-50` -> `bg-muted`
- Event text: `text-gray-*` -> semantic tokens
- Timestamps: `text-gray-500` -> `text-muted-foreground`

For BlockerCard.tsx:

- Card: `bg-white` -> `bg-card`
- Blocker status indicator: Ensure colored indicators have dark: variants
- Text: `text-gray-*` -> semantic tokens

For ChecklistPanel.tsx:

- Panel: `bg-white` -> `bg-card`
- Checklist items: `hover:bg-gray-50` -> `hover:bg-muted`
- Item text: `text-gray-*` -> semantic tokens

For GoLiveChecklist.tsx:

- Checklist container: `bg-white` -> `bg-card`
- Gate states (passed/blocked/warning): Use dark: variants from 22-12
- Step labels: `text-gray-*` -> semantic tokens

For ProjectCard.tsx:

- Card: `bg-white` -> `bg-card`
- Project name: `text-gray-900` -> `text-foreground`
- Metadata: `text-gray-500` -> `text-muted-foreground`
  </action>
  <verify>
  Run:

```bash
for f in ConflictAlert EntityTimeline; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/conflicts/$f.tsx" || echo "CLEAN"
done

for f in BlockerCard ChecklistPanel GoLiveChecklist ProjectCard; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/implementation/$f.tsx" || echo "CLEAN"
done
```

All 6 should show "CLEAN".
</verify>
<done>All 6 conflicts and implementation components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining miscellaneous components (4 files)</name>
  <files>
apps/frontend/src/components/help/ticket-list.tsx
apps/frontend/src/components/operator/riu-type-selector.tsx
apps/frontend/src/components/ethics/status-badge.tsx
apps/frontend/src/components/analytics/dashboard-template-picker.tsx
apps/frontend/src/components/auth/mfa-setup.tsx
apps/frontend/src/components/ui/association-card.tsx
  </files>
  <action>
Read all 6 files and migrate hardcoded colors:

For ticket-list.tsx:

- List container: `bg-white` -> `bg-card`
- Ticket items: `hover:bg-gray-50` -> `hover:bg-muted`
- Ticket subject: `text-gray-900` -> `text-foreground`
- Status/priority: Add dark: variants
- Metadata: `text-gray-500` -> `text-muted-foreground`

For riu-type-selector.tsx:

- Selector container: `bg-white` -> `bg-card`
- Type options: `hover:bg-gray-50` -> `hover:bg-muted`
- Type labels: `text-gray-*` -> semantic tokens
- Selected indicator: Add dark: variant if colored

For status-badge.tsx:

- Badge styles: Should already use theme-colors.ts or need dark: variants
- Ensure all badge variants have dark mode support

For dashboard-template-picker.tsx:

- Picker container: `bg-white` -> `bg-card`
- Template cards: `hover:bg-gray-50` -> `hover:bg-muted`
- Template names: `text-gray-900` -> `text-foreground`
- Descriptions: `text-gray-500` -> `text-muted-foreground`

For mfa-setup.tsx:

- Setup container: `bg-white` -> `bg-card`
- Instructions: `text-gray-*` -> semantic tokens
- QR code area: Ensure visible background
- Code inputs: `bg-white` -> `bg-background`

For association-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Card header: `bg-gray-50` -> `bg-muted`
- Count badge: Ensure visible in dark mode
- View all link: Ensure link color has dark: variant
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/help/ticket-list.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/operator/riu-type-selector.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/ethics/status-badge.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/analytics/dashboard-template-picker.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/auth/mfa-setup.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/ui/association-card.tsx
```

All 6 should return 0 matches.
</verify>
<done>All 6 remaining miscellaneous components contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Verify all remaining component directories are clean:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/settings/ apps/frontend/src/components/conflicts/ apps/frontend/src/components/views/ apps/frontend/src/components/users/ apps/frontend/src/components/help/ apps/frontend/src/components/operator/ apps/frontend/src/components/ethics/ apps/frontend/src/components/analytics/ apps/frontend/src/components/auth/ apps/frontend/src/components/ui/ apps/frontend/src/components/implementation/
```

Should return 0 matches or only lines with dark: variants.

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- DataTable THEME-06 requirement satisfied (fallback badges have dark: variants)
- All 12 remaining miscellaneous components migrated
- All component directories clean of hardcoded gray/white colors
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-10-SUMMARY.md`
</output>
