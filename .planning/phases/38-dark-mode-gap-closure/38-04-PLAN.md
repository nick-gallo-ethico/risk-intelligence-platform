---
phase: 38-dark-mode-gap-closure
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/frontend/src/components/cases/merge-modal.tsx
  - apps/frontend/src/components/cases/linked-riu-form-answers.tsx
  - apps/frontend/src/components/cases/summary-tab.tsx
  - apps/frontend/src/components/cases/assign-modal.tsx
  - apps/frontend/src/components/cases/case-info-summary.tsx
  - apps/frontend/src/components/cases/case-properties-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Case merge modal renders correctly in dark mode"
    - "Case summary tab and property panels have proper contrast"
    - "Search results in modals are readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/cases/merge-modal.tsx"
      provides: "Merge modal with semantic tokens"
      contains: "hover:bg-muted"
    - path: "apps/frontend/src/components/cases/linked-riu-form-answers.tsx"
      provides: "RIU form answers with semantic tokens"
      contains: "bg-muted"
    - path: "apps/frontend/src/components/cases/summary-tab.tsx"
      provides: "Summary tab with semantic tokens"
      contains: "text-foreground"
  key_links:
    - from: "cases components"
      to: "case detail page"
      via: "component imports"
      pattern: "merge-modal|summary-tab|case-properties-panel"
---

<objective>
Migrate the highest-occurrence case components (6 files, ~55 occurrences) to semantic tokens.

Purpose: Cases are the core product area. These 6 files include the top-occurrence files: merge-modal (15), linked-riu-form-answers (11), summary-tab (10).

Output: 6 high-impact case components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate merge-modal.tsx and assign-modal.tsx (20 occurrences)</name>
  <files>
apps/frontend/src/components/cases/merge-modal.tsx
apps/frontend/src/components/cases/assign-modal.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For merge-modal.tsx (15 occurrences):

- Modal content area: Dialog component handles background, but inner content may use `bg-white`
- Search input area: `bg-white` -> `bg-background`
- Search results list: `bg-gray-50` -> `bg-muted`
- Result item hover: `hover:bg-gray-50` -> `hover:bg-muted`
- Case preview section: `bg-white` -> `bg-card`
- Case ID text: `text-gray-600` -> `text-muted-foreground`
- Case summary text: `text-gray-700` -> `text-foreground`
- Borders: `border-gray-*` -> `border-border`

For assign-modal.tsx (5 occurrences):

- User list items: `hover:bg-gray-50` -> `hover:bg-muted`
- User names: `text-gray-900` -> `text-foreground`
- User email/role: `text-gray-500` -> `text-muted-foreground`
- Search input: `bg-white` -> `bg-background`
- Selected user highlight: May use colored background - add dark: variant
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/merge-modal.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/assign-modal.tsx
```

Both should return 0 matches.
</verify>
<done>merge-modal.tsx and assign-modal.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate linked-riu-form-answers.tsx and summary-tab.tsx (21 occurrences)</name>
  <files>
apps/frontend/src/components/cases/linked-riu-form-answers.tsx
apps/frontend/src/components/cases/summary-tab.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For linked-riu-form-answers.tsx (11 occurrences):
This file displays form answers from linked RIUs in collapsible sections.

- CollapsibleTrigger: `bg-gray-50 hover:bg-gray-100` -> `bg-muted hover:bg-accent`
- Section title: `text-gray-700` -> `text-foreground`
- Field labels: `text-gray-500` -> `text-muted-foreground`
- Field values: `text-gray-900` -> `text-foreground`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- Card borders: `border-gray-*` -> `border-border`

For summary-tab.tsx (10 occurrences):
This is the case Overview/Summary tab showing key case information.

- Section containers: `bg-white` -> `bg-card`
- Section headers: `bg-gray-50` -> `bg-muted`
- Section titles: `text-gray-900` -> `text-foreground`
- Field labels: `text-gray-500` -> `text-muted-foreground`
- Field values: `text-gray-900` -> `text-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/linked-riu-form-answers.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/summary-tab.tsx
```

Both should return 0 matches.
</verify>
<done>linked-riu-form-answers.tsx and summary-tab.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate case-info-summary.tsx and case-properties-panel.tsx</name>
  <files>
apps/frontend/src/components/cases/case-info-summary.tsx
apps/frontend/src/components/cases/case-properties-panel.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For case-info-summary.tsx:

- Summary card: `bg-white` -> `bg-card`
- Case title: `text-gray-900` -> `text-foreground`
- Metadata labels: `text-gray-500` -> `text-muted-foreground`
- Metadata values: `text-gray-700` -> `text-foreground`
- Severity/status badges: Use theme-colors.ts or add dark: variants
- Borders: `border-gray-*` -> `border-border`

For case-properties-panel.tsx:

- Panel container: `bg-white` -> `bg-card`
- Property group headers: `bg-gray-50` -> `bg-muted`
- Property labels: `text-gray-500` -> `text-muted-foreground`
- Property values: `text-gray-900` -> `text-foreground`
- Edit icons: `text-gray-400` -> `text-muted-foreground`
- Hover states: `hover:bg-gray-50` -> `hover:bg-muted`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/case-info-summary.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/case-properties-panel.tsx
```

Both should return 0 matches.
</verify>
<done>case-info-summary.tsx and case-properties-panel.tsx contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all 6 migrated files:

```bash
for f in merge-modal linked-riu-form-answers summary-tab assign-modal case-info-summary case-properties-panel; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/cases/$f.tsx" || echo "CLEAN"
done
```

All 6 should show "CLEAN".

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- 6 high-impact case components migrated (~55 occurrences)
- All modals (merge, assign) render correctly in dark mode
- Summary/property panels have proper contrast
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-04-SUMMARY.md`
</output>
