---
phase: 38-dark-mode-gap-closure
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/frontend/src/components/projects/AddTaskRow.tsx
  - apps/frontend/src/components/projects/ColumnCenterDialog.tsx
  - apps/frontend/src/components/projects/ColumnConfigPanel.tsx
  - apps/frontend/src/components/projects/MentionInput.tsx
  - apps/frontend/src/components/projects/MilestoneTimeline.tsx
  - apps/frontend/src/components/projects/ProjectBoardView.tsx
  - apps/frontend/src/components/projects/ProjectDashboardView.tsx
  - apps/frontend/src/components/projects/ProjectGroupHeader.tsx
  - apps/frontend/src/components/projects/ProjectTaskTable.tsx
  - apps/frontend/src/components/projects/ProjectWorkloadView.tsx
autonomous: true

must_haves:
  truths:
    - "Project views render correctly in dark mode"
    - "Column configuration dialogs have proper contrast"
    - "Board and workload views are readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/projects/ProjectBoardView.tsx"
      provides: "Board view with semantic tokens"
      contains: "bg-muted"
    - path: "apps/frontend/src/components/projects/ColumnConfigPanel.tsx"
      provides: "Column config with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "project views"
      to: "project detail page"
      via: "view switching"
      pattern: "ProjectBoardView|ProjectDashboardView|ProjectWorkloadView"
---

<objective>
Complete project components migration with remaining 10 files (~20 occurrences).

Purpose: Complete the projects/ directory. These include views (board, dashboard, workload), column configuration, and table components.

Output: All 10 remaining project components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

NOTE: Some project components use inline styles for DYNAMIC colors (user-configurable column colors, workload bars). These should NOT be converted - they are intentional.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate column configuration components (3 files)</name>
  <files>
apps/frontend/src/components/projects/ColumnCenterDialog.tsx
apps/frontend/src/components/projects/ColumnConfigPanel.tsx
apps/frontend/src/components/projects/AddTaskRow.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For ColumnCenterDialog.tsx:

- Dialog content: Handled by Dialog component
- Column type list: `hover:bg-gray-50` -> `hover:bg-muted`
- Column type icons: `text-gray-400` -> `text-muted-foreground`
- Column type labels: `text-gray-900` -> `text-foreground`
- Column type descriptions: `text-gray-500` -> `text-muted-foreground`
- Search input: `bg-white` -> `bg-background`
- Borders: `border-gray-*` -> `border-border`

For ColumnConfigPanel.tsx:

- Panel container: `bg-white` -> `bg-card`
- Panel header: `bg-gray-50` -> `bg-muted`
- Field labels: `text-gray-500` -> `text-muted-foreground`
- Input backgrounds: `bg-white` -> `bg-background`
- Helper text: `text-gray-500` -> `text-muted-foreground`
- Color pickers: Keep inline styles for color previews (intentional)
- Borders: `border-gray-*` -> `border-border`

For AddTaskRow.tsx:

- Row container: `bg-gray-50` -> `bg-muted`
- Add button area: `hover:bg-gray-100` -> `hover:bg-accent`
- Placeholder text: `text-gray-400` -> `text-muted-foreground`
- Input: `bg-white` -> `bg-background`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
for f in ColumnCenterDialog ColumnConfigPanel AddTaskRow; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/projects/$f.tsx" || echo "CLEAN"
done
```

All 3 should show "CLEAN".
</verify>
<done>ColumnCenterDialog.tsx, ColumnConfigPanel.tsx, and AddTaskRow.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate project views (4 files)</name>
  <files>
apps/frontend/src/components/projects/ProjectBoardView.tsx
apps/frontend/src/components/projects/ProjectDashboardView.tsx
apps/frontend/src/components/projects/ProjectWorkloadView.tsx
apps/frontend/src/components/projects/ProjectTaskTable.tsx
  </files>
  <action>
Read all 4 files and migrate hardcoded colors:

For ProjectBoardView.tsx:

- Board container: `bg-gray-100` -> `bg-muted`
- Column headers: `bg-white` -> `bg-card`
- Column containers: `bg-gray-50` -> `bg-muted`
- Card containers: `bg-white` -> `bg-card`
- Card hover: `hover:bg-gray-50` -> `hover:bg-muted`
- Drop zone indicators: May use colored borders - add dark: variants
- NOTE: Keep inline styles for dynamic column colors (intentional)
- Borders: `border-gray-*` -> `border-border`

For ProjectDashboardView.tsx:

- Dashboard container: `bg-white` -> `bg-card`
- KPI cards: `bg-gray-50` -> `bg-muted`
- Card labels: `text-gray-500` -> `text-muted-foreground`
- Card values: `text-gray-900` -> `text-foreground`
- Chart containers: `bg-white` -> `bg-card`
- Borders: `border-gray-*` -> `border-border`

For ProjectWorkloadView.tsx:

- View container: `bg-white` -> `bg-card`
- Team member rows: `hover:bg-gray-50` -> `hover:bg-muted`
- Names: `text-gray-900` -> `text-foreground`
- Capacity labels: `text-gray-500` -> `text-muted-foreground`
- NOTE: Keep inline styles for workload bars (dynamic colors)
- Borders: `border-gray-*` -> `border-border`

For ProjectTaskTable.tsx:

- Table container: `bg-white` -> `bg-card`
- Table header: `bg-gray-50` -> `bg-muted`
- Header text: `text-gray-700` -> `text-foreground`
- Row hover: `hover:bg-gray-50` -> `hover:bg-muted`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
for f in ProjectBoardView ProjectDashboardView ProjectWorkloadView ProjectTaskTable; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/projects/$f.tsx" || echo "CLEAN"
done
```

All 4 should show "CLEAN".
</verify>
<done>All 4 project view components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining project components (3 files)</name>
  <files>
apps/frontend/src/components/projects/MentionInput.tsx
apps/frontend/src/components/projects/MilestoneTimeline.tsx
apps/frontend/src/components/projects/ProjectGroupHeader.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For MentionInput.tsx:

- Input container: `bg-white` -> `bg-background`
- Mention dropdown: `bg-white` -> `bg-popover`
- Dropdown items: `hover:bg-gray-100` -> `hover:bg-accent`
- User names: `text-gray-900` -> `text-foreground`
- User details: `text-gray-500` -> `text-muted-foreground`
- Selected mention highlight: May use colored background - add dark: variant
- Borders: `border-gray-*` -> `border-border`

For MilestoneTimeline.tsx:

- Timeline container: `bg-white` -> `bg-card`
- Timeline line: `bg-gray-200` -> `bg-border`
- Milestone markers: Colored markers need dark: variants
- Milestone labels: `text-gray-900` -> `text-foreground`
- Dates: `text-gray-500` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`

For ProjectGroupHeader.tsx:

- Group header: `bg-gray-50` -> `bg-muted`
- Group name: `text-gray-900` -> `text-foreground`
- Task count: `text-gray-500` -> `text-muted-foreground`
- Collapse toggle: `text-gray-400` -> `text-muted-foreground`
- NOTE: Keep inline styles for group colors (dynamic)
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
for f in MentionInput MilestoneTimeline ProjectGroupHeader; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/projects/$f.tsx" || echo "CLEAN"
done
```

All 3 should show "CLEAN".
</verify>
<done>MentionInput.tsx, MilestoneTimeline.tsx, and ProjectGroupHeader.tsx contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check entire projects directory is clean:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/projects/
```

Should return 0 matches.

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- All 10 remaining project components migrated
- projects/ directory completely clean of hardcoded gray/white colors
- Dynamic inline styles preserved (column colors, workload bars)
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-08-SUMMARY.md`
</output>
