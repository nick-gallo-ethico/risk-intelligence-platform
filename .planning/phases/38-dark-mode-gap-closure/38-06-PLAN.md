---
phase: 38-dark-mode-gap-closure
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/frontend/src/components/cases/add-note-modal.tsx
  - apps/frontend/src/components/cases/attach-document-modal.tsx
  - apps/frontend/src/components/cases/create-task-modal.tsx
  - apps/frontend/src/components/cases/email-log-modal.tsx
  - apps/frontend/src/components/cases/log-interview-modal.tsx
  - apps/frontend/src/components/cases/status-change-modal.tsx
  - apps/frontend/src/components/cases/remediation-tab.tsx
  - apps/frontend/src/components/cases/remediation-step-card.tsx
  - apps/frontend/src/components/cases/ai-chat-panel.tsx
autonomous: true

must_haves:
  truths:
    - "Case modal dialogs render correctly in dark mode"
    - "Remediation tab and step cards have proper contrast"
    - "AI chat panel is readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/cases/status-change-modal.tsx"
      provides: "Status modal with semantic tokens"
      contains: "text-muted-foreground"
    - path: "apps/frontend/src/components/cases/remediation-tab.tsx"
      provides: "Remediation tab with semantic tokens"
      contains: "bg-card"
  key_links:
    - from: "case modals"
      to: "case detail page"
      via: "modal triggers"
      pattern: "add-note-modal|status-change-modal|attach-document-modal"
---

<objective>
Complete case components migration with modals (6 files), remediation components (2 files), and AI chat panel (1 file).

Purpose: Complete the cases/ directory migration. These modals are triggered from the case detail page and need dark mode support.

Output: All remaining case components (9 files) use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate case modals (6 files)</name>
  <files>
apps/frontend/src/components/cases/add-note-modal.tsx
apps/frontend/src/components/cases/attach-document-modal.tsx
apps/frontend/src/components/cases/create-task-modal.tsx
apps/frontend/src/components/cases/email-log-modal.tsx
apps/frontend/src/components/cases/log-interview-modal.tsx
apps/frontend/src/components/cases/status-change-modal.tsx
  </files>
  <action>
Read all 6 files and migrate hardcoded colors:

For add-note-modal.tsx:

- Modal content: Dialog handles background
- Text area: May have `bg-white` or `bg-gray-*` styling
- Labels: `text-gray-*` -> semantic tokens
- Helper text: `text-gray-500` -> `text-muted-foreground`

For attach-document-modal.tsx:

- Drop zone: `bg-gray-50` -> `bg-muted`, `border-gray-*` -> `border-border`
- Drop zone active state: May use colored border - add dark: variant
- File list: `bg-white` -> `bg-card`
- File items: `hover:bg-gray-50` -> `hover:bg-muted`

For create-task-modal.tsx:

- Form labels: `text-gray-*` -> semantic tokens
- Input backgrounds: `bg-white` -> `bg-background`
- Helper text: `text-gray-500` -> `text-muted-foreground`

For email-log-modal.tsx:

- Email content display: `bg-gray-50` -> `bg-muted`
- Headers: `text-gray-*` -> semantic tokens
- Email body: `text-gray-700` -> `text-foreground`

For log-interview-modal.tsx:

- Form styling: Apply semantic tokens to all form elements
- Date/time labels: `text-gray-500` -> `text-muted-foreground`

For status-change-modal.tsx:

- Status options: May use colored backgrounds
- Option hover: `hover:bg-gray-*` -> `hover:bg-muted`
- Status labels: `text-gray-*` -> semantic tokens
  </action>
  <verify>
  Run:

```bash
for f in add-note-modal attach-document-modal create-task-modal email-log-modal log-interview-modal status-change-modal; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/cases/$f.tsx" || echo "CLEAN"
done
```

All 6 should show "CLEAN".
</verify>
<done>All 6 case modal components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remediation-tab.tsx and remediation-step-card.tsx</name>
  <files>
apps/frontend/src/components/cases/remediation-tab.tsx
apps/frontend/src/components/cases/remediation-step-card.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For remediation-tab.tsx:

- Tab container: `bg-white` -> `bg-card`
- Section headers: `bg-gray-50` -> `bg-muted`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- Progress overview: May use colored backgrounds - add dark: variants
- Borders: `border-gray-*` -> `border-border`

For remediation-step-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Step number/label: `text-gray-*` -> semantic tokens
- Step status indicator: Colored indicators need dark: variants
- Completion checkbox area: `hover:bg-gray-*` -> `hover:bg-muted`
- Assignee: `text-gray-500` -> `text-muted-foreground`
- Due date: `text-gray-500` -> `text-muted-foreground`
- Overdue indicator: Ensure red is visible in dark mode
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/remediation-tab.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/remediation-step-card.tsx
```

Both should return 0 matches.
</verify>
<done>remediation-tab.tsx and remediation-step-card.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate ai-chat-panel.tsx</name>
  <files>apps/frontend/src/components/cases/ai-chat-panel.tsx</files>
  <action>
Read the file and migrate hardcoded colors:

For ai-chat-panel.tsx:
This is the AI assistant panel that slides in from the side.

- Panel container: `bg-white` -> `bg-card`
- Chat header: `bg-gray-50` -> `bg-muted`
- Message bubbles:
  - User messages: May use colored background - add dark: variant
  - AI messages: `bg-gray-100` -> `bg-muted`
- Message text: `text-gray-700` -> `text-foreground`
- Timestamps: `text-gray-500` -> `text-muted-foreground`
- Input area: `bg-white` -> `bg-background`
- Input placeholder: Ensure visible in dark mode
- Send button: Ensure consistent styling
- Loading indicators: `text-gray-400` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/ai-chat-panel.tsx
```

Should return 0 matches.
</verify>
<done>ai-chat-panel.tsx contains zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all 9 migrated files:

```bash
for f in add-note-modal attach-document-modal create-task-modal email-log-modal log-interview-modal status-change-modal remediation-tab remediation-step-card ai-chat-panel; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/cases/$f.tsx" || echo "CLEAN"
done
```

All 9 should show "CLEAN".

2. Check remaining cases/ files (test files will be handled in plan 38-12):

```bash
grep -rlE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/ | grep -v __tests__
```

Should only show files covered in plans 38-04, 38-05, 38-06 (all migrated).

3. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- 9 remaining case components migrated (modals, remediation, AI)
- All case modals render correctly in dark mode
- Remediation components have proper status indicator contrast
- AI chat panel readable in dark mode
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-06-SUMMARY.md`
</output>
