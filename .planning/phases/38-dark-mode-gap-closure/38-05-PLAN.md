---
phase: 38-dark-mode-gap-closure
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/frontend/src/components/cases/investigation-card.tsx
  - apps/frontend/src/components/cases/case-investigations-panel.tsx
  - apps/frontend/src/components/cases/connected-people-card.tsx
  - apps/frontend/src/components/cases/linked-rius-card.tsx
  - apps/frontend/src/components/cases/linked-riu-list.tsx
  - apps/frontend/src/components/cases/related-cases-card.tsx
  - apps/frontend/src/components/cases/related-policies-card.tsx
  - apps/frontend/src/components/cases/remediation-status-card.tsx
autonomous: true

must_haves:
  truths:
    - "Case sidebar cards render correctly in dark mode"
    - "Investigation and RIU cards have proper contrast"
    - "Connected entities cards are readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/cases/investigation-card.tsx"
      provides: "Investigation card with semantic tokens"
      contains: "bg-card"
    - path: "apps/frontend/src/components/cases/connected-people-card.tsx"
      provides: "People card with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "sidebar cards"
      to: "case detail page"
      via: "right sidebar imports"
      pattern: "connected-people-card|linked-rius-card|related-cases-card"
---

<objective>
Migrate case sidebar and association card components (8 files) to semantic tokens.

Purpose: These components appear in the case detail right sidebar showing related entities. They're highly visible and need proper dark mode support.

Output: 8 case sidebar/card components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate investigation-card.tsx and case-investigations-panel.tsx</name>
  <files>
apps/frontend/src/components/cases/investigation-card.tsx
apps/frontend/src/components/cases/case-investigations-panel.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For investigation-card.tsx:
This displays an individual investigation as a card in the case view.

- Card container: `bg-white` -> `bg-card`
- Card header: `bg-gray-50` -> `bg-muted`
- Investigation title: `text-gray-900` -> `text-foreground`
- Status badge: Add dark: variants or use getStatusColor from theme-colors.ts
- Progress indicator: May use colored backgrounds - add dark: variants
- Metadata (dates, assignee): `text-gray-500` -> `text-muted-foreground`
- Hover state: `hover:bg-gray-50` -> `hover:bg-muted`
- Borders: `border-gray-*` -> `border-border`

For case-investigations-panel.tsx:
This is the panel listing all investigations for a case.

- Panel container: `bg-white` -> `bg-card`
- Section header: `text-gray-900` -> `text-foreground`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- Add button: Ensure consistent with design system
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/investigation-card.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/case-investigations-panel.tsx
```

Both should return 0 matches.
</verify>
<done>investigation-card.tsx and case-investigations-panel.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate connected entity cards (4 files)</name>
  <files>
apps/frontend/src/components/cases/connected-people-card.tsx
apps/frontend/src/components/cases/linked-rius-card.tsx
apps/frontend/src/components/cases/related-cases-card.tsx
apps/frontend/src/components/cases/related-policies-card.tsx
  </files>
  <action>
Read all 4 files and migrate hardcoded colors:

For connected-people-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Person list items: `hover:bg-gray-50` -> `hover:bg-muted`
- Person name: `text-gray-900` -> `text-foreground`
- Person role/email: `text-gray-500` -> `text-muted-foreground`
- Role badge: Add dark: variants if using colored badges
- Count badge: Ensure visible in dark mode
- Borders: `border-gray-*` -> `border-border`

For linked-rius-card.tsx:

- Card container: `bg-white` -> `bg-card`
- RIU list items: `hover:bg-gray-50` -> `hover:bg-muted`
- RIU ID/type: `text-gray-*` -> semantic tokens
- Type badge: Add dark: variants
- Borders: `border-gray-*` -> `border-border`

For related-cases-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Case list items: `hover:bg-gray-50` -> `hover:bg-muted`
- Case ID: `text-gray-*` -> semantic tokens
- Status badge: Use getStatusColor or add dark: variants
- Borders: `border-gray-*` -> `border-border`

For related-policies-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Policy list items: `hover:bg-gray-50` -> `hover:bg-muted`
- Policy name: `text-gray-900` -> `text-foreground`
- Policy metadata: `text-gray-500` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
for f in connected-people-card linked-rius-card related-cases-card related-policies-card; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/cases/$f.tsx" || echo "CLEAN"
done
```

All 4 should show "CLEAN".
</verify>
<done>All 4 connected entity card components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate linked-riu-list.tsx and remediation-status-card.tsx</name>
  <files>
apps/frontend/src/components/cases/linked-riu-list.tsx
apps/frontend/src/components/cases/remediation-status-card.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For linked-riu-list.tsx:

- List container: `bg-white` -> `bg-card`
- RIU items: `hover:bg-gray-50` -> `hover:bg-muted`
- RIU type/source: `text-gray-*` -> semantic tokens
- Date/time: `text-gray-500` -> `text-muted-foreground`
- Source channel badge: Add dark: variants
- Borders: `border-gray-*` -> `border-border`

For remediation-status-card.tsx:

- Card container: `bg-white` -> `bg-card`
- Status indicator: Colored indicators need dark: variants
- Progress bar track: `bg-gray-200` -> `bg-secondary`
- Step count: `text-gray-500` -> `text-muted-foreground`
- Completion percentage: `text-gray-900` -> `text-foreground`
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/linked-riu-list.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/remediation-status-card.tsx
```

Both should return 0 matches.
</verify>
<done>linked-riu-list.tsx and remediation-status-card.tsx contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all 8 migrated files:

```bash
for f in investigation-card case-investigations-panel connected-people-card linked-rius-card related-cases-card related-policies-card linked-riu-list remediation-status-card; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/cases/$f.tsx" || echo "CLEAN"
done
```

All 8 should show "CLEAN".

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- 8 case sidebar/card components migrated
- All connected entity cards render correctly in dark mode
- Investigation cards have proper status badge contrast
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-05-SUMMARY.md`
</output>
