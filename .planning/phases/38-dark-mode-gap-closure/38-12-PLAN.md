---
phase: 38-dark-mode-gap-closure
plan: 12
type: execute
wave: 5
depends_on:
  - 38-04
  - 38-05
  - 38-06
files_modified:
  - apps/frontend/src/components/cases/__tests__/activity-entry.test.tsx
  - apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx
  - apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx
  - apps/frontend/src/components/cases/__tests__/case-header.test.tsx
  - apps/frontend/src/components/cases/__tests__/investigation-card.test.tsx
  - apps/frontend/src/components/cases/__tests__/property-section.test.tsx
autonomous: true

must_haves:
  truths:
    - "Test files compile without TypeScript errors"
    - "Test assertions match updated component class names"
    - "All case component tests pass after class name updates"
  artifacts:
    - path: "apps/frontend/src/components/cases/__tests__/activity-entry.test.tsx"
      provides: "Updated test with semantic token class assertions"
      contains: "text-muted-foreground"
  key_links:
    - from: "test files"
      to: "component files"
      via: "test assertions"
      pattern: "expect.*className|toHaveClass"
---

<objective>
Update test files to match new semantic token class names after component migrations.

Purpose: Test files may contain class name assertions that need updating to match the migrated components.

Output: All 6 test files updated with correct semantic token class names.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Test File Update Strategy

Test files may contain:

1. Class name assertions in `expect()` calls
2. Snapshot tests with class strings
3. Test data with hardcoded class names
4. Comments referencing old class names

For each file:

1. Search for hardcoded gray/white class references
2. Update assertions to use semantic token class names
3. Update snapshots if needed (regenerate with --updateSnapshot)
4. Ensure tests still validate the intended behavior
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Update activity-entry.test.tsx and activity-filters.test.tsx</name>
  <files>
apps/frontend/src/components/cases/__tests__/activity-entry.test.tsx
apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx
  </files>
  <action>
Read both test files and update hardcoded class references:

For activity-entry.test.tsx:

- Find assertions like `expect(element).toHaveClass('text-gray-500')`
- Update to semantic: `expect(element).toHaveClass('text-muted-foreground')`
- Check for snapshot tests - may need regeneration
- Update any test data that includes class names

For activity-filters.test.tsx:

- Same pattern: find hardcoded class assertions
- Update `bg-gray-*` -> `bg-muted` or `bg-accent`
- Update `text-gray-*` -> `text-foreground` or `text-muted-foreground`
- Update `hover:bg-gray-*` -> `hover:bg-muted` or `hover:bg-accent`

If the test is asserting on specific class names and those classes are now semantic tokens, update the assertions to match.

If the test is checking element existence or behavior (not specific classes), no changes needed.
</action>
<verify>
Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/activity-entry.test.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/activity-filters.test.tsx
```

Both should return 0 matches (or only show false positives in comments).

Run tests:

```bash
cd apps/frontend && npm test -- --testPathPattern="activity-entry|activity-filters" --passWithNoTests
```

  </verify>
  <done>activity-entry.test.tsx and activity-filters.test.tsx updated with semantic token assertions</done>
</task>

<task type="auto">
  <name>Task 2: Update case-activity-timeline.test.tsx and case-header.test.tsx</name>
  <files>
apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx
apps/frontend/src/components/cases/__tests__/case-header.test.tsx
  </files>
  <action>
Read both test files and update hardcoded class references:

For case-activity-timeline.test.tsx:

- Update class assertions to semantic tokens
- Timeline items: `bg-gray-50` -> `bg-muted`
- Timestamps: `text-gray-500` -> `text-muted-foreground`
- Event descriptions: `text-gray-700` -> `text-foreground`

For case-header.test.tsx:

- Update class assertions to semantic tokens
- Header backgrounds: `bg-white` -> `bg-card`
- Title text: `text-gray-900` -> `text-foreground`
- Metadata: `text-gray-500` -> `text-muted-foreground`
- Status badges: May need dark: variant assertions if testing badge styling
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/case-activity-timeline.test.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/case-header.test.tsx
```

Both should return 0 matches.

Run tests:

```bash
cd apps/frontend && npm test -- --testPathPattern="case-activity-timeline|case-header" --passWithNoTests
```

  </verify>
  <done>case-activity-timeline.test.tsx and case-header.test.tsx updated with semantic token assertions</done>
</task>

<task type="auto">
  <name>Task 3: Update investigation-card.test.tsx and property-section.test.tsx</name>
  <files>
apps/frontend/src/components/cases/__tests__/investigation-card.test.tsx
apps/frontend/src/components/cases/__tests__/property-section.test.tsx
  </files>
  <action>
Read both test files and update hardcoded class references:

For investigation-card.test.tsx:

- Update card container assertions: `bg-white` -> `bg-card`
- Update header: `bg-gray-50` -> `bg-muted`
- Update text assertions: `text-gray-*` -> semantic tokens
- Update status badge assertions: may need dark: variant patterns

For property-section.test.tsx:

- Update section header: `bg-gray-50` -> `bg-muted`
- Update labels: `text-gray-500` -> `text-muted-foreground`
- Update values: `text-gray-900` -> `text-foreground`
- Update hover states: `hover:bg-gray-50` -> `hover:bg-muted`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/investigation-card.test.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/property-section.test.tsx
```

Both should return 0 matches.

Run tests:

```bash
cd apps/frontend && npm test -- --testPathPattern="investigation-card|property-section" --passWithNoTests
```

  </verify>
  <done>investigation-card.test.tsx and property-section.test.tsx updated with semantic token assertions</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all test files are clean:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/cases/__tests__/
```

Should return 0 matches.

2. Run all case tests:

```bash
cd apps/frontend && npm test -- --testPathPattern="cases/__tests__" --passWithNoTests
```

All tests should pass.

3. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- All 6 test files updated with semantic token class assertions
- All case component tests pass
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-12-SUMMARY.md`
</output>
