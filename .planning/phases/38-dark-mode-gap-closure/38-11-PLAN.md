---
phase: 38-dark-mode-gap-closure
plan: 11
type: execute
wave: 5
depends_on: []
files_modified:
  - apps/frontend/src/app/(authenticated)/projects/[id]/page.tsx
  - apps/frontend/src/app/(authenticated)/search/page.tsx
  - apps/frontend/src/app/(authenticated)/reports/page.tsx
  - apps/frontend/src/app/(authenticated)/settings/properties/page.tsx
  - apps/frontend/src/app/(authenticated)/settings/roles/page.tsx
  - apps/frontend/src/app/(authenticated)/cases/[id]/loading.tsx
autonomous: true

must_haves:
  truths:
    - "All page files render correctly in dark mode"
    - "Project detail, search, reports, and settings pages have proper contrast"
    - "Case loading skeleton is visible in dark mode"
  artifacts:
    - path: "apps/frontend/src/app/(authenticated)/projects/[id]/page.tsx"
      provides: "Project detail page with semantic tokens"
      contains: "bg-background"
    - path: "apps/frontend/src/app/(authenticated)/search/page.tsx"
      provides: "Search page with semantic tokens"
      contains: "text-muted-foreground"
  key_links:
    - from: "page files"
      to: "route rendering"
      via: "Next.js routing"
      pattern: "page.tsx"
---

<objective>
Migrate all page files with hardcoded colors (6 files, ~11 occurrences) to semantic tokens.

Purpose: Pages are entry points for routes. These 6 pages have hardcoded colors that need migration.

Output: All 6 page files use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate project detail page (6 occurrences)</name>
  <files>apps/frontend/src/app/(authenticated)/projects/[id]/page.tsx</files>
  <action>
Read the file and migrate hardcoded colors:

For projects/[id]/page.tsx (6 occurrences - highest count among pages):
This is the main project detail page.

- Page container: `bg-white` -> `bg-background`
- Header area: `bg-gray-50` -> `bg-muted`
- Tab panels: `bg-white` -> `bg-card`
- Section dividers: `border-gray-*` -> `border-border`
- Empty states: `text-gray-500` -> `text-muted-foreground`
- Status indicators: Add dark: variants if using colored backgrounds
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/projects/\[id\]/page.tsx
```

Should return 0 matches.
</verify>
<done>projects/[id]/page.tsx contains zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate search and reports pages</name>
  <files>
apps/frontend/src/app/(authenticated)/search/page.tsx
apps/frontend/src/app/(authenticated)/reports/page.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For search/page.tsx:

- Search container: `bg-white` -> `bg-background`
- Search input area: May have `bg-gray-*` styling
- Results container: `bg-white` -> `bg-card`
- Result items: `hover:bg-gray-50` -> `hover:bg-muted`
- Result text: `text-gray-*` -> semantic tokens
- Entity type badges: Use entityTypeConfig dark: variants from 22-13
- Empty state: `text-gray-500` -> `text-muted-foreground`

For reports/page.tsx:

- Page container: `bg-white` -> `bg-background`
- Report list: `bg-white` -> `bg-card`
- Report items: `hover:bg-gray-50` -> `hover:bg-muted`
- Report names: `text-gray-900` -> `text-foreground`
- Descriptions: `text-gray-500` -> `text-muted-foreground`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/search/page.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/reports/page.tsx
```

Both should return 0 matches.
</verify>
<done>search/page.tsx and reports/page.tsx contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate settings pages and case loading</name>
  <files>
apps/frontend/src/app/(authenticated)/settings/properties/page.tsx
apps/frontend/src/app/(authenticated)/settings/roles/page.tsx
apps/frontend/src/app/(authenticated)/cases/[id]/loading.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For settings/properties/page.tsx:

- Page container: `bg-white` -> `bg-background`
- Property list: `bg-white` -> `bg-card`
- Property items: `hover:bg-gray-50` -> `hover:bg-muted`
- Property names: `text-gray-900` -> `text-foreground`
- Descriptions: `text-gray-500` -> `text-muted-foreground`

For settings/roles/page.tsx:

- Page container: `bg-white` -> `bg-background`
- Role list: `bg-white` -> `bg-card`
- Role items: `hover:bg-gray-50` -> `hover:bg-muted`
- Role names: `text-gray-900` -> `text-foreground`
- Descriptions: `text-gray-500` -> `text-muted-foreground`
- Permission badges: Add dark: variants if needed

For cases/[id]/loading.tsx:
This is a loading skeleton for the case detail page.

- Skeleton container: `bg-white` -> `bg-card`
- Skeleton pulse elements: `bg-gray-200` -> `bg-muted`
- If using custom skeleton styling, ensure it animates correctly in dark mode
- Borders: `border-gray-*` -> `border-border`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/settings/properties/page.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/settings/roles/page.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/\(authenticated\)/cases/\[id\]/loading.tsx
```

All 3 should return 0 matches.
</verify>
<done>All 3 settings/loading pages contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check all app page files are clean:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/app/
```

Should return 0 matches.

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- All 6 page files migrated to semantic tokens
- Zero hardcoded gray/white colors in app/ directory
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-11-SUMMARY.md`
</output>
