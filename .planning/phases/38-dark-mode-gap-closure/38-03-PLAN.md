---
phase: 38-dark-mode-gap-closure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/record-detail/MobileSidebarDrawer.tsx
  - apps/frontend/src/components/record-detail/PipelineStageBar.tsx
  - apps/frontend/src/components/record-detail/RecordHeader.tsx
  - apps/frontend/src/components/record-detail/StatusHistoryTimeline.tsx
  - apps/frontend/src/components/layout/top-nav.tsx
  - apps/frontend/src/components/shared/quick-action-grid.tsx
autonomous: true

must_haves:
  truths:
    - "Record-detail shared components render correctly in dark mode"
    - "Top navigation and shared components render correctly in dark mode"
    - "Pipeline stage bar and status timeline have proper contrast in both themes"
  artifacts:
    - path: "apps/frontend/src/components/record-detail/PipelineStageBar.tsx"
      provides: "Pipeline bar with semantic tokens"
      contains: "bg-muted"
    - path: "apps/frontend/src/components/layout/top-nav.tsx"
      provides: "Top nav with semantic tokens"
      contains: "text-muted-foreground"
    - path: "apps/frontend/src/components/shared/quick-action-grid.tsx"
      provides: "Quick action grid with semantic tokens"
      contains: "bg-card"
  key_links:
    - from: "record-detail components"
      to: "case/investigation pages"
      via: "shared component imports"
      pattern: "PipelineStageBar|RecordHeader|StatusHistoryTimeline"
---

<objective>
Complete record-detail migration (4 files) and migrate high-visibility layout/shared components (2 files).

Purpose: Complete the record-detail shared components and fix top-nav (visible on every page) and quick-action-grid (used in sidebars).

Output: All record-detail components and 2 high-visibility shared components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Complete record-detail components (4 files)</name>
  <files>
apps/frontend/src/components/record-detail/MobileSidebarDrawer.tsx
apps/frontend/src/components/record-detail/PipelineStageBar.tsx
apps/frontend/src/components/record-detail/RecordHeader.tsx
apps/frontend/src/components/record-detail/StatusHistoryTimeline.tsx
  </files>
  <action>
Read all 4 files and migrate hardcoded colors:

For MobileSidebarDrawer.tsx:

- Drawer background: `bg-white` -> `bg-background`
- Overlay: Keep as-is if using rgba, or use `bg-background/80`
- Close button: `text-gray-*` -> `text-muted-foreground`

For PipelineStageBar.tsx:

- Stage track: `bg-gray-200` -> `bg-secondary` (progress track pattern)
- Active stage: Keep the colored variant but add dark: if needed
- Inactive stages: `text-gray-500` -> `text-muted-foreground`
- Stage connectors: `bg-gray-300` -> `bg-border`
- Completed stages: Ensure checkmark visible in both modes

For RecordHeader.tsx:

- Header background: `bg-white` -> `bg-background` or `bg-card`
- Title: `text-gray-900` -> `text-foreground`
- Subtitle/metadata: `text-gray-500` -> `text-muted-foreground`
- Action buttons area: Consistent with card styling
- Breadcrumbs: `text-gray-500` -> `text-muted-foreground`

For StatusHistoryTimeline.tsx:

- Timeline line: `bg-gray-200` -> `border-border` or `bg-muted`
- Timeline dots: Colored dots should have dark: variants
- Entry cards: `bg-white` -> `bg-card`
- Timestamps: `text-gray-500` -> `text-muted-foreground`
- Status text: `text-gray-900` -> `text-foreground`
  </action>
  <verify>
  Run:

```bash
for f in MobileSidebarDrawer PipelineStageBar RecordHeader StatusHistoryTimeline; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/record-detail/$f.tsx" || echo "CLEAN"
done
```

All 4 should show "CLEAN".
</verify>
<done>All 4 record-detail components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate top-nav.tsx (8 occurrences)</name>
  <files>apps/frontend/src/components/layout/top-nav.tsx</files>
  <action>
Read top-nav.tsx and migrate hardcoded colors.

Note: The top nav stays dark in both modes (HubSpot pattern). However, certain elements within may use hardcoded gray classes that need fixing.

Apply these transformations:

- Any `bg-gray-*` for dropdown menus -> `bg-popover`
- Menu item text: `text-gray-*` -> `text-popover-foreground` or semantic tokens
- Hover states in dropdowns: `hover:bg-gray-100` -> `hover:bg-accent`
- Kbd styling: `bg-gray-100` -> `bg-muted`, `border-gray-300` -> `border-border`
- User avatar fallback: If using gray, keep it (acceptable for avatar)
- Notification badge: Colored badges need dark: variants

The nav bar itself likely uses dark styling (bg-slate-900 etc.) - those can stay as they form the intentional dark design.
</action>
<verify>
Run:

```bash
grep -E "bg-white|bg-gray-[0-9]+|text-gray-|border-gray-|hover:bg-gray-[0-9]+" apps/frontend/src/components/layout/top-nav.tsx
```

Should return 0 matches for the hardcoded patterns. Slate colors for the nav bar itself are acceptable.
</verify>
<done>top-nav.tsx contains zero hardcoded gray/white color classes in interactive elements</done>
</task>

<task type="auto">
  <name>Task 3: Migrate quick-action-grid.tsx (7 occurrences)</name>
  <files>apps/frontend/src/components/shared/quick-action-grid.tsx</files>
  <action>
Read quick-action-grid.tsx and migrate hardcoded colors.

QuickActionGrid is used in case/investigation left sidebars for action buttons.

Apply these transformations:

- Grid container: `bg-white` -> `bg-card`
- Action button backgrounds: `bg-gray-50` -> `bg-muted`
- Action button hover: `hover:bg-gray-100` -> `hover:bg-accent`
- Icon colors: `text-gray-600` -> `text-muted-foreground`
- Action labels: `text-gray-700` -> `text-foreground`
- Borders: `border-gray-*` -> `border-border`
- Focus states: Ensure ring colors use semantic tokens
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/shared/quick-action-grid.tsx
```

Should return 0 matches.
</verify>
<done>quick-action-grid.tsx contains zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run grep on record-detail directory (should be completely clean now):

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/record-detail/
```

Should return 0 matches.

2. Check layout directory:

```bash
grep -rE "bg-white|bg-gray-[0-9]+|text-gray-|border-gray-|hover:bg-gray-[0-9]+" apps/frontend/src/components/layout/
```

Should return 0 matches (ignoring intentional slate colors for nav).

3. Check shared directory:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/shared/
```

Should return 0 matches.

4. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- All 7 record-detail component files now have zero hardcoded colors
- top-nav.tsx migrated (high visibility - every page)
- quick-action-grid.tsx migrated (used in case/investigation sidebars)
- record-detail/, layout/, shared/ directories all clean
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-03-SUMMARY.md`
</output>
