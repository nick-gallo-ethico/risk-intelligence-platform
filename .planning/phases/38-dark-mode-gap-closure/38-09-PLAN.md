---
phase: 38-dark-mode-gap-closure
plan: 09
type: execute
wave: 4
depends_on: []
files_modified:
  - apps/frontend/src/components/ai/ai-category-suggest.tsx
  - apps/frontend/src/components/ai/ai-risk-score.tsx
  - apps/frontend/src/components/ai/ai-summary-button.tsx
  - apps/frontend/src/components/workflows/builder/stage-properties.tsx
  - apps/frontend/src/components/workflows/workflow-list-table.tsx
  - apps/frontend/src/components/workflows/workflow-progress-indicator.tsx
  - apps/frontend/src/components/exports/FlatExportBuilder.tsx
  - apps/frontend/src/components/exports/TaggedFieldConfig.tsx
  - apps/frontend/src/components/policies/policy-attestations-panel.tsx
  - apps/frontend/src/components/policies/policy-cases-panel.tsx
  - apps/frontend/src/components/policies/policy-detail-header.tsx
  - apps/frontend/src/components/policies/policy-list.tsx
autonomous: true

must_haves:
  truths:
    - "AI components render correctly in dark mode"
    - "Workflow builder components have proper contrast"
    - "Export and policy components are readable in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/ai/ai-summary-button.tsx"
      provides: "AI summary button with semantic tokens"
      contains: "text-muted-foreground"
    - path: "apps/frontend/src/components/workflows/workflow-list-table.tsx"
      provides: "Workflow table with semantic tokens"
      contains: "bg-muted"
  key_links:
    - from: "AI components"
      to: "case detail page"
      via: "AI panel"
      pattern: "ai-category-suggest|ai-risk-score|ai-summary-button"
---

<objective>
Migrate AI, workflows, exports, and policies components (12 files, ~35 occurrences) to semantic tokens.

Purpose: These are secondary but important feature areas that need dark mode support.

Output: 12 components across 4 domains use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AI components (3 files)</name>
  <files>
apps/frontend/src/components/ai/ai-category-suggest.tsx
apps/frontend/src/components/ai/ai-risk-score.tsx
apps/frontend/src/components/ai/ai-summary-button.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For ai-category-suggest.tsx:

- Container: `bg-white` -> `bg-card`
- Category suggestion items: `hover:bg-gray-50` -> `hover:bg-muted`
- Category names: `text-gray-900` -> `text-foreground`
- Confidence indicator: May use colored backgrounds - add dark: variants
- Borders: `border-gray-*` -> `border-border`

For ai-risk-score.tsx:

- Score container: `bg-white` -> `bg-card`
- Score label: `text-gray-500` -> `text-muted-foreground`
- Score value: `text-gray-900` -> `text-foreground`
- Risk level colors (HIGH/MEDIUM/LOW): Add dark: variants
- Progress bar track: `bg-gray-200` -> `bg-secondary`
- Borders: `border-gray-*` -> `border-border`

For ai-summary-button.tsx:

- Button states: If using gray backgrounds, convert to semantic tokens
- Loading state: `text-gray-400` -> `text-muted-foreground`
- Tooltip text: Ensure visible in dark mode
  </action>
  <verify>
  Run:

```bash
for f in ai-category-suggest ai-risk-score ai-summary-button; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/ai/$f.tsx" || echo "CLEAN"
done
```

All 3 should show "CLEAN".
</verify>
<done>All 3 AI components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate workflow components (3 files)</name>
  <files>
apps/frontend/src/components/workflows/builder/stage-properties.tsx
apps/frontend/src/components/workflows/workflow-list-table.tsx
apps/frontend/src/components/workflows/workflow-progress-indicator.tsx
  </files>
  <action>
Read all 3 files and migrate hardcoded colors:

For stage-properties.tsx:

- Properties panel: `bg-white` -> `bg-card`
- Section headers: `bg-gray-50` -> `bg-muted`
- Property labels: `text-gray-500` -> `text-muted-foreground`
- Property values: `text-gray-900` -> `text-foreground`
- Input backgrounds: `bg-white` -> `bg-background`
- Borders: `border-gray-*` -> `border-border`

For workflow-list-table.tsx:

- Table container: `bg-white` -> `bg-card`
- Table header: `bg-gray-50` -> `bg-muted`
- Header text: `text-gray-700` -> `text-foreground`
- Row hover: `hover:bg-gray-50` -> `hover:bg-muted`
- Status badges: Add dark: variants
- Borders: `border-gray-*` -> `border-border`

For workflow-progress-indicator.tsx:

- Progress track: `bg-gray-200` -> `bg-secondary`
- Stage dots: Ensure colored dots visible in dark mode
- Stage labels: `text-gray-*` -> semantic tokens
- Current stage highlight: Add dark: variant if needed
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/workflows/builder/stage-properties.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/workflows/workflow-list-table.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/workflows/workflow-progress-indicator.tsx
```

All 3 should return 0 matches.
</verify>
<done>All 3 workflow components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate exports and policies components (6 files)</name>
  <files>
apps/frontend/src/components/exports/FlatExportBuilder.tsx
apps/frontend/src/components/exports/TaggedFieldConfig.tsx
apps/frontend/src/components/policies/policy-attestations-panel.tsx
apps/frontend/src/components/policies/policy-cases-panel.tsx
apps/frontend/src/components/policies/policy-detail-header.tsx
apps/frontend/src/components/policies/policy-list.tsx
  </files>
  <action>
Read all 6 files and migrate hardcoded colors:

For FlatExportBuilder.tsx:

- Builder container: `bg-white` -> `bg-card`
- Field list: `hover:bg-gray-50` -> `hover:bg-muted`
- Field labels: `text-gray-*` -> semantic tokens
- Tag colors: Use dark: variants from STATE.md (22-12 decision)
- Borders: `border-gray-*` -> `border-border`

For TaggedFieldConfig.tsx:

- Config container: `bg-white` -> `bg-card`
- Tag badges: Use dark: variants (6 field tag colors from 22-12)
- Field names: `text-gray-900` -> `text-foreground`
- Descriptions: `text-gray-500` -> `text-muted-foreground`
- Borders: `border-gray-*` -> `border-border`

For policy-attestations-panel.tsx:

- Panel container: `bg-white` -> `bg-card`
- Attestation items: `hover:bg-gray-50` -> `hover:bg-muted`
- Status badges: Add dark: variants
- Date text: `text-gray-500` -> `text-muted-foreground`

For policy-cases-panel.tsx:

- Panel container: `bg-white` -> `bg-card`
- Case items: `hover:bg-gray-50` -> `hover:bg-muted`
- Case IDs: `text-gray-*` -> semantic tokens

For policy-detail-header.tsx:

- Header container: `bg-white` -> `bg-card`
- Policy title: `text-gray-900` -> `text-foreground`
- Metadata: `text-gray-500` -> `text-muted-foreground`
- Status badge: Add dark: variant

For policy-list.tsx:

- List container: `bg-white` -> `bg-card`
- List items: `hover:bg-gray-50` -> `hover:bg-muted`
- Policy names: `text-gray-900` -> `text-foreground`
- Metadata: `text-gray-500` -> `text-muted-foreground`
  </action>
  <verify>
  Run:

```bash
for f in FlatExportBuilder TaggedFieldConfig; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/exports/$f.tsx" || echo "CLEAN"
done

for f in policy-attestations-panel policy-cases-panel policy-detail-header policy-list; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/policies/$f.tsx" || echo "CLEAN"
done
```

All 6 should show "CLEAN".
</verify>
<done>All 6 exports and policies components contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Check ai/, workflows/, exports/, policies/ directories are clean:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/ai/
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/workflows/
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/exports/
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/policies/
```

All should return 0 matches.

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- 12 components across AI, workflows, exports, policies migrated
- All 4 directories completely clean of hardcoded gray/white colors
- TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-09-SUMMARY.md`
</output>
