---
phase: 38-dark-mode-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/investigations/checklist-item.tsx
  - apps/frontend/src/components/investigations/investigation-evidence-card.tsx
  - apps/frontend/src/components/investigations/investigation-findings.tsx
  - apps/frontend/src/components/investigations/note-card.tsx
  - apps/frontend/src/components/investigations/parent-case-card.tsx
  - apps/frontend/src/components/record-detail/CollapsiblePropertyCard.tsx
  - apps/frontend/src/components/record-detail/DataHighlightsCard.tsx
  - apps/frontend/src/components/record-detail/EditableSummary.tsx
autonomous: true

must_haves:
  truths:
    - "Remaining investigation components render correctly in dark mode"
    - "Record-detail shared components render correctly in dark mode"
    - "Property cards and highlights cards have proper contrast in both themes"
  artifacts:
    - path: "apps/frontend/src/components/investigations/note-card.tsx"
      provides: "Note card with semantic tokens"
      contains: "text-muted-foreground"
    - path: "apps/frontend/src/components/record-detail/DataHighlightsCard.tsx"
      provides: "Highlights card with semantic tokens"
      contains: "bg-card"
  key_links:
    - from: "record-detail components"
      to: "case and investigation pages"
      via: "shared component imports"
      pattern: "CollapsiblePropertyCard|DataHighlightsCard|EditableSummary"
---

<objective>
Migrate remaining investigation components (5 files) and high-impact record-detail components (3 files) to semantic tokens.

Purpose: Complete the investigation domain and start the record-detail shared components which are used across case and investigation pages.

Output: All investigation components and 3 record-detail components use semantic tokens.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
- OR use theme-colors.ts helpers: getStatusColor(), getSeverityColor(), priorityColors
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining 5 investigation components</name>
  <files>
apps/frontend/src/components/investigations/checklist-item.tsx
apps/frontend/src/components/investigations/investigation-evidence-card.tsx
apps/frontend/src/components/investigations/investigation-findings.tsx
apps/frontend/src/components/investigations/note-card.tsx
apps/frontend/src/components/investigations/parent-case-card.tsx
  </files>
  <action>
Read all 5 files and migrate hardcoded colors:

For checklist-item.tsx:

- Item backgrounds: `bg-white` -> `bg-card`, `bg-gray-50` -> `bg-muted`
- Checkbox labels: `text-gray-*` -> semantic tokens
- Completed items: May use line-through styling (keep)

For investigation-evidence-card.tsx:

- Card: `bg-white` -> `bg-card`
- Labels/values: `text-gray-*` -> semantic tokens
- Evidence status badges: Add dark: variants if using colored badges

For investigation-findings.tsx:

- Section backgrounds: `bg-gray-50` -> `bg-muted`
- Finding labels: `text-gray-500` -> `text-muted-foreground`
- Finding text: `text-gray-900` -> `text-foreground`

For note-card.tsx:

- Card: `bg-white` -> `bg-card`
- Timestamp: `text-gray-500` -> `text-muted-foreground`
- Note content: `text-gray-700` -> `text-foreground`
- Author name: Apply appropriate semantic token

For parent-case-card.tsx:

- Card styling: `bg-white` -> `bg-card`
- Case ID link: Keep link color, but ensure dark: variant
- Metadata: `text-gray-*` -> semantic tokens
  </action>
  <verify>
  Run:

```bash
for f in checklist-item investigation-evidence-card investigation-findings note-card parent-case-card; do
  echo "=== $f ===" && grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" "apps/frontend/src/components/investigations/$f.tsx" || echo "CLEAN"
done
```

All 5 should show "CLEAN".
</verify>
<done>All 5 remaining investigation components contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate DataHighlightsCard.tsx and EditableSummary.tsx (high-occurrence)</name>
  <files>
apps/frontend/src/components/record-detail/DataHighlightsCard.tsx
apps/frontend/src/components/record-detail/EditableSummary.tsx
  </files>
  <action>
Read both files and migrate hardcoded colors:

For DataHighlightsCard.tsx (10 occurrences):

- Card container: `bg-white` -> `bg-card`
- Highlight labels: `text-gray-500` -> `text-muted-foreground`
- Highlight values: `text-gray-900` -> `text-foreground`
- Metric containers: `bg-gray-50` -> `bg-muted`
- Borders: `border-gray-*` -> `border-border`
- Icon colors: If using gray icons, convert to `text-muted-foreground`

For EditableSummary.tsx:

- Container: `bg-white` -> `bg-card`
- Label: `text-gray-500` -> `text-muted-foreground`
- Summary text: `text-gray-700` -> `text-foreground`
- Placeholder: `text-gray-400` -> `text-muted-foreground` (or keep as placeholder-specific)
- Edit state background: `bg-gray-50` -> `bg-muted`
  </action>
  <verify>
  Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/record-detail/DataHighlightsCard.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/record-detail/EditableSummary.tsx
```

Both should return 0 matches.
</verify>
<done>DataHighlightsCard.tsx and EditableSummary.tsx contain zero hardcoded color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate CollapsiblePropertyCard.tsx</name>
  <files>apps/frontend/src/components/record-detail/CollapsiblePropertyCard.tsx</files>
  <action>
Read the file and migrate hardcoded colors:

CollapsiblePropertyCard.tsx is used for property cards in the left sidebar of case/investigation pages.

Apply these transformations:

- Card container: `bg-white` -> `bg-card`
- Card header: `bg-gray-50` -> `bg-muted`, `hover:bg-gray-100` -> `hover:bg-accent`
- Card title: `text-gray-700` or `text-gray-900` -> `text-foreground`
- Property labels: `text-gray-500` -> `text-muted-foreground`
- Property values: `text-gray-900` -> `text-foreground`
- Collapsed state: Ensure consistent with expanded
- Borders: `border-gray-*` -> `border-border`
- Chevron icon: `text-gray-400` -> `text-muted-foreground`

This component is heavily used, so ensure all variants (collapsed/expanded, with/without edit button) are covered.
</action>
<verify>
Run:

```bash
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/record-detail/CollapsiblePropertyCard.tsx
```

Should return 0 matches.
</verify>
<done>CollapsiblePropertyCard.tsx contains zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run grep on investigations directory (should be completely clean now):

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/
```

Should return 0 matches.

2. Check record-detail progress:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/record-detail/
```

Should only show remaining files (MobileSidebarDrawer, PipelineStageBar, RecordHeader, StatusHistoryTimeline - handled in 38-03).

3. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

</verification>

<success_criteria>

- All 11 investigation component files now have zero hardcoded colors
- 3 record-detail components migrated
- TypeScript compiles without errors
- investigations/ directory is completely clean of hardcoded gray/white colors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-02-SUMMARY.md`
</output>
