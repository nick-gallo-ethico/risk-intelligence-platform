---
phase: 38-dark-mode-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/investigations/investigation-properties-panel.tsx
  - apps/frontend/src/components/investigations/investigation-files-tab.tsx
  - apps/frontend/src/components/investigations/investigation-interviews-tab.tsx
  - apps/frontend/src/components/investigations/investigation-info-summary.tsx
  - apps/frontend/src/components/investigations/investigation-header.tsx
  - apps/frontend/src/components/investigations/investigation-activity-timeline.tsx
autonomous: true

must_haves:
  truths:
    - "Investigation components render with proper contrast in dark mode"
    - "Property labels and values are readable in both light and dark modes"
    - "File/interview cards have appropriate backgrounds in dark mode"
  artifacts:
    - path: "apps/frontend/src/components/investigations/investigation-properties-panel.tsx"
      provides: "Investigation property panel with semantic tokens"
      contains: "text-muted-foreground"
    - path: "apps/frontend/src/components/investigations/investigation-files-tab.tsx"
      provides: "Files tab with semantic tokens"
      contains: "bg-muted"
    - path: "apps/frontend/src/components/investigations/investigation-interviews-tab.tsx"
      provides: "Interviews tab with semantic tokens"
      contains: "text-foreground"
  key_links:
    - from: "investigation-properties-panel.tsx"
      to: "CSS variables"
      via: "semantic token classes"
      pattern: "bg-background|bg-card|bg-muted|text-foreground|text-muted-foreground|border-border"
---

<objective>
Migrate hardcoded Tailwind color classes to semantic tokens in the 6 highest-impact investigation components (96 total occurrences).

Purpose: These components are central to case workflow and have the most hardcoded colors (investigation-properties-panel.tsx alone has 39 occurrences).

Output: All 6 investigation components use semantic tokens for backgrounds, text, and borders.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-dark-mode-gap-closure/38-RESEARCH.md

## Mapping Reference (USE THIS)

| Hardcoded               | Semantic Token                           |
| ----------------------- | ---------------------------------------- |
| bg-white                | bg-background or bg-card                 |
| bg-gray-50              | bg-muted                                 |
| bg-gray-100             | bg-muted or bg-accent                    |
| text-gray-400           | text-muted-foreground                    |
| text-gray-500           | text-muted-foreground                    |
| text-gray-600           | text-muted-foreground or text-foreground |
| text-gray-700           | text-foreground                          |
| text-gray-900           | text-foreground                          |
| border-gray-100/200/300 | border-border                            |
| hover:bg-gray-50        | hover:bg-muted                           |
| hover:bg-gray-100       | hover:bg-accent                          |

For semantic colors (blue, red, green, yellow, orange badges):

- Add dark: variants: `bg-blue-100 text-blue-800 dark:bg-blue-900/30 dark:text-blue-300`
- OR use theme-colors.ts helpers: getStatusColor(), getSeverityColor(), priorityColors
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate investigation-properties-panel.tsx (39 occurrences)</name>
  <files>apps/frontend/src/components/investigations/investigation-properties-panel.tsx</files>
  <action>
Read the file and identify all hardcoded gray/white Tailwind classes.

Apply these transformations:

- `text-gray-500` -> `text-muted-foreground` (property labels)
- `text-gray-900` -> `text-foreground` (property values)
- `text-gray-700` -> `text-foreground` (headings)
- `text-gray-600` -> `text-muted-foreground` (secondary text)
- `bg-white` -> `bg-card` (card backgrounds)
- `bg-gray-50` -> `bg-muted` (section backgrounds)
- `bg-gray-100` -> `bg-muted` or `bg-accent` (hover states)
- `border-gray-*` -> `border-border`
- `hover:bg-gray-50` -> `hover:bg-muted`
- `hover:bg-gray-100` -> `hover:bg-accent`

For any semantic color badges (status, severity), add dark: variants or use theme-colors.ts helpers.

Do NOT modify inline styles that use dynamic colors (like user-configurable colors).
</action>
<verify>
Run: `grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-properties-panel.tsx`

Should return 0 matches. Run TypeScript check: `cd apps/frontend && npx tsc --noEmit --skipLibCheck`
</verify>
<done>investigation-properties-panel.tsx contains zero hardcoded gray/white color classes; all use semantic tokens</done>
</task>

<task type="auto">
  <name>Task 2: Migrate investigation-files-tab.tsx and investigation-interviews-tab.tsx (28 occurrences)</name>
  <files>
apps/frontend/src/components/investigations/investigation-files-tab.tsx
apps/frontend/src/components/investigations/investigation-interviews-tab.tsx
  </files>
  <action>
Read both files and identify all hardcoded gray/white Tailwind classes.

For investigation-files-tab.tsx (15 occurrences):

- File list items: `hover:bg-gray-50` -> `hover:bg-muted`
- Empty state: `text-gray-500` -> `text-muted-foreground`
- File cards: `bg-white` -> `bg-card`, borders -> `border-border`
- File metadata: `text-gray-600` -> `text-muted-foreground`

For investigation-interviews-tab.tsx (13 occurrences):

- Interview cards: `bg-white` -> `bg-card`
- Labels: `text-gray-500` -> `text-muted-foreground`
- Values: `text-gray-900` -> `text-foreground`
- Hover states: `hover:bg-gray-*` -> `hover:bg-muted` or `hover:bg-accent`
- Borders: `border-gray-*` -> `border-border`

For any colored badges, add dark: variants.
</action>
<verify>
Run:

```
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-files-tab.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-interviews-tab.tsx
```

Both should return 0 matches.
</verify>
<done>Both files contain zero hardcoded gray/white color classes</done>
</task>

<task type="auto">
  <name>Task 3: Migrate remaining investigation components (3 files, ~15 occurrences)</name>
  <files>
apps/frontend/src/components/investigations/investigation-info-summary.tsx
apps/frontend/src/components/investigations/investigation-header.tsx
apps/frontend/src/components/investigations/investigation-activity-timeline.tsx
  </files>
  <action>
Read all three files and apply semantic token migrations.

For investigation-info-summary.tsx:

- Card background: `bg-white` -> `bg-card`
- Labels: `text-gray-500` -> `text-muted-foreground`
- Values: `text-gray-900` -> `text-foreground`

For investigation-header.tsx:

- Breadcrumb text: `text-gray-*` -> semantic tokens
- Badge backgrounds: Add dark: variants if needed

For investigation-activity-timeline.tsx:

- Timeline items: `bg-gray-50` -> `bg-muted`
- Timestamps: `text-gray-500` -> `text-muted-foreground`
- Event descriptions: `text-gray-700` -> `text-foreground`
- Hover states: Convert to `hover:bg-muted`
  </action>
  <verify>
  Run:

```
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-info-summary.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-header.tsx
grep -E "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/investigation-activity-timeline.tsx
```

All should return 0 matches.
</verify>
<done>All three investigation components contain zero hardcoded gray/white color classes</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run grep on entire investigations directory:

```bash
grep -rE "bg-white|bg-gray-|text-gray-|border-gray-|hover:bg-gray-" apps/frontend/src/components/investigations/
```

Should only show files not in this plan (checklist-item.tsx, investigation-evidence-card.tsx, investigation-findings.tsx, note-card.tsx, parent-case-card.tsx - handled in 38-02).

2. TypeScript compilation:

```bash
cd apps/frontend && npx tsc --noEmit --skipLibCheck
```

3. Verify no runtime errors by starting dev server (optional spot check).
   </verification>

<success_criteria>

- 6 investigation component files migrated to semantic tokens
- Zero hardcoded gray/white color classes in migrated files
- TypeScript compiles without errors
- ~60 occurrences addressed (out of 96 total in investigations/)
  </success_criteria>

<output>
After completion, create `.planning/phases/38-dark-mode-gap-closure/38-01-SUMMARY.md`
</output>
