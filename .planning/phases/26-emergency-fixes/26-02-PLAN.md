---
phase: 26-emergency-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/.env
autonomous: false

user_setup:
  - service: anthropic
    why: "API key was exposed in code review audit; must be rotated for security"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys -> Create new key"
    dashboard_config:
      - task: "Rotate API key"
        location: "https://console.anthropic.com/settings/keys"
        steps:
          - "1. Log in to Anthropic Console"
          - "2. Navigate to Settings -> API Keys"
          - "3. Create a new API key (copy it immediately)"
          - "4. Delete the old key (sk-ant-api03-* prefix)"
          - "5. Confirm the old key is invalidated"

must_haves:
  truths:
    - "Anthropic API key has been rotated in the dashboard"
    - "Old API key (sk-ant-api03-* prefix) is invalidated and will not work"
    - "New API key is stored in .env (file remains gitignored)"
  artifacts:
    - path: "apps/backend/.env"
      provides: "New Anthropic API key (rotated)"
      contains: "ANTHROPIC_API_KEY="
  key_links:
    - from: "apps/backend/.env"
      to: "Anthropic API"
      via: "ANTHROPIC_API_KEY environment variable"
      pattern: "ANTHROPIC_API_KEY=sk-ant-"
---

<objective>
Rotate the exposed Anthropic API key and update the local environment.

Purpose: Address EMER-02 from the Code Review audit. The current API key was flagged as exposed. Even though `.env` is gitignored, the key must be rotated as a security precaution. This is a mandatory human action.

Output:

- Old Anthropic API key invalidated in dashboard
- New API key generated and stored in `.env`
- AI features continue to work with new key
  </objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-emergency-fixes/26-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Rotate Anthropic API key in dashboard</name>
  <action>
You must rotate the API key in the Anthropic Console. Claude cannot do this for you.

**Steps:**

1. Go to https://console.anthropic.com/settings/keys
2. Log in with your Anthropic account
3. Click "Create Key" to generate a new API key
4. **IMPORTANT:** Copy the new key immediately (it won't be shown again)
5. Find the old key (starts with `sk-ant-api03-`)
6. Click the delete/revoke button for the old key
7. Confirm the old key is no longer listed

**Why this is necessary:**
The Code Review audit flagged that an API key was exposed. Even though `.env` is gitignored, best practice is to rotate any key that may have been seen. Revoking the old key ensures it cannot be used even if compromised.
</action>
<what-to-provide>
Provide the new API key to Claude so it can update the `.env` file.

Format: `ANTHROPIC_API_KEY=sk-ant-xxxxx`

(The key starts with `sk-ant-` followed by your specific key characters)
</what-to-provide>
<resume-signal>
Paste the new API key, or type "key rotated" and provide the key value separately.
</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update .env with new API key</name>
  <files>apps/backend/.env</files>
  <action>
After receiving the new API key from the user:

1. Read the current `.env` file
2. Find the line starting with `ANTHROPIC_API_KEY=`
3. Replace the value with the new API key provided by the user
4. Write the updated `.env` file

**Format validation:**

- The new key should start with `sk-ant-`
- If the provided key doesn't match this format, warn the user

**Do NOT commit the .env file** (it should be gitignored).
</action>
<verify>

1. Verify `.env` contains the new API key
2. Verify the old key (`sk-ant-api03-*`) is no longer in the file
3. Verify `.env` is in `.gitignore` (should already be)
   </verify>
   <done>

- `.env` updated with new Anthropic API key
- Old key no longer present in file
- File remains gitignored
  </done>
  </task>

<task type="auto">
  <name>Task 3: Verify AI connectivity with new key</name>
  <files>none</files>
  <action>
Test that the AI service works with the new API key:

1. Start the backend server (if not running): `cd apps/backend && npm run start:dev`
2. Make a simple API call to verify connectivity

Alternatively, if the backend has a health check that verifies AI:

- Check `/health` endpoint for AI provider status

If the backend cannot start or AI calls fail:

- Check that the new key is correctly formatted
- Verify the key is active in Anthropic Console
  </action>
  <verify>

1. Backend starts without API key errors
2. No "invalid API key" errors in server logs
3. If AI health check exists, it returns healthy status
   </verify>
   <done>

- Backend starts successfully
- No authentication errors with Anthropic API
- AI features operational with new key
  </done>
  </task>

</tasks>

<verification>
After all tasks complete:

1. Confirm old key is revoked in Anthropic Console (cannot be used)
2. Confirm new key is in `.env` (starts with `sk-ant-`)
3. Confirm backend starts without API key errors
4. Confirm `.env` is gitignored (run `git status` - should not show .env)

```bash
# Verify .env is gitignored
git check-ignore apps/backend/.env && echo ".env is properly gitignored"

# Verify key format (without exposing the actual key)
grep -q "^ANTHROPIC_API_KEY=sk-ant-" apps/backend/.env && echo "Key format valid"
```

Success criteria (from ROADMAP.md):

1. [x] Anthropic API key has been rotated in the dashboard
2. [x] Old key is invalidated
       </verification>

<success_criteria>

1. Old Anthropic API key deleted from console (cannot be used)
2. New API key stored in `.env` with correct format
3. Backend starts without authentication errors
4. `.env` file remains gitignored
   </success_criteria>

<output>
After completion, create `.planning/phases/26-emergency-fixes/26-02-SUMMARY.md`
</output>
