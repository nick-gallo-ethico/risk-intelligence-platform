---
phase: 36-test-coverage-expansion
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/test/e2e/hris-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/workflow-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/search-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/projects-tenant-isolation.e2e-spec.ts
autonomous: true

must_haves:
  truths:
    - "HRIS tenant isolation E2E tests exist"
    - "Workflow tenant isolation E2E tests exist"
    - "Search tenant isolation E2E tests exist"
    - "Projects tenant isolation E2E tests exist"
    - "All 12+ modules now have tenant isolation E2E tests"
  artifacts:
    - path: "apps/backend/test/e2e/hris-tenant-isolation.e2e-spec.ts"
      provides: "HRIS tenant isolation E2E tests"
      min_lines: 60
    - path: "apps/backend/test/e2e/workflow-tenant-isolation.e2e-spec.ts"
      provides: "Workflow tenant isolation E2E tests"
      min_lines: 80
    - path: "apps/backend/test/e2e/search-tenant-isolation.e2e-spec.ts"
      provides: "Search tenant isolation E2E tests"
      min_lines: 60
    - path: "apps/backend/test/e2e/projects-tenant-isolation.e2e-spec.ts"
      provides: "Projects tenant isolation E2E tests"
      min_lines: 80
  key_links:
    - from: "*-tenant-isolation.e2e-spec.ts"
      to: "test/helpers/test-setup.ts"
      via: "import createTestContext"
      pattern: "import.*createTestContext.*from"
---

<objective>
Add tenant isolation E2E tests for final 4 modules: HRIS, workflow, search, projects.

Purpose: Complete TEST-04 requirement - ensure 12+ modules have tenant isolation verification
Output: 4 E2E test files completing the tenant isolation test suite
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/test/helpers/test-setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HRIS and workflow tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/hris-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/workflow-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests using createTestContext() pattern.

    hris-tenant-isolation.e2e-spec.ts:
    - GET /hris/employees: Org A sees only Org A HRIS employees
    - GET /hris/sync-status: Org B cannot see Org A sync status
    - POST /hris/sync: Org B cannot trigger sync for Org A
    - GET /persons: Verify Person records tenant-isolated
    - Test HRIS integration config isolation

    workflow-tenant-isolation.e2e-spec.ts:
    - GET /workflows: Org A sees only Org A workflow templates
    - GET /workflows/:id: Org B gets 404 for Org A workflow
    - PUT /workflows/:id: Org B cannot update Org A workflow
    - GET /workflow-instances: Org B cannot see Org A instances
    - POST /workflows/:id/instances: Cannot create instance from Org A template
    - PUT /workflow-instances/:id/transition: Cannot transition Org A instance
    - Test workflow assignment rules are tenant-scoped

  </action>
  <verify>npm run test:e2e -- --testPathPattern="hris-tenant|workflow-tenant" passes</verify>
  <done>2 E2E test files with 6+ tests each</done>
</task>

<task type="auto">
  <name>Task 2: Create search and projects tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/search-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/projects-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests following same pattern.

    search-tenant-isolation.e2e-spec.ts:
    - GET /search?q=test: Results only from caller's tenant
    - Verify Elasticsearch index names include tenant prefix
    - Test saved searches are tenant-isolated
    - Search for entity that exists in Org A - Org B gets no results
    - Test search suggestions respect tenant boundaries

    projects-tenant-isolation.e2e-spec.ts:
    - GET /projects: Org A sees only Org A projects
    - GET /projects/:id: Org B gets 404 for Org A project
    - PUT /projects/:id: Org B cannot update Org A project
    - GET /projects/:id/tasks: Org B cannot see Org A project tasks
    - POST /projects/:id/tasks: Org B cannot add tasks to Org A project
    - Test project task assignments respect tenant
    - Test project comments/updates isolation

    Ensure proper cleanup in afterAll.

  </action>
  <verify>npm run test:e2e -- --testPathPattern="search-tenant|projects-tenant" passes</verify>
  <done>2 E2E test files with 6+ tests each</done>
</task>

</tasks>

<verification>
Run all tenant isolation E2E tests to verify complete coverage:
```bash
npm run test:e2e -- --testPathPattern="tenant-isolation"
```

Count modules with tenant isolation tests:

- Existing: activity, investigations, investigation-notes, cases, smoke/tenant
- Plan 04: auth, campaigns, disclosures, policies
- Plan 05: reporting, AI, forms, notifications
- Plan 06: HRIS, workflow, search, projects

Total: 16+ modules with tenant isolation E2E tests (exceeds 12+ requirement)
</verification>

<success_criteria>

1. 4 new E2E test files created
2. Each file has 6+ test cases
3. All cross-tenant access returns 404
4. All tests pass: `npm run test:e2e -- --testPathPattern="tenant-isolation"`
5. 12+ modules have tenant isolation E2E coverage (TEST-04 complete)
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-06-SUMMARY.md`
</output>
