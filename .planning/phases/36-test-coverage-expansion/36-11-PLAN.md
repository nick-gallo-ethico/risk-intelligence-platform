---
phase: 36-test-coverage-expansion
plan: 11
type: execute
wave: 2
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - apps/frontend/src/components/auth/__tests__/login-form.test.tsx
  - apps/frontend/src/components/auth/__tests__/mfa-setup.test.tsx
  - apps/frontend/src/app/(authenticated)/settings/__tests__/profile-settings.test.tsx
  - apps/frontend/src/app/(authenticated)/settings/__tests__/notification-settings.test.tsx
autonomous: true

must_haves:
  truths:
    - "login-form.test.tsx exists with auth flow tests"
    - "mfa-setup.test.tsx exists with MFA flow tests"
    - "profile-settings.test.tsx exists"
    - "notification-settings.test.tsx exists"
    - "All frontend tests pass with vitest"
  artifacts:
    - path: "apps/frontend/src/components/auth/__tests__/login-form.test.tsx"
      provides: "Login form component tests"
      min_lines: 80
    - path: "apps/frontend/src/components/auth/__tests__/mfa-setup.test.tsx"
      provides: "MFA setup component tests"
      min_lines: 60
    - path: "apps/frontend/src/app/(authenticated)/settings/__tests__/profile-settings.test.tsx"
      provides: "Profile settings page tests"
      min_lines: 60
    - path: "apps/frontend/src/app/(authenticated)/settings/__tests__/notification-settings.test.tsx"
      provides: "Notification settings page tests"
      min_lines: 60
  key_links:
    - from: "*.test.tsx"
      to: "components/**/*.tsx"
      via: "import statement"
      pattern: "import.*from.*components"
---

<objective>
Expand frontend test coverage for auth pages and settings.

Purpose: Start TEST-09 requirement - improve frontend coverage from 13% toward 40%
Output: 4 test files covering login, MFA, and settings components
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth component tests (login-form, mfa-setup)</name>
  <files>
    apps/frontend/src/components/auth/__tests__/login-form.test.tsx
    apps/frontend/src/components/auth/__tests__/mfa-setup.test.tsx
  </files>
  <action>
    Create React Testing Library tests for auth components with MSW mocking.

    login-form.test.tsx:
    1. should render email and password fields
    2. should show validation error for invalid email format
    3. should show validation error for empty password
    4. should call login API on form submit
    5. should show error message on invalid credentials (401)
    6. should redirect to dashboard on successful login
    7. should show MFA challenge when MFA required
    8. should disable submit button while loading
    9. should show SSO buttons when SSO enabled
    10. should handle forgot password link

    mfa-setup.test.tsx:
    1. should render QR code for TOTP setup
    2. should show manual entry code option
    3. should validate 6-digit TOTP code format
    4. should show error on invalid code
    5. should show recovery codes after successful setup
    6. should allow downloading recovery codes
    7. should confirm MFA setup completion

    Use pattern from research:
    ```typescript
    const server = setupServer(
      http.post('/api/v1/auth/login', () => {
        return HttpResponse.json({ accessToken: 'mock-token' });
      })
    );
    beforeAll(() => server.listen());
    afterEach(() => server.resetHandlers());
    afterAll(() => server.close());
    ```

  </action>
  <verify>npm run test:frontend -- --testPathPattern="(login-form|mfa-setup).test" passes</verify>
  <done>2 auth test files with 10+ tests total</done>
</task>

<task type="auto">
  <name>Task 2: Create settings page tests (profile, notifications)</name>
  <files>
    apps/frontend/src/app/(authenticated)/settings/__tests__/profile-settings.test.tsx
    apps/frontend/src/app/(authenticated)/settings/__tests__/notification-settings.test.tsx
  </files>
  <action>
    Create tests for settings pages with auth context wrapper.

    profile-settings.test.tsx:
    1. should render current user profile data
    2. should allow editing name fields
    3. should validate email format on change
    4. should show password change form
    5. should validate password strength
    6. should save profile changes on submit
    7. should show success toast after save
    8. should handle API errors gracefully

    notification-settings.test.tsx:
    1. should render notification preference toggles
    2. should show preferences organized by category
    3. should toggle email notification preference
    4. should toggle in-app notification preference
    5. should save preferences on change (auto-save)
    6. should show loading state while saving
    7. should handle disabled notifications (org-enforced)
    8. should render digest frequency selector

    Wrap components with AuthProvider and QueryClientProvider:
    ```typescript
    const renderWithProviders = (ui: React.ReactElement) => {
      return render(
        <QueryClientProvider client={queryClient}>
          <AuthProvider>
            {ui}
          </AuthProvider>
        </QueryClientProvider>
      );
    };
    ```

  </action>
  <verify>npm run test:frontend -- --testPathPattern="(profile-settings|notification-settings).test" passes</verify>
  <done>2 settings test files with 8+ tests each</done>
</task>

</tasks>

<verification>
Run all frontend tests:
```bash
cd apps/frontend && npm run test -- --coverage
```

Verify:

- All 4 test files exist in appropriate **tests** directories
- Tests use MSW for API mocking
- Tests use @testing-library/react and userEvent
- No console errors during test runs
  </verification>

<success_criteria>

1. 4 frontend test files created
2. Auth tests cover login flow and MFA setup
3. Settings tests cover profile and notification preferences
4. All tests pass with vitest
5. Frontend auth/settings coverage increased
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-11-SUMMARY.md`
</output>
