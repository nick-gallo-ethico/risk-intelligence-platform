---
phase: 36-test-coverage-expansion
plan: 04
type: execute
wave: 2
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - apps/backend/test/e2e/auth-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/campaigns-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/disclosures-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/policies-tenant-isolation.e2e-spec.ts
autonomous: true

must_haves:
  truths:
    - "Auth SSO tenant isolation E2E tests exist"
    - "Campaigns tenant isolation E2E tests exist"
    - "Disclosures tenant isolation E2E tests exist"
    - "Policies tenant isolation E2E tests exist"
    - "All E2E tests verify cross-tenant access returns 404 (not 403)"
  artifacts:
    - path: "apps/backend/test/e2e/auth-tenant-isolation.e2e-spec.ts"
      provides: "Auth/SSO tenant isolation E2E tests"
      min_lines: 100
    - path: "apps/backend/test/e2e/campaigns-tenant-isolation.e2e-spec.ts"
      provides: "Campaigns tenant isolation E2E tests"
      min_lines: 100
    - path: "apps/backend/test/e2e/disclosures-tenant-isolation.e2e-spec.ts"
      provides: "Disclosures tenant isolation E2E tests"
      min_lines: 100
    - path: "apps/backend/test/e2e/policies-tenant-isolation.e2e-spec.ts"
      provides: "Policies tenant isolation E2E tests"
      min_lines: 100
  key_links:
    - from: "*-tenant-isolation.e2e-spec.ts"
      to: "test/helpers/test-setup.ts"
      via: "import createTestContext"
      pattern: "import.*createTestContext.*from"
---

<objective>
Add tenant isolation E2E tests for 4 modules: auth/SSO, campaigns, disclosures, policies.

Purpose: Complete TEST-04 requirement (partial) - verify RLS prevents cross-tenant data access at HTTP level
Output: 4 E2E test files with comprehensive CRUD isolation verification
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/test/helpers/test-setup.ts
@apps/backend/test/tenant-isolation.e2e-spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth/SSO and campaigns tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/auth-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/campaigns-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests using createTestContext() from test-setup.ts.

    auth-tenant-isolation.e2e-spec.ts:
    - Test SSO config isolation: Org B cannot view/modify Org A's SSO settings
    - Test domain verification isolation: Org B cannot access Org A's verified domains
    - Test user listing isolation: GET /users only returns users in caller's org
    - Test session isolation: Org B cannot invalidate Org A user sessions

    campaigns-tenant-isolation.e2e-spec.ts (reference research example):
    - GET /campaigns: Org A sees only Org A campaigns
    - GET /campaigns/:id: Org B gets 404 for Org A campaign
    - PUT /campaigns/:id: Org B cannot update Org A campaign (404)
    - DELETE /campaigns/:id: Org B cannot delete Org A campaign (404)
    - POST /campaigns/:id/assignments: Org B cannot add assignments to Org A campaign
    - Verify database unchanged after cross-tenant attempts

    CRITICAL: Expect 404 (not 403) for cross-tenant access - prevents enumeration attacks.

    Use pattern:
    ```typescript
    beforeAll(async () => {
      ctx = await createTestContext();
      // Create test data in Org A
    });
    afterAll(async () => {
      // Clean up test data
      await destroyTestContext(ctx);
    });
    ```

  </action>
  <verify>npm run test:e2e -- --testPathPattern="(auth-tenant|campaigns-tenant)-isolation" passes</verify>
  <done>2 E2E test files with 8+ tests each, all cross-tenant access returns 404</done>
</task>

<task type="auto">
  <name>Task 2: Create disclosures and policies tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/disclosures-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/policies-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests following same pattern as Task 1.

    disclosures-tenant-isolation.e2e-spec.ts:
    - GET /disclosures: Org A sees only Org A disclosures
    - GET /disclosures/:id: Org B gets 404 for Org A disclosure
    - PUT /disclosures/:id: Org B cannot update Org A disclosure
    - GET /conflicts: Org B cannot see Org A conflicts
    - POST /conflicts/:id/dismiss: Org B cannot dismiss Org A conflicts
    - Test disclosure form template isolation

    policies-tenant-isolation.e2e-spec.ts:
    - GET /policies: Org A sees only Org A policies
    - GET /policies/:id: Org B gets 404 for Org A policy
    - PUT /policies/:id: Org B cannot update Org A policy
    - POST /policies/:id/publish: Org B cannot publish Org A policy
    - GET /policies/:id/versions: Org B cannot see Org A version history
    - Test policy attestation isolation

    Ensure test data cleanup in afterAll to prevent test pollution.

  </action>
  <verify>npm run test:e2e -- --testPathPattern="disclosures-tenant|policies-tenant" passes</verify>
  <done>2 E2E test files with 8+ tests each, all cross-tenant access returns 404</done>
</task>

</tasks>

<verification>
Run all tenant isolation E2E tests:
```bash
npm run test:e2e -- --testPathPattern="tenant-isolation"
```

Verify:

- All 4 new E2E test files exist in test/e2e/
- Cross-tenant access attempts receive 404 responses
- Database state unchanged after cross-tenant attempts
- No test failures
  </verification>

<success_criteria>

1. 4 new E2E test files created in apps/backend/test/e2e/
2. Each file has 8+ test cases covering CRUD operations
3. All cross-tenant access returns 404 (not 403)
4. All tests pass: `npm run test:e2e -- --testPathPattern="tenant-isolation"`
5. Plan 04 adds 4 modules: auth, campaigns, disclosures, policies
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-04-SUMMARY.md`
</output>
