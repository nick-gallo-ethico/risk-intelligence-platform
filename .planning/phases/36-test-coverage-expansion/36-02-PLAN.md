---
phase: 36-test-coverage-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/auth/strategies/jwt.strategy.spec.ts
  - apps/backend/src/modules/auth/strategies/azure-ad.strategy.spec.ts
  - apps/backend/src/modules/auth/strategies/google.strategy.spec.ts
  - apps/backend/src/modules/auth/strategies/saml.strategy.spec.ts
autonomous: true

must_haves:
  truths:
    - "jwt.strategy.spec.ts exists with 90%+ coverage"
    - "azure-ad.strategy.spec.ts exists with 90%+ coverage"
    - "google.strategy.spec.ts exists with 90%+ coverage"
    - "saml.strategy.spec.ts exists with 90%+ coverage"
    - "All strategy tests pass with npm test"
  artifacts:
    - path: "apps/backend/src/modules/auth/strategies/jwt.strategy.spec.ts"
      provides: "JWT strategy validate() tests"
      min_lines: 100
    - path: "apps/backend/src/modules/auth/strategies/azure-ad.strategy.spec.ts"
      provides: "Azure AD strategy validate() tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/strategies/google.strategy.spec.ts"
      provides: "Google OAuth strategy validate() tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/strategies/saml.strategy.spec.ts"
      provides: "SAML strategy validate() tests"
      min_lines: 80
  key_links:
    - from: "*.strategy.spec.ts"
      to: "*.strategy.ts"
      via: "import statement"
      pattern: "import.*Strategy.*from"
---

<objective>
Write unit tests for all 4 auth strategies (jwt, azure-ad, google, saml) to achieve 90%+ coverage.

Purpose: Complete TEST-02 requirement - auth strategies are core authentication path requiring comprehensive validation testing
Output: 4 new test files covering strategy validate() methods and JIT user provisioning
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/auth/strategies/jwt.strategy.ts
@apps/backend/src/modules/auth/strategies/azure-ad.strategy.ts
@apps/backend/src/modules/auth/strategies/google.strategy.ts
@apps/backend/src/modules/auth/strategies/saml.strategy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jwt.strategy.spec.ts with token validation tests</name>
  <files>apps/backend/src/modules/auth/strategies/jwt.strategy.spec.ts</files>
  <action>
    Create unit tests for JwtStrategy.validate() method.

    Test cases required:
    1. should reject token with type='refresh' (only accept 'access')
    2. should reject token with missing sub claim
    3. should reject token when user not found in database
    4. should reject token when user.isActive is false
    5. should reject token when session not found (if sessionId claim exists)
    6. should reject token when session is expired
    7. should return user object when token is valid
    8. should include organizationId, role, sessionId in returned user
    9. should handle MFA fields (mfaVerified, mfaEnabled) if present

    Critical mocking:
    - Mock PrismaService with user.findUnique and session.findUnique
    - Mock withBypassRLS to execute callback: `withBypassRLS: jest.fn(cb => cb())`
    - Mock ConfigService.get() for JWT_SECRET
    - Mock JwtKeyService.getVerificationKey() if asymmetric keys used

    Reference research "Pattern 2: Unit Test for Passport Strategies".

  </action>
  <verify>npm test -- --testPathPattern="jwt.strategy.spec" passes with all tests green</verify>
  <done>jwt.strategy.spec.ts has 9+ test cases covering all validation branches, 90%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create SSO strategy tests (azure-ad, google, saml)</name>
  <files>
    apps/backend/src/modules/auth/strategies/azure-ad.strategy.spec.ts
    apps/backend/src/modules/auth/strategies/google.strategy.spec.ts
    apps/backend/src/modules/auth/strategies/saml.strategy.spec.ts
  </files>
  <action>
    Create unit tests for all 3 SSO strategies. They share similar patterns:
    - validate() receives provider profile
    - Calls SsoService.findOrCreateUser for JIT provisioning
    - Returns user object on success

    For EACH strategy, test:
    1. should extract email from provider profile
    2. should call ssoService.findOrCreateUser with correct params
    3. should return user from findOrCreateUser on success
    4. should throw when organization not found (domain not registered)
    5. should throw when ssoService.findOrCreateUser fails
    6. should handle missing profile fields gracefully

    Azure AD specific:
    - Handle oid, upn, preferred_username fields for email extraction
    - Handle tenant claim for multi-tenant Azure AD

    Google specific:
    - Handle emails[0].value vs email field
    - Handle verified_email field

    SAML specific:
    - Handle nameID, email attributes from SAML assertion
    - Handle different attribute name formats (urn:oid vs shortname)

    Reference research "Complete Strategy Test File (SAML)" for structure.

  </action>
  <verify>npm test -- --testPathPattern="(azure-ad|google|saml).strategy.spec" passes</verify>
  <done>3 SSO strategy spec files with 6+ tests each, 90%+ coverage</done>
</task>

</tasks>

<verification>
Run all strategy tests:
```bash
npm test -- --testPathPattern="strategy.spec" --coverage --collectCoverageFrom="**/strategies/*.ts"
```

Verify:

- All 4 strategy spec files exist
- Combined strategy coverage is 90%+
- No test failures
  </verification>

<success_criteria>

1. 4 new strategy spec files created
2. jwt.strategy.spec.ts has 9+ test cases
3. Each SSO strategy spec has 6+ test cases
4. All tests pass: `npm test -- --testPathPattern="strategy.spec"`
5. Auth strategy coverage reaches 90%+
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-02-SUMMARY.md`
</output>
