---
phase: 36-test-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/auth/guards/jwt-ws.guard.spec.ts
  - apps/backend/src/modules/auth/guards/mfa.guard.spec.ts
  - apps/backend/src/modules/auth/guards/throttle-behind-proxy.guard.spec.ts
autonomous: true

must_haves:
  truths:
    - "jwt-ws.guard.spec.ts exists with 90%+ coverage"
    - "mfa.guard.spec.ts exists with 90%+ coverage"
    - "throttle-behind-proxy.guard.spec.ts exists with 90%+ coverage"
    - "All guard tests pass with npm test"
  artifacts:
    - path: "apps/backend/src/modules/auth/guards/jwt-ws.guard.spec.ts"
      provides: "WebSocket JWT guard unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/guards/mfa.guard.spec.ts"
      provides: "MFA guard unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/guards/throttle-behind-proxy.guard.spec.ts"
      provides: "Throttle behind proxy guard unit tests"
      min_lines: 60
  key_links:
    - from: "*.guard.spec.ts"
      to: "*.guard.ts"
      via: "import statement"
      pattern: "import.*Guard.*from"
---

<objective>
Write unit tests for the 3 remaining auth guards (jwt-ws, mfa, throttle-behind-proxy) to achieve 90%+ coverage on all 6 auth guards.

Purpose: Complete TEST-01 requirement - auth guards are critical security surface requiring comprehensive unit tests
Output: 3 new test files with full coverage of guard canActivate() methods and edge cases
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/common/guards/jwt-auth.guard.spec.ts
@apps/backend/src/modules/auth/guards/jwt-ws.guard.ts
@apps/backend/src/modules/auth/guards/mfa.guard.ts
@apps/backend/src/modules/auth/guards/throttle-behind-proxy.guard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create jwt-ws.guard.spec.ts with WebSocket context mocking</name>
  <files>apps/backend/src/modules/auth/guards/jwt-ws.guard.spec.ts</files>
  <action>
    Create unit tests for JwtWsGuard following the existing jwt-auth.guard.spec.ts pattern.

    Test cases required:
    1. should extract token from WebSocket handshake query string
    2. should extract token from WebSocket handshake headers (Authorization: Bearer)
    3. should throw WsException when no token provided
    4. should throw WsException when token is invalid/expired
    5. should set user on socket data when token is valid
    6. should verify JWT signature using JwtService
    7. should handle malformed Authorization header gracefully

    Mocking requirements:
    - Mock ExecutionContext.switchToWs() returning WsArgumentsHost
    - Mock getClient() returning socket with handshake.query and handshake.headers
    - Mock JwtService.verify() for token validation
    - Mock PrismaService.user.findUnique for user lookup
    - Mock withBypassRLS to pass through callback (critical - see research pitfall 1)

    Follow exact pattern from jwt-auth.guard.spec.ts for structure.

  </action>
  <verify>npm test -- --testPathPattern="jwt-ws.guard.spec" passes with all tests green</verify>
  <done>jwt-ws.guard.spec.ts has 7+ test cases covering all branches, 90%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create mfa.guard.spec.ts with async canActivate testing</name>
  <files>apps/backend/src/modules/auth/guards/mfa.guard.spec.ts</files>
  <action>
    Create unit tests for MfaGuard. Note: MfaGuard.canActivate() is ASYNC (calls mfaService.isMfaEnabled).

    Test cases required:
    1. should throw UnauthorizedException when no user in request
    2. should allow access when MFA is disabled for user
    3. should allow access when MFA is enabled AND mfaVerified is true in JWT
    4. should throw UnauthorizedException when MFA enabled but mfaVerified is false
    5. should throw UnauthorizedException when MFA enabled but mfaVerified is undefined
    6. should call mfaService.isMfaEnabled with correct userId
    7. should handle mfaService.isMfaEnabled errors gracefully

    Critical: Use async/await pattern for all tests - await expect(guard.canActivate(ctx)).rejects.toThrow()

    Mocking requirements:
    - Mock MfaService with isMfaEnabled: jest.fn()
    - Mock ExecutionContext with user object containing sub and optional mfaVerified
    - Use mockResolvedValue for async behavior

    Reference research code example "Complete Guard Test File (MfaGuard)".

  </action>
  <verify>npm test -- --testPathPattern="mfa.guard.spec" passes with all tests green</verify>
  <done>mfa.guard.spec.ts has 7+ test cases covering async behavior, 90%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Create throttle-behind-proxy.guard.spec.ts</name>
  <files>apps/backend/src/modules/auth/guards/throttle-behind-proxy.guard.spec.ts</files>
  <action>
    Create unit tests for ThrottleBehindProxyGuard which extracts client IP from X-Forwarded-For header.

    Test cases required:
    1. should use X-Forwarded-For header when present (first IP in comma-separated list)
    2. should fall back to request.ip when X-Forwarded-For not present
    3. should handle X-Forwarded-For with single IP
    4. should handle X-Forwarded-For with multiple IPs (use first)
    5. should handle malformed X-Forwarded-For gracefully
    6. should pass to parent ThrottlerGuard.getTracker() method

    Mocking requirements:
    - Mock ExecutionContext.switchToHttp().getRequest() with headers and ip
    - Verify correct tracker is returned based on IP extraction logic

  </action>
  <verify>npm test -- --testPathPattern="throttle-behind-proxy.guard.spec" passes</verify>
  <done>throttle-behind-proxy.guard.spec.ts has 6+ test cases, 90%+ coverage</done>
</task>

</tasks>

<verification>
Run all auth guard tests together:
```bash
npm test -- --testPathPattern="guard.spec" --coverage --collectCoverageFrom="**/guards/*.ts"
```

Verify:

- All 6 guards (jwt-auth, roles, tenant, jwt-ws, mfa, throttle-behind-proxy) have spec files
- Combined guard coverage is 90%+
- No test failures
  </verification>

<success_criteria>

1. 3 new guard spec files created
2. Each file has 6-8 comprehensive test cases
3. All tests pass: `npm test -- --testPathPattern="guard.spec"`
4. Auth guard coverage reaches 90%+ (from ~50% with 3 guards tested)
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-01-SUMMARY.md`
</output>
