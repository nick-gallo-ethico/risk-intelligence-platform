---
phase: 36-test-coverage-expansion
plan: 09
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/backend/src/modules/ai/services/prompt.service.spec.ts
  - apps/backend/src/modules/ai/services/rate-limiter.service.spec.ts
  - apps/backend/src/modules/ai/actions/action-executor.service.spec.ts
autonomous: true

must_haves:
  truths:
    - "prompt.service.spec.ts exists with 70%+ coverage"
    - "rate-limiter.service.spec.ts exists with 70%+ coverage"
    - "action-executor.service.spec.ts exists with 70%+ coverage"
    - "All AI service tests pass"
  artifacts:
    - path: "apps/backend/src/modules/ai/services/prompt.service.spec.ts"
      provides: "Prompt service unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/ai/services/rate-limiter.service.spec.ts"
      provides: "Rate limiter service unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/ai/actions/action-executor.service.spec.ts"
      provides: "Action executor service unit tests"
      min_lines: 100
  key_links:
    - from: "*.service.spec.ts"
      to: "*.service.ts"
      via: "import statement"
      pattern: "import.*Service.*from"
---

<objective>
Write unit tests for remaining AI services: prompt, rate-limiter, action-executor.

Purpose: Complete TEST-07 requirement - test prompt templating, rate limiting, and action execution
Output: 3 test files completing AI service test coverage
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/ai/services/prompt.service.ts
@apps/backend/src/modules/ai/services/rate-limiter.service.ts
@apps/backend/src/modules/ai/actions/action-executor.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prompt.service.spec.ts and rate-limiter.service.spec.ts</name>
  <files>
    apps/backend/src/modules/ai/services/prompt.service.spec.ts
    apps/backend/src/modules/ai/services/rate-limiter.service.spec.ts
  </files>
  <action>
    Create unit tests for prompt templating and rate limiting.

    prompt.service.spec.ts:
    1. should load prompt template by name
    2. should render template with Handlebars variables
    3. should handle missing template gracefully
    4. should support nested helpers in templates
    5. should escape HTML in variable values
    6. should cache compiled templates
    7. should version templates (getByVersion)
    8. should create new template
    9. should list templates by category
    10. should throw on invalid template syntax

    rate-limiter.service.spec.ts:
    1. should allow request within rate limit
    2. should reject request exceeding rate limit
    3. should track limits per tenant (not global)
    4. should track limits per user within tenant
    5. should reset limits after window expires
    6. should use Redis sorted set for sliding window
    7. should return remaining quota in response
    8. should handle Redis connection errors gracefully
    9. should support different limits per tier (free/pro/enterprise)
    10. should log rate limit violations

    Mock Redis for rate limiter:
    ```typescript
    const mockRedis = {
      zrangebyscore: jest.fn(),
      zadd: jest.fn(),
      zremrangebyscore: jest.fn(),
      expire: jest.fn()
    };
    ```

  </action>
  <verify>npm test -- --testPathPattern="prompt.service.spec|rate-limiter.service.spec" passes</verify>
  <done>2 spec files with 10+ tests each, 70%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create action-executor.service.spec.ts</name>
  <files>apps/backend/src/modules/ai/actions/action-executor.service.spec.ts</files>
  <action>
    Create unit tests for ActionExecutorService covering preview-then-execute pattern.

    Test cases:
    1. preview() should return proposed changes without executing
    2. preview() should validate action parameters
    3. preview() should check user permissions for action
    4. execute() should apply the previewed changes
    5. execute() should reject if preview not called first
    6. execute() should reject if preview expired (>5 min)
    7. execute() should reject if entity changed since preview
    8. should support action types: UPDATE_STATUS, ADD_NOTE, ASSIGN_USER, UPDATE_FIELD
    9. should log all executed actions to activity
    10. should emit events after successful execution
    11. should rollback on partial failure
    12. should respect tenant isolation (cannot execute cross-tenant)

    Mock requirements:
    - Mock individual action handlers (add-note.action, update-status.action)
    - Mock PrismaService for entity updates
    - Mock ActivityService for logging
    - Mock EventEmitter2 for events

    Test the full preview -> confirm -> execute flow.

  </action>
  <verify>npm test -- --testPathPattern="action-executor.service.spec" passes</verify>
  <done>action-executor.service.spec.ts has 12+ tests covering preview-execute pattern, 70%+ coverage</done>
</task>

</tasks>

<verification>
Run all AI service tests including previous plan:
```bash
npm test -- --testPathPattern="ai.*spec|action-executor" --coverage --collectCoverageFrom="**/ai/**/*.ts"
```

Verify:

- All 7 AI service spec files exist (4 from plan 08 + 3 from plan 09)
- Rate limiting tests verify per-tenant isolation
- Action executor tests cover preview-then-execute pattern
- Combined AI module coverage is 70%+
  </verification>

<success_criteria>

1. 3 spec files created
2. prompt.service has 10+ tests
3. rate-limiter.service has 10+ tests with Redis mocking
4. action-executor.service has 12+ tests covering full flow
5. TEST-07 complete: all 7 AI services have tests
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-09-SUMMARY.md`
</output>
