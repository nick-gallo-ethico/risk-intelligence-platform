---
phase: 36-test-coverage-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/operations/impersonation/impersonation.service.spec.ts
  - apps/backend/src/modules/operations/impersonation/impersonation.middleware.spec.ts
  - apps/backend/src/modules/operations/impersonation/impersonation.guard.spec.ts
autonomous: true

must_haves:
  truths:
    - "impersonation.service.spec.ts exists with 90%+ coverage"
    - "impersonation.middleware.spec.ts exists with 90%+ coverage"
    - "impersonation.guard.spec.ts exists with 90%+ coverage"
    - "All impersonation tests pass with npm test"
  artifacts:
    - path: "apps/backend/src/modules/operations/impersonation/impersonation.service.spec.ts"
      provides: "Impersonation service unit tests"
      min_lines: 120
    - path: "apps/backend/src/modules/operations/impersonation/impersonation.middleware.spec.ts"
      provides: "Impersonation middleware unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/operations/impersonation/impersonation.guard.spec.ts"
      provides: "Impersonation guard unit tests"
      min_lines: 60
  key_links:
    - from: "impersonation.service.spec.ts"
      to: "impersonation.service.ts"
      via: "import statement"
      pattern: "import.*ImpersonationService.*from"
---

<objective>
Write unit tests for impersonation service, middleware, and guard to achieve 90%+ coverage.

Purpose: Complete TEST-03 requirement - impersonation is a high-risk cross-tenant feature requiring comprehensive security testing
Output: 3 test files covering impersonation start/stop, session switching, and permission validation
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/operations/impersonation/impersonation.service.ts
@apps/backend/src/modules/operations/impersonation/impersonation.middleware.ts
@apps/backend/src/modules/operations/impersonation/impersonation.guard.ts
@apps/backend/src/common/middleware/tenant.middleware.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create impersonation.service.spec.ts</name>
  <files>apps/backend/src/modules/operations/impersonation/impersonation.service.spec.ts</files>
  <action>
    Create unit tests for ImpersonationService covering all impersonation lifecycle operations.

    Test cases required:
    1. startImpersonation():
       - should create impersonation session in Redis
       - should store original user context (id, organizationId, role)
       - should return new JWT with impersonated user claims
       - should throw when impersonator lacks SYSTEM_ADMIN or SUPPORT role
       - should throw when target user not found
       - should throw when target user is in different org (unless super admin)
       - should log audit event for impersonation start

    2. stopImpersonation():
       - should delete impersonation session from Redis
       - should return JWT with original user claims
       - should throw when no active impersonation session
       - should log audit event for impersonation stop

    3. getOriginalUser():
       - should return original user from Redis session
       - should return null when no active impersonation

    4. isImpersonating():
       - should return true when impersonation session exists
       - should return false when no session

    Mocking requirements:
    - Mock Redis client with get, set, del methods
    - Mock PrismaService for user lookup
    - Mock JwtService for token generation
    - Mock ActivityService for audit logging

  </action>
  <verify>npm test -- --testPathPattern="impersonation.service.spec" passes</verify>
  <done>impersonation.service.spec.ts has 12+ test cases covering all methods, 90%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create impersonation.middleware.spec.ts and impersonation.guard.spec.ts</name>
  <files>
    apps/backend/src/modules/operations/impersonation/impersonation.middleware.spec.ts
    apps/backend/src/modules/operations/impersonation/impersonation.guard.spec.ts
  </files>
  <action>
    Create unit tests for ImpersonationMiddleware and ImpersonationGuard.

    ImpersonationMiddleware tests:
    1. should check for impersonation session on each request
    2. should modify request.user with impersonated identity when active
    3. should preserve original user reference for audit
    4. should pass through unchanged when no impersonation active
    5. should call next() in all cases
    6. should handle Redis errors gracefully (continue without impersonation)

    ImpersonationGuard tests:
    1. should allow access when user has SYSTEM_ADMIN role
    2. should allow access when user has SUPPORT role
    3. should deny access when user lacks required roles
    4. should check role from request.user
    5. should throw ForbiddenException with clear message

    Follow tenant.middleware.spec.ts pattern for middleware testing structure.

  </action>
  <verify>npm test -- --testPathPattern="impersonation.(middleware|guard).spec" passes</verify>
  <done>Both spec files created with comprehensive tests, 90%+ coverage</done>
</task>

</tasks>

<verification>
Run all impersonation tests:
```bash
npm test -- --testPathPattern="impersonation" --coverage --collectCoverageFrom="**/impersonation/*.ts"
```

Verify:

- All 3 impersonation spec files exist
- Combined impersonation coverage is 90%+
- No test failures
  </verification>

<success_criteria>

1. 3 new impersonation spec files created
2. impersonation.service.spec.ts has 12+ test cases
3. Middleware and guard specs have 6+ test cases each
4. All tests pass: `npm test -- --testPathPattern="impersonation"`
5. Impersonation module coverage reaches 90%+
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-03-SUMMARY.md`
</output>
