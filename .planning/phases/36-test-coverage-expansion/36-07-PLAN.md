---
phase: 36-test-coverage-expansion
plan: 07
type: execute
wave: 3
depends_on: ["36-01", "36-02", "36-03"]
files_modified:
  - apps/backend/src/modules/cases/case-merge.service.spec.ts
  - apps/backend/src/modules/disclosures/conflict-detection.service.spec.ts
autonomous: true

must_haves:
  truths:
    - "case-merge.service.spec.ts exists with 80%+ coverage"
    - "conflict-detection.service.spec.ts exists with 80%+ coverage"
    - "All merge and conflict tests pass"
  artifacts:
    - path: "apps/backend/src/modules/cases/case-merge.service.spec.ts"
      provides: "Case merge service unit tests"
      min_lines: 150
    - path: "apps/backend/src/modules/disclosures/conflict-detection.service.spec.ts"
      provides: "Conflict detection service unit tests"
      min_lines: 120
  key_links:
    - from: "case-merge.service.spec.ts"
      to: "case-merge.service.ts"
      via: "import statement"
      pattern: "import.*CaseMergeService.*from"
---

<objective>
Write unit tests for case-merge.service.ts and conflict-detection.service.ts.

Purpose: Complete TEST-05 and TEST-06 requirements - these services have complex business logic requiring thorough testing
Output: 2 test files covering merge transactions and conflict type detection
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/cases/case-merge.service.ts
@apps/backend/src/modules/disclosures/conflict-detection.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create case-merge.service.spec.ts with transaction mocking</name>
  <files>apps/backend/src/modules/cases/case-merge.service.spec.ts</files>
  <action>
    Create comprehensive unit tests for CaseMergeService.

    Test cases for merge():
    1. should reject merging case into itself
    2. should reject when source case not found
    3. should reject when target case not found
    4. should reject when cases are in different organizations
    5. should move all RIU associations from source to target
    6. should move all Subject associations from source to target
    7. should move all Investigation links from source to target
    8. should move all CaseMessage records from source to target
    9. should move all Interaction records from source to target
    10. should update source case status to MERGED
    11. should set mergedIntoId on source case
    12. should log activity on both source and target cases
    13. should emit case.merged event

    Test cases for rollback():
    1. should restore original associations
    2. should clear mergedIntoId
    3. should restore source case status
    4. should reject rollback if merge too old (>7 days)

    Critical: Mock $transaction to execute callback with mock prisma:
    ```typescript
    $transaction: jest.fn(cb => cb(mockPrisma))
    ```

    Reference research "Pattern 3: Service Test with Prisma Transaction Mocking".

  </action>
  <verify>npm test -- --testPathPattern="case-merge.service.spec" passes with 80%+ coverage</verify>
  <done>case-merge.service.spec.ts has 15+ test cases covering merge and rollback flows</done>
</task>

<task type="auto">
  <name>Task 2: Create conflict-detection.service.spec.ts for all 6 conflict types</name>
  <files>apps/backend/src/modules/disclosures/conflict-detection.service.spec.ts</files>
  <action>
    Create unit tests for ConflictDetectionService covering all 6 conflict types.

    Conflict types to test:
    1. SELF_DEALING - Employee has financial interest in disclosed entity
    2. FAMILY_RELATIONSHIP - Disclosed relationship with family member at vendor/customer
    3. OUTSIDE_EMPLOYMENT - Employment at competitor or conflicting organization
    4. GIFT_THRESHOLD - Gift value exceeds allowed threshold
    5. HOSPITALITY_THRESHOLD - Entertainment/hospitality exceeds threshold
    6. RECURRING_PATTERN - Same entity appears in multiple disclosures

    For each conflict type, test:
    - detectConflicts() identifies the conflict correctly
    - Conflict has appropriate severity (HIGH, MEDIUM, LOW)
    - Conflict reason message is clear and actionable
    - Edge cases (null values, missing relationships)

    Additional tests:
    - should return empty array when no conflicts detected
    - should detect multiple conflicts in single disclosure
    - should use fuzzy matching for entity names (per research)
    - should handle prior disclosure lookups
    - should not flag dismissed conflicts

    Mock requirements:
    - PrismaService for disclosure/conflict lookups
    - ThresholdService for threshold value lookups
    - FuzzyMatchingService for name comparison

  </action>
  <verify>npm test -- --testPathPattern="conflict-detection.service.spec" passes with 80%+ coverage</verify>
  <done>conflict-detection.service.spec.ts has 18+ test cases covering all 6 conflict types</done>
</task>

</tasks>

<verification>
Run case and conflict tests:
```bash
npm test -- --testPathPattern="case-merge|conflict-detection" --coverage
```

Verify:

- Both spec files exist
- case-merge.service has 80%+ coverage
- conflict-detection.service has 80%+ coverage
- All tests pass
  </verification>

<success_criteria>

1. case-merge.service.spec.ts created with 15+ tests
2. conflict-detection.service.spec.ts created with 18+ tests
3. Transaction mocking correctly executes callback
4. All 6 conflict types have explicit tests
5. Both services achieve 80%+ coverage
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-07-SUMMARY.md`
</output>
