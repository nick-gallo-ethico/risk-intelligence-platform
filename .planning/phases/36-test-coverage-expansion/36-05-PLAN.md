---
phase: 36-test-coverage-expansion
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/test/e2e/reporting-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/ai-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/forms-tenant-isolation.e2e-spec.ts
  - apps/backend/test/e2e/notifications-tenant-isolation.e2e-spec.ts
autonomous: true

must_haves:
  truths:
    - "Reporting tenant isolation E2E tests exist"
    - "AI tenant isolation E2E tests exist"
    - "Forms tenant isolation E2E tests exist"
    - "Notifications tenant isolation E2E tests exist"
    - "All E2E tests verify cross-tenant access returns 404"
  artifacts:
    - path: "apps/backend/test/e2e/reporting-tenant-isolation.e2e-spec.ts"
      provides: "Reporting tenant isolation E2E tests"
      min_lines: 80
    - path: "apps/backend/test/e2e/ai-tenant-isolation.e2e-spec.ts"
      provides: "AI tenant isolation E2E tests"
      min_lines: 80
    - path: "apps/backend/test/e2e/forms-tenant-isolation.e2e-spec.ts"
      provides: "Forms tenant isolation E2E tests"
      min_lines: 80
    - path: "apps/backend/test/e2e/notifications-tenant-isolation.e2e-spec.ts"
      provides: "Notifications tenant isolation E2E tests"
      min_lines: 80
  key_links:
    - from: "*-tenant-isolation.e2e-spec.ts"
      to: "test/helpers/test-setup.ts"
      via: "import createTestContext"
      pattern: "import.*createTestContext.*from"
---

<objective>
Add tenant isolation E2E tests for 4 more modules: reporting, AI, forms, notifications.

Purpose: Continue TEST-04 requirement - expand tenant isolation verification to 8 more modules total
Output: 4 E2E test files ensuring cross-tenant data cannot be accessed
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/test/helpers/test-setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reporting and AI tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/reporting-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/ai-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests using createTestContext() pattern.

    reporting-tenant-isolation.e2e-spec.ts:
    - GET /reports: Org A sees only Org A saved reports
    - GET /reports/:id: Org B gets 404 for Org A report
    - POST /reports/:id/run: Org B cannot run Org A report
    - GET /dashboards: Org A sees only Org A dashboards
    - GET /dashboards/:id: Org B gets 404 for Org A dashboard
    - GET /exports: Org B cannot see Org A scheduled exports
    - Verify report results never include cross-tenant data

    ai-tenant-isolation.e2e-spec.ts:
    - GET /ai/conversations: Org A sees only Org A AI conversations
    - GET /ai/conversations/:id: Org B gets 404 for Org A conversation
    - POST /ai/chat: Verify AI context loading respects tenant
    - Test that AI skills only access caller's tenant data
    - Verify AI-generated summaries don't leak cross-tenant info
    - Test rate limiting is per-tenant (not global)

    CRITICAL: AI tests must verify context isolation - AI should NEVER see data from other tenants.

  </action>
  <verify>npm run test:e2e -- --testPathPattern="reporting-tenant|ai-tenant" passes</verify>
  <done>2 E2E test files with 6+ tests each, AI context verified tenant-isolated</done>
</task>

<task type="auto">
  <name>Task 2: Create forms and notifications tenant isolation E2E tests</name>
  <files>
    apps/backend/test/e2e/forms-tenant-isolation.e2e-spec.ts
    apps/backend/test/e2e/notifications-tenant-isolation.e2e-spec.ts
  </files>
  <action>
    Create E2E tests following same pattern.

    forms-tenant-isolation.e2e-spec.ts:
    - GET /forms: Org A sees only Org A form definitions
    - GET /forms/:id: Org B gets 404 for Org A form
    - PUT /forms/:id: Org B cannot update Org A form
    - GET /form-submissions: Org B cannot see Org A submissions
    - POST /forms/:id/submit: Verify submission creates in caller's org only
    - Test form template isolation

    notifications-tenant-isolation.e2e-spec.ts:
    - GET /notifications: Org A sees only Org A user's notifications
    - PUT /notifications/:id/read: Org B cannot mark Org A notification as read
    - GET /notification-preferences: Returns only caller's preferences
    - Test WebSocket notification rooms are tenant-scoped
    - Verify email delivery records are tenant-isolated

    Ensure proper cleanup in afterAll.

  </action>
  <verify>npm run test:e2e -- --testPathPattern="forms-tenant|notifications-tenant" passes</verify>
  <done>2 E2E test files with 6+ tests each, all cross-tenant access returns 404</done>
</task>

</tasks>

<verification>
Run all new tenant isolation E2E tests:
```bash
npm run test:e2e -- --testPathPattern="(reporting|ai|forms|notifications)-tenant-isolation"
```

Verify:

- All 4 new E2E test files exist
- Cross-tenant access attempts receive 404
- AI context loading verified tenant-scoped
- No test failures
  </verification>

<success_criteria>

1. 4 new E2E test files created
2. Each file has 6+ test cases
3. AI tests verify context isolation
4. All tests pass
5. 8 total new modules with tenant isolation coverage (plan 04 + 05)
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-05-SUMMARY.md`
</output>
