---
phase: 36-test-coverage-expansion
plan: 12
type: execute
wave: 4
depends_on: ["36-11"]
files_modified:
  - apps/frontend/src/components/forms/__tests__/form-builder.test.tsx
  - apps/frontend/src/components/forms/__tests__/form-renderer.test.tsx
  - apps/frontend/src/components/workflow/__tests__/workflow-builder.test.tsx
  - apps/frontend/src/components/workflow/__tests__/workflow-canvas.test.tsx
autonomous: true

must_haves:
  truths:
    - "form-builder.test.tsx exists with form construction tests"
    - "form-renderer.test.tsx exists with submission tests"
    - "workflow-builder.test.tsx exists"
    - "workflow-canvas.test.tsx exists"
    - "All frontend tests pass"
  artifacts:
    - path: "apps/frontend/src/components/forms/__tests__/form-builder.test.tsx"
      provides: "Form builder component tests"
      min_lines: 80
    - path: "apps/frontend/src/components/forms/__tests__/form-renderer.test.tsx"
      provides: "Form renderer component tests"
      min_lines: 80
    - path: "apps/frontend/src/components/workflow/__tests__/workflow-builder.test.tsx"
      provides: "Workflow builder component tests"
      min_lines: 60
    - path: "apps/frontend/src/components/workflow/__tests__/workflow-canvas.test.tsx"
      provides: "Workflow canvas component tests"
      min_lines: 60
  key_links:
    - from: "*.test.tsx"
      to: "components/**/*.tsx"
      via: "import statement"
      pattern: "import.*from.*components"
---

<objective>
Expand frontend test coverage for forms and workflow builder components.

Purpose: Continue TEST-09 requirement - test complex interactive components
Output: 4 test files covering form building/rendering and workflow visual editor
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create form builder and renderer tests</name>
  <files>
    apps/frontend/src/components/forms/__tests__/form-builder.test.tsx
    apps/frontend/src/components/forms/__tests__/form-renderer.test.tsx
  </files>
  <action>
    Create tests for form components with drag-drop and validation.

    form-builder.test.tsx:
    1. should render empty form canvas
    2. should add text field on drag from palette
    3. should add select field with options
    4. should reorder fields via drag
    5. should edit field properties in side panel
    6. should set field as required
    7. should add conditional logic to field
    8. should preview form in preview mode
    9. should save form definition
    10. should validate form has at least one field

    form-renderer.test.tsx:
    1. should render form fields from definition
    2. should show required indicator on required fields
    3. should validate required fields on submit
    4. should show validation errors inline
    5. should handle text field input
    6. should handle select field selection
    7. should handle checkbox field
    8. should handle date field with date picker
    9. should handle file upload field
    10. should submit form data on valid submission
    11. should auto-save draft periodically
    12. should restore from saved draft

    Mock @dnd-kit for drag operations:
    ```typescript
    jest.mock('@dnd-kit/core', () => ({
      DndContext: ({ children }) => <>{children}</>,
      useDraggable: () => ({ attributes: {}, listeners: {}, setNodeRef: jest.fn() }),
      useDroppable: () => ({ setNodeRef: jest.fn(), isOver: false })
    }));
    ```

  </action>
  <verify>npm run test:frontend -- --testPathPattern="form-(builder|renderer).test" passes</verify>
  <done>2 form test files with 10+ tests each</done>
</task>

<task type="auto">
  <name>Task 2: Create workflow builder and canvas tests</name>
  <files>
    apps/frontend/src/components/workflow/__tests__/workflow-builder.test.tsx
    apps/frontend/src/components/workflow/__tests__/workflow-canvas.test.tsx
  </files>
  <action>
    Create tests for workflow visual editor with React Flow.

    workflow-builder.test.tsx:
    1. should render workflow builder layout
    2. should show stage palette with available stages
    3. should load existing workflow definition
    4. should save workflow on save button click
    5. should show validation errors for invalid workflow
    6. should publish workflow (version increment)
    7. should show unsaved changes indicator

    workflow-canvas.test.tsx:
    1. should render React Flow canvas
    2. should add stage node on drag from palette
    3. should connect nodes with edge on drag between handles
    4. should select node on click
    5. should show properties panel for selected node
    6. should delete node on delete key
    7. should delete edge on edge delete
    8. should zoom canvas with scroll
    9. should pan canvas with drag
    10. should auto-layout nodes

    Mock @xyflow/react:
    ```typescript
    jest.mock('@xyflow/react', () => ({
      ReactFlow: ({ children }) => <div data-testid="react-flow">{children}</div>,
      Background: () => null,
      Controls: () => null,
      useReactFlow: () => ({ fitView: jest.fn(), setNodes: jest.fn() })
    }));
    ```

  </action>
  <verify>npm run test:frontend -- --testPathPattern="workflow-(builder|canvas).test" passes</verify>
  <done>2 workflow test files with 8+ tests each</done>
</task>

</tasks>

<verification>
Run all frontend tests:
```bash
cd apps/frontend && npm run test -- --coverage
```

Verify:

- All 4 test files exist
- Drag-drop interactions properly mocked
- React Flow properly mocked
- Form validation tested
- No console errors
  </verification>

<success_criteria>

1. 4 frontend test files created
2. Form tests cover builder and renderer flows
3. Workflow tests cover canvas interactions
4. Complex library dependencies mocked
5. All tests pass
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-12-SUMMARY.md`
</output>
