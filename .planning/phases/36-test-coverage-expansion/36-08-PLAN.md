---
phase: 36-test-coverage-expansion
plan: 08
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/backend/src/modules/ai/services/ai-client.service.spec.ts
  - apps/backend/src/modules/ai/services/ai-orchestration.service.spec.ts
  - apps/backend/src/modules/ai/services/context-loader.service.spec.ts
  - apps/backend/src/modules/ai/services/conversation.service.spec.ts
autonomous: true

must_haves:
  truths:
    - "ai-client.service.spec.ts exists with 70%+ coverage"
    - "ai-orchestration.service.spec.ts exists with 70%+ coverage"
    - "context-loader.service.spec.ts exists with 70%+ coverage"
    - "conversation.service.spec.ts exists with 70%+ coverage"
    - "All AI service tests pass"
  artifacts:
    - path: "apps/backend/src/modules/ai/services/ai-client.service.spec.ts"
      provides: "AI client service unit tests"
      min_lines: 100
    - path: "apps/backend/src/modules/ai/services/ai-orchestration.service.spec.ts"
      provides: "AI orchestration service unit tests"
      min_lines: 100
    - path: "apps/backend/src/modules/ai/services/context-loader.service.spec.ts"
      provides: "Context loader service unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/ai/services/conversation.service.spec.ts"
      provides: "Conversation service unit tests"
      min_lines: 80
  key_links:
    - from: "*.service.spec.ts"
      to: "*.service.ts"
      via: "import statement"
      pattern: "import.*Service.*from"
    - from: "ai-client.service.spec.ts"
      to: "@anthropic-ai/sdk"
      via: "jest.mock"
      pattern: "jest.mock.*anthropic"
---

<objective>
Write unit tests for 4 core AI services: ai-client, ai-orchestration, context-loader, conversation.

Purpose: Complete TEST-07 requirement (partial) - test AI services with mocked Anthropic SDK
Output: 4 test files covering AI request/response handling, context building, and conversation persistence
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/ai/services/ai-client.service.ts
@apps/backend/src/modules/ai/services/ai-orchestration.service.ts
@apps/backend/src/modules/ai/services/context-loader.service.ts
@apps/backend/src/modules/ai/services/conversation.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ai-client and ai-orchestration service tests</name>
  <files>
    apps/backend/src/modules/ai/services/ai-client.service.spec.ts
    apps/backend/src/modules/ai/services/ai-orchestration.service.spec.ts
  </files>
  <action>
    Create unit tests with mocked Anthropic SDK.

    ai-client.service.spec.ts:
    1. should create Anthropic client on init
    2. should send message to Claude API
    3. should handle streaming response correctly
    4. should respect max_tokens limit
    5. should include system prompt in request
    6. should handle API rate limit errors (429)
    7. should handle API authentication errors (401)
    8. should handle network timeout errors
    9. should retry on transient failures (configurable)
    10. should log token usage after response

    ai-orchestration.service.spec.ts:
    1. should route request to appropriate skill
    2. should build context from context-loader
    3. should validate user permissions for actions
    4. should execute action after user confirmation
    5. should handle skill not found error
    6. should handle context building failure
    7. should log conversation to database
    8. should respect tenant isolation in context

    Mock Anthropic SDK at top of test file:
    ```typescript
    jest.mock('@anthropic-ai/sdk', () => ({
      Anthropic: jest.fn().mockImplementation(() => ({
        messages: {
          create: jest.fn().mockResolvedValue({
            content: [{ type: 'text', text: 'Response' }],
            usage: { input_tokens: 100, output_tokens: 50 }
          })
        }
      }))
    }));
    ```

  </action>
  <verify>npm test -- --testPathPattern="ai-client.service.spec|ai-orchestration.service.spec" passes</verify>
  <done>2 AI service spec files with 10+ tests each, 70%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create context-loader and conversation service tests</name>
  <files>
    apps/backend/src/modules/ai/services/context-loader.service.spec.ts
    apps/backend/src/modules/ai/services/conversation.service.spec.ts
  </files>
  <action>
    Create unit tests for context and conversation management.

    context-loader.service.spec.ts:
    1. should load platform-level context
    2. should load organization-level context for tenant
    3. should load team-level context when team provided
    4. should load user-level context (preferences, history)
    5. should load entity-specific context (case, investigation)
    6. should merge context hierarchy correctly
    7. should exclude cross-tenant data (CRITICAL - tenant isolation)
    8. should handle missing context levels gracefully
    9. should cache frequently accessed context
    10. should truncate context if exceeding token limit

    conversation.service.spec.ts:
    1. should create new conversation
    2. should add message to existing conversation
    3. should retrieve conversation history
    4. should filter conversations by tenant
    5. should paginate conversation list
    6. should update conversation title
    7. should delete conversation
    8. should associate conversation with entity (case, investigation)
    9. should track token usage per conversation

    Mock PrismaService for all database operations.

  </action>
  <verify>npm test -- --testPathPattern="context-loader.service.spec|conversation.service.spec" passes</verify>
  <done>2 spec files with 10+ tests each, 70%+ coverage</done>
</task>

</tasks>

<verification>
Run all AI service tests:
```bash
npm test -- --testPathPattern="ai.*service.spec" --coverage --collectCoverageFrom="**/ai/services/*.ts"
```

Verify:

- All 4 AI service spec files exist
- Anthropic SDK properly mocked with jest.mock('@anthropic-ai/sdk')
- Tenant isolation verified in context-loader tests
- Combined AI service coverage is 70%+
  </verification>

<success_criteria>

1. 4 AI service spec files created
2. Each file has 8-10 test cases
3. Anthropic SDK mocked (no real API calls)
4. Context-loader tests verify tenant isolation
5. AI services achieve 70%+ coverage
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-08-SUMMARY.md`
</output>
