---
phase: 36-test-coverage-expansion
plan: 10
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/backend/src/modules/workflow/engine/workflow-engine.service.spec.ts
  - apps/backend/src/modules/workflow/assignment/strategies/round-robin.strategy.spec.ts
  - apps/backend/src/modules/workflow/assignment/strategies/least-loaded.strategy.spec.ts
  - apps/backend/src/modules/workflow/assignment/strategies/geographic.strategy.spec.ts
autonomous: true

must_haves:
  truths:
    - "workflow-engine.service.spec.ts exists with 70%+ coverage"
    - "round-robin.strategy.spec.ts exists with 90%+ coverage"
    - "least-loaded.strategy.spec.ts exists with 90%+ coverage"
    - "geographic.strategy.spec.ts exists with 90%+ coverage"
    - "All workflow tests pass"
  artifacts:
    - path: "apps/backend/src/modules/workflow/engine/workflow-engine.service.spec.ts"
      provides: "Workflow engine service unit tests"
      min_lines: 150
    - path: "apps/backend/src/modules/workflow/assignment/strategies/round-robin.strategy.spec.ts"
      provides: "Round-robin strategy unit tests"
      min_lines: 60
    - path: "apps/backend/src/modules/workflow/assignment/strategies/least-loaded.strategy.spec.ts"
      provides: "Least-loaded strategy unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/workflow/assignment/strategies/geographic.strategy.spec.ts"
      provides: "Geographic strategy unit tests"
      min_lines: 60
  key_links:
    - from: "*.spec.ts"
      to: "*.ts"
      via: "import statement"
      pattern: "import.*(Service|Strategy).*from"
---

<objective>
Write unit tests for workflow engine and 3 assignment strategies.

Purpose: Complete TEST-08 requirement - test state machine transitions and assignment algorithms
Output: 4 test files covering workflow lifecycle and assignment strategy selection
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-test-coverage-expansion/36-RESEARCH.md
@apps/backend/src/modules/workflow/engine/workflow-engine.service.ts
@apps/backend/src/modules/workflow/assignment/strategies/round-robin.strategy.ts
@apps/backend/src/modules/workflow/assignment/strategies/least-loaded.strategy.ts
@apps/backend/src/modules/workflow/assignment/strategies/geographic.strategy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow-engine.service.spec.ts</name>
  <files>apps/backend/src/modules/workflow/engine/workflow-engine.service.spec.ts</files>
  <action>
    Create comprehensive unit tests for WorkflowEngineService state machine.

    Test cases for state transitions:
    1. should start workflow instance at initial stage
    2. should transition to next stage on action
    3. should reject invalid transition (not in allowed transitions)
    4. should execute entry actions on stage enter
    5. should execute exit actions on stage leave
    6. should evaluate conditions for conditional transitions
    7. should handle parallel branches (fork)
    8. should handle branch merge (join)
    9. should complete workflow when reaching terminal stage
    10. should cancel workflow instance
    11. should handle version-on-publish (in-flight instances stay on old version)

    Test cases for assignment:
    12. should auto-assign using configured strategy
    13. should invoke assignment strategy on stage transition
    14. should skip assignment when stage.autoAssign is false

    Test cases for SLA:
    15. should set due date based on stage SLA configuration
    16. should emit SLA warning event when approaching deadline
    17. should emit SLA breach event when past deadline

    Mock requirements:
    - PrismaService for instance and stage lookups
    - EventEmitter2 for workflow events
    - AssignmentService for user assignment

  </action>
  <verify>npm test -- --testPathPattern="workflow-engine.service.spec" passes with 70%+ coverage</verify>
  <done>workflow-engine.service.spec.ts has 17+ tests covering state machine, 70%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Create assignment strategy tests</name>
  <files>
    apps/backend/src/modules/workflow/assignment/strategies/round-robin.strategy.spec.ts
    apps/backend/src/modules/workflow/assignment/strategies/least-loaded.strategy.spec.ts
    apps/backend/src/modules/workflow/assignment/strategies/geographic.strategy.spec.ts
  </files>
  <action>
    Create unit tests for each assignment strategy.

    round-robin.strategy.spec.ts:
    1. should return users in rotating order
    2. should persist position in Redis
    3. should handle single user (always return same)
    4. should handle user list change (re-index)
    5. should filter inactive users
    6. should respect team membership

    least-loaded.strategy.spec.ts:
    1. should return user with fewest active cases
    2. should handle tie (return first)
    3. should only count open cases (not closed)
    4. should weight cases by priority if configured
    5. should filter inactive users
    6. should respect capacity limits if configured
    7. should consider pending assignments

    geographic.strategy.spec.ts:
    1. should match case location to user location
    2. should fall back to least-loaded when no location match
    3. should handle missing location data
    4. should support region hierarchy (city > state > country)
    5. should respect user location assignments
    6. should handle no available users in region

    All strategies should:
    - Return null when no eligible users
    - Respect tenant isolation
    - Log assignment decision for audit

  </action>
  <verify>npm test -- --testPathPattern="(round-robin|least-loaded|geographic).strategy.spec" passes</verify>
  <done>3 strategy spec files with 6+ tests each, 90%+ coverage</done>
</task>

</tasks>

<verification>
Run all workflow tests:
```bash
npm test -- --testPathPattern="workflow.*spec" --coverage --collectCoverageFrom="**/workflow/**/*.ts"
```

Verify:

- workflow-engine.service.spec.ts exists with 17+ tests
- 3 strategy spec files exist with 6+ tests each
- State machine transitions tested
- Assignment strategies return correct users
- Combined workflow coverage is 70%+
  </verification>

<success_criteria>

1. workflow-engine.service.spec.ts created with 17+ tests
2. 3 assignment strategy spec files created
3. State machine transitions fully tested
4. Assignment algorithms produce correct selections
5. TEST-08 complete: workflow engine and strategies have tests
   </success_criteria>

<output>
After completion, create `.planning/phases/36-test-coverage-expansion/36-10-SUMMARY.md`
</output>
