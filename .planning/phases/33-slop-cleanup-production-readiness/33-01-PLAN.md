---
phase: 33-slop-cleanup-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/app.module.ts
  - apps/backend/src/modules/notifications/gateways/notification.gateway.ts
  - apps/backend/src/modules/projects/gateways/project.gateway.ts
  - apps/backend/src/modules/storage/storage.module.ts
  - apps/backend/package.json
autonomous: true

must_haves:
  truths:
    - "FeatureFlagsModule, MetricsModule, SentryModule are imported in AppModule"
    - "Gateway files use ConfigService.getOrThrow() not process.env.JWT_SECRET"
    - "storage.module.ts uses NestJS Logger not console.error"
    - "@faker-js/faker is in devDependencies, not dependencies"
  artifacts:
    - path: "apps/backend/src/app.module.ts"
      provides: "Module registration"
      contains: "FeatureFlagsModule"
    - path: "apps/backend/src/modules/projects/gateways/project.gateway.ts"
      provides: "ConfigService usage"
      contains: "configService.getOrThrow"
    - path: "apps/backend/src/modules/storage/storage.module.ts"
      provides: "Logger usage"
      contains: "Logger"
  key_links:
    - from: "apps/backend/src/app.module.ts"
      to: "FeatureFlagsModule"
      via: "import statement"
      pattern: "import.*FeatureFlagsModule"
---

<objective>
Register orphaned modules and fix production configuration issues.

Purpose: Three complete modules (feature-flags, metrics, sentry) exist but are not imported in AppModule. Additionally, gateway files use direct process.env access which bypasses NestJS ConfigService validation, and storage.module.ts uses console.error instead of structured logging.
Output: All modules registered, production-ready configuration patterns applied.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-slop-cleanup-production-readiness/33-RESEARCH.md
@apps/backend/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register orphaned modules in AppModule</name>
  <files>apps/backend/src/app.module.ts</files>
  <action>
Add imports for the three orphaned modules at the top of app.module.ts:

```typescript
import { FeatureFlagsModule } from "./modules/feature-flags/feature-flags.module";
import { MetricsModule } from "./modules/metrics/metrics.module";
import { SentryModule } from "./modules/sentry/sentry.module";
```

Add these modules to the imports array in @Module decorator. All three are @Global() so order doesn't matter, but place them near the top with other infrastructure modules.

SLOP-01: These modules are complete implementations (feature flags with Redis caching, Prometheus metrics, Sentry error tracking) - they just weren't imported.
</action>
<verify>
Run: `cd apps/backend && npx tsc --noEmit`
Confirm no TypeScript errors related to module imports.
</verify>
<done>FeatureFlagsModule, MetricsModule, and SentryModule are imported and registered in AppModule imports array.</done>
</task>

<task type="auto">
  <name>Task 2: Replace process.env.JWT_SECRET with ConfigService</name>
  <files>
apps/backend/src/modules/notifications/gateways/notification.gateway.ts
apps/backend/src/modules/projects/gateways/project.gateway.ts
  </files>
  <action>
In both gateway files:

1. Add ConfigService import if not present:

```typescript
import { ConfigService } from "@nestjs/config";
```

2. Inject ConfigService in constructor if not already injected.

3. Replace direct process.env.JWT_SECRET access with:

```typescript
this.configService.getOrThrow<string>("JWT_SECRET");
```

Use getOrThrow (not get) to fail fast if JWT_SECRET is missing - this is a required config.

PROD-03: Direct process.env access bypasses NestJS ConfigService validation and typing.
</action>
<verify>
Run: `cd apps/backend && grep -r "process\.env\.JWT_SECRET" src/modules/*/gateways/`
Expected: No matches (configuration.ts is acceptable, it reads env to provide to ConfigService).
</verify>
<done>Both gateway files use ConfigService.getOrThrow('JWT_SECRET') instead of direct process.env access.</done>
</task>

<task type="auto">
  <name>Task 3: Replace console.error and move faker</name>
  <files>
apps/backend/src/modules/storage/storage.module.ts
apps/backend/package.json
  </files>
  <action>
In storage.module.ts:

1. Import Logger from @nestjs/common if not present
2. Create a logger instance: `private readonly logger = new Logger(StorageModule.name);` or use a static logger for module-level logging
3. Replace console.error with: `this.logger.error('message', error.stack);`

For NestJS module-level logging (not injectable), use:

```typescript
const logger = new Logger("StorageModule");
logger.error("Failed to initialize LocalStorageProvider", err.stack);
```

In package.json (apps/backend/package.json):

Run these commands:

```bash
cd apps/backend
npm uninstall @faker-js/faker
npm install --save-dev @faker-js/faker
```

This moves faker from dependencies to devDependencies, reducing production bundle size by ~5MB.

PROD-04, PROD-05: Production logging and bundle optimization.
</action>
<verify>

1. Run: `cd apps/backend && grep -n "console.error" src/modules/storage/`
   Expected: No matches
2. Run: `cd apps/backend && npm ls @faker-js/faker`
   Expected: Shows as devDependency
   </verify>
   <done>console.error replaced with Logger in storage.module.ts, faker moved to devDependencies.</done>
   </task>

</tasks>

<verification>
- [ ] `cd apps/backend && npx tsc --noEmit` passes
- [ ] `cd apps/backend && npm run lint` passes
- [ ] `grep -r "FeatureFlagsModule" apps/backend/src/app.module.ts` returns match
- [ ] `grep -r "MetricsModule" apps/backend/src/app.module.ts` returns match
- [ ] `grep -r "SentryModule" apps/backend/src/app.module.ts` returns match
- [ ] Gateway files use ConfigService for JWT_SECRET
- [ ] storage.module.ts uses Logger
- [ ] faker is in devDependencies
</verification>

<success_criteria>

1. All 3 orphaned modules registered in AppModule (SLOP-01)
2. Gateway JWT_SECRET access uses ConfigService (PROD-03)
3. storage.module.ts uses NestJS Logger (PROD-05)
4. faker in devDependencies (PROD-04)
5. Backend compiles without errors
   </success_criteria>

<output>
After completion, create `.planning/phases/33-slop-cleanup-production-readiness/33-01-SUMMARY.md`
</output>
