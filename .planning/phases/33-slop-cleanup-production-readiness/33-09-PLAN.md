---
phase: 33-slop-cleanup-production-readiness
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/**/*.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All remaining 37 TODO comments (of 54 original) systematically triaged to complete SLOP-05"
    - "Auth-related TODOs use AUTH-TODO prefix or are resolved"
    - "Integration stub TODOs use STUB-TODO prefix or are resolved"
    - "Completed work TODOs removed"
    - "Only future enhancement TODOs remain"
  artifacts:
    - path: "apps/backend/src/modules/**/*.ts"
      provides: "Triaged TODO comments"
  key_links: []
---

<objective>
Complete systematic triage of remaining 37 TODO comments (SLOP-05 gap closure).

Purpose: Verification found 37 TODO comments remain after Plan 33-06 execution. The original plan expected to reduce from 54 to ~15-20, but the baseline may have been different. This plan completes the triage using the established AUTH-TODO and STUB-TODO prefix patterns.
Output: All TODOs either resolved, prefixed for tracking, or retained as valid future enhancements.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-slop-cleanup-production-readiness/33-VERIFICATION.md
@.planning/phases/33-slop-cleanup-production-readiness/33-06-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory and categorize all remaining TODOs</name>
  <files>apps/backend/src/modules/**/*.ts</files>
  <action>
First, generate a complete inventory of remaining TODOs:

```bash
cd apps/backend && grep -rn "TODO" src/modules/ --include="*.ts" | grep -v ".spec.ts"
```

For each TODO found, categorize into one of four types:

**Category 1: Auth-Related (~should already be marked AUTH-TODO from 33-06)**
Indicators: "guard", "auth", "context", "user ID", "JWT"
Action: Verify marked as `AUTH-TODO:` or resolve if guards now exist

**Category 2: Integration Stubs (~should already be marked STUB-TODO)**
Indicators: "implement", "integration", "external service", "API call"
Action: Verify marked as `STUB-TODO:` or remove if implemented

**Category 3: Completed Work**
Indicators: References work that's now done, old placeholders
Action: Remove entirely

**Category 4: Future Enhancements (KEEP)**
Indicators: "consider", "could", "future", "optional", "enhancement"
Action: Keep as-is with clear context

Document findings for Task 2 processing.
</action>
<verify>
Run: `cd apps/backend && grep -rn "TODO" src/modules/ --include="*.ts" | grep -v ".spec.ts" | wc -l`
Document the count and categorization.
</verify>
<done>All 37 TODOs inventoried and categorized by type.</done>
</task>

<task type="auto">
  <name>Task 2: Process each TODO according to category</name>
  <files>apps/backend/src/modules/**/*.ts</files>
  <action>
Process each TODO based on its category from Task 1:

**For Auth-Related TODOs:**
If guards ARE implemented on the controller:

- Replace TODO with actual guard application
- Use: `@UseGuards(JwtAuthGuard, TenantGuard)`
- Use: `@CurrentUser() user: User` for user context

If guards NOT yet implemented:

- Ensure marked as: `// AUTH-TODO: [description] - requires guard integration`

**For Integration Stub TODOs:**
If integration IS implemented (check imports/code):

- Remove the TODO entirely

If still a stub:

- Ensure marked as: `// STUB-TODO: [description] - requires [service] integration`

**For Completed Work TODOs:**

- Verify work is actually complete
- Remove the TODO comment entirely
- If work NOT complete, re-categorize

**For Future Enhancement TODOs:**

- Keep with clear, actionable description
- Ensure format: `// TODO: [what] - [why needed]`

After processing, verify the final count:

```bash
cd apps/backend && grep -rn "TODO" src/modules/ --include="*.ts" | grep -v ".spec.ts" | wc -l
```

Expected: ~10-20 remaining (future enhancements only)

SLOP-05 gap closure: Complete systematic triage.
</action>
<verify>

1. Run: `cd apps/backend && grep -rn "TODO.*auth\|TODO.*guard" src/modules/ | wc -l`
   Expected: 0 (all auth TODOs resolved or marked AUTH-TODO)
2. Run: `cd apps/backend && grep -rn "AUTH-TODO\|STUB-TODO" src/modules/ | wc -l`
   Document the tracked items count.
3. Run: `cd apps/backend && npx tsc --noEmit`
   Expected: No errors.
   </verify>
   <done>All TODOs triaged: auth marked, stubs documented, completed removed, future kept.</done>
   </task>

</tasks>

<verification>
- [ ] `cd apps/backend && npx tsc --noEmit` passes
- [ ] All auth-related TODOs either resolved or marked AUTH-TODO
- [ ] All stub TODOs either resolved or marked STUB-TODO
- [ ] Completed work TODOs removed
- [ ] Only future enhancement TODOs remain (expected: 10-20)
- [ ] SLOP-05 requirement satisfied: All 54 original TODOs triaged (17 by plan 06 + 37 remaining by this plan)
</verification>

<success_criteria>

1. Zero "TODO.*auth" or "TODO.*guard" patterns remain (converted to AUTH-TODO or resolved)
2. All integration stubs marked with STUB-TODO prefix or implemented
3. Completed work TODOs removed
4. Future enhancement TODOs retained with clear context
5. TypeScript compilation passes
6. SLOP-05 gap closed
   </success_criteria>

<output>
After completion, create `.planning/phases/33-slop-cleanup-production-readiness/33-09-SUMMARY.md`
</output>
