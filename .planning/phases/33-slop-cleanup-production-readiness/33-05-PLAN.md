---
phase: 33-slop-cleanup-production-readiness
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/src/modules/events/**/*.ts
  - apps/backend/src/modules/feature-flags/**/*.ts
  - apps/backend/src/modules/forms/**/*.ts
  - apps/backend/src/modules/health/**/*.ts
  - apps/backend/src/modules/help/**/*.ts
  - apps/backend/src/modules/hris/**/*.ts
  - apps/backend/src/modules/investigation-notes/**/*.ts
  - apps/backend/src/modules/investigations/**/*.ts
  - apps/backend/src/modules/jobs/**/*.ts
  - apps/backend/src/modules/messaging/**/*.ts
  - apps/backend/src/modules/metrics/**/*.ts
  - apps/backend/src/modules/notifications/**/*.ts
  - apps/backend/src/modules/operations/**/*.ts
  - apps/backend/src/modules/organization/**/*.ts
  - apps/backend/src/modules/persons/**/*.ts
  - apps/backend/src/modules/policies/**/*.ts
  - apps/backend/src/modules/portals/**/*.ts
  - apps/backend/src/modules/prisma/**/*.ts
  - apps/backend/src/modules/projects/**/*.ts
  - apps/backend/src/modules/remediation/**/*.ts
  - apps/backend/src/modules/reporting/**/*.ts
  - apps/backend/src/modules/rius/**/*.ts
  - apps/backend/src/modules/saved-views/**/*.ts
  - apps/backend/src/modules/search/**/*.ts
  - apps/backend/src/modules/sentry/**/*.ts
  - apps/backend/src/modules/storage/**/*.ts
  - apps/backend/src/modules/tables/**/*.ts
  - apps/backend/src/modules/users/**/*.ts
  - apps/backend/src/modules/workflow/**/*.ts
autonomous: true

must_haves:
  truths:
    - "No section separator comments remain in second-half module files"
    - "Code functionality unchanged after cleanup"
    - "All 820+ separator occurrences addressed across full codebase"
  artifacts:
    - path: "apps/backend/src/modules/**/*.ts"
      provides: "Clean code without separators"
  key_links: []
---

<objective>
Remove section-separator comments from second half of codebase (SLOP-04 batch 2).

Purpose: Complete the separator cleanup started in Plan 04. This covers modules from events/ through workflow/ (alphabetically second half) plus any remaining files.
Output: Entire backend codebase free of visual separator noise.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-slop-cleanup-production-readiness/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove separators from modules/events through modules/messaging</name>
  <files>
apps/backend/src/modules/events/**/*.ts
apps/backend/src/modules/feature-flags/**/*.ts
apps/backend/src/modules/forms/**/*.ts
apps/backend/src/modules/health/**/*.ts
apps/backend/src/modules/help/**/*.ts
apps/backend/src/modules/hris/**/*.ts
apps/backend/src/modules/investigation-notes/**/*.ts
apps/backend/src/modules/investigations/**/*.ts
apps/backend/src/modules/jobs/**/*.ts
apps/backend/src/modules/messaging/**/*.ts
  </files>
  <action>
Apply separator removal process:

1. Find lines matching `// ===` or `// ---` with 3+ characters
2. Delete pure separator lines (// ===== only)
3. Convert descriptive separators to simple comments (// ===== SECTION ===== -> // SECTION)

**Key files with separators (from research):**

- forms/forms.controller.ts (6 separators)
- investigations/investigations.service.ts (20+ separators)
- investigations/templates/\*.ts (multiple)
- investigation-notes/investigation-notes.service.ts (27 separators)
- investigation-notes/investigation-notes.module.ts (3 separators)

Process methodically through each directory.

SLOP-04: Continue systematic cleanup.
</action>
<verify>
Run: `cd apps/backend && grep -rn "// ===\|// ---" src/modules/events src/modules/feature-flags src/modules/forms src/modules/health src/modules/help src/modules/hris src/modules/investigation-notes src/modules/investigations src/modules/jobs src/modules/messaging | wc -l`
Expected: 0 or near-zero.
</verify>
<done>Separators removed from events through messaging modules.</done>
</task>

<task type="auto">
  <name>Task 2: Remove separators from modules/metrics through modules/projects</name>
  <files>
apps/backend/src/modules/metrics/**/*.ts
apps/backend/src/modules/notifications/**/*.ts
apps/backend/src/modules/operations/**/*.ts
apps/backend/src/modules/organization/**/*.ts
apps/backend/src/modules/persons/**/*.ts
apps/backend/src/modules/policies/**/*.ts
apps/backend/src/modules/portals/**/*.ts
apps/backend/src/modules/prisma/**/*.ts
apps/backend/src/modules/projects/**/*.ts
  </files>
  <action>
Apply same process to these directories.

**Key files with separators:**

- operations/training/certification.service.ts (2 separators per research)
- notifications/ (multiple services and controllers)
- policies/ (approval, associations, translations subdirs)
- portals/ (ethics, employee, operator subdirs)
- projects/ (services, gateways, listeners subdirs)

operations/ has many subdirectories - process each:

- client-health/
- client-success/
- hotline-ops/
- implementation/
- impersonation/
- support/
- training/

SLOP-04: Continue through middle directories.
</action>
<verify>
Run: `cd apps/backend && grep -rn "// ===\|// ---" src/modules/metrics src/modules/notifications src/modules/operations src/modules/organization src/modules/persons src/modules/policies src/modules/portals src/modules/prisma src/modules/projects | wc -l`
Expected: 0 or near-zero.
</verify>
<done>Separators removed from metrics through projects modules.</done>
</task>

<task type="auto">
  <name>Task 3: Remove separators from remaining modules and verify full cleanup</name>
  <files>
apps/backend/src/modules/remediation/**/*.ts
apps/backend/src/modules/reporting/**/*.ts
apps/backend/src/modules/rius/**/*.ts
apps/backend/src/modules/saved-views/**/*.ts
apps/backend/src/modules/search/**/*.ts
apps/backend/src/modules/sentry/**/*.ts
apps/backend/src/modules/storage/**/*.ts
apps/backend/src/modules/tables/**/*.ts
apps/backend/src/modules/users/**/*.ts
apps/backend/src/modules/workflow/**/*.ts
  </files>
  <action>
Apply same process to final directories.

**Key files with separators:**

- reporting/types/report.types.ts
- workflow/workflow.service.ts (2 separators)
- workflow/workflow.controller.ts (4 separators)
- storage/ (already partially cleaned in Plan 02)

After completing all modules, run comprehensive verification:

```bash
# Count remaining separators in entire backend
cd apps/backend && grep -rn "// ===\|// ---" src/ | wc -l

# List any remaining files (should be 0 or very few)
cd apps/backend && grep -rl "// ===\|// ---" src/
```

If any files remain, clean them to complete SLOP-04.

Run full TypeScript and lint checks:

```bash
cd apps/backend && npx tsc --noEmit
cd apps/backend && npm run lint
```

SLOP-04: Complete cleanup and verify.
</action>
<verify>

1. Run: `cd apps/backend && grep -rn "// ===\|// ---" src/ | wc -l`
   Expected: 0 or very low (only if descriptive headers deliberately kept).
2. Run: `cd apps/backend && npx tsc --noEmit`
3. Run: `cd apps/backend && npm run lint`
   </verify>
   <done>All separator comments removed from entire backend codebase, full verification passes.</done>
   </task>

</tasks>

<verification>
- [ ] `cd apps/backend && npx tsc --noEmit` passes
- [ ] `cd apps/backend && npm run lint` passes
- [ ] `grep -rn "// ===\|// ---" src/` returns 0 or near-zero results
- [ ] No functional changes to code behavior
- [ ] Original 820+ separators addressed
</verification>

<success_criteria>

1. All pure separator lines removed from entire codebase
2. Descriptive headers converted to simple comments where valuable
3. TypeScript compilation passes
4. Lint passes
5. SLOP-04 requirement fully satisfied
   </success_criteria>

<output>
After completion, create `.planning/phases/33-slop-cleanup-production-readiness/33-05-SUMMARY.md`
</output>
