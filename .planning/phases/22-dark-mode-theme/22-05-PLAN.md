---
phase: 22-dark-mode-theme
plan: 05
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified:
  - apps/frontend/src/components/dashboard/stats-cards.tsx
  - apps/frontend/src/components/dashboard/recent-cases.tsx
  - apps/frontend/src/components/dashboard/my-assignments.tsx
  - apps/frontend/src/components/dashboard/quick-actions.tsx
  - apps/frontend/src/components/dashboard/my-tasks.tsx
  - apps/frontend/src/app/(authenticated)/dashboard/page.tsx
  - apps/frontend/src/components/cases/case-detail-header.tsx
  - apps/frontend/src/components/cases/case-detail-layout.tsx
  - apps/frontend/src/components/cases/case-header.tsx
  - apps/frontend/src/components/cases/case-tabs.tsx
  - apps/frontend/src/components/cases/case-list-filters.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page renders correctly in dark mode with stat cards, recent cases, and tasks"
    - "Case detail layout (header, tabs) renders correctly in dark mode"
    - "Case list page with filters renders correctly in dark mode"
    - "Status and severity indicators use centralized theme-colors utility"
  artifacts:
    - path: "apps/frontend/src/components/dashboard/stats-cards.tsx"
      provides: "Dark-mode-compatible dashboard stat cards"
    - path: "apps/frontend/src/components/cases/case-detail-layout.tsx"
      provides: "Dark-mode-compatible case detail layout"
  key_links:
    - from: "case-detail-header.tsx"
      to: "lib/theme-colors.ts"
      via: "statusColors or getStatusColor import"
      pattern: "theme-colors"
---

<objective>
Theme the dashboard components and case management layout/header/tabs — the two highest-traffic areas. Dashboard is the landing page; case detail is where users spend 80% of their time.

Purpose: These components are seen by every user on every session. Getting them right maximizes dark mode's visual impact.
Output: Dashboard and case layout/header/tabs fully dark-mode-compatible.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-dark-mode-theme/22-RESEARCH.md
@apps/frontend/src/lib/theme-colors.ts
@apps/frontend/src/components/dashboard/stats-cards.tsx
@apps/frontend/src/components/cases/case-detail-layout.tsx
@apps/frontend/src/components/cases/case-detail-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme dashboard components</name>
  <files>
    apps/frontend/src/components/dashboard/stats-cards.tsx
    apps/frontend/src/components/dashboard/recent-cases.tsx
    apps/frontend/src/components/dashboard/my-assignments.tsx
    apps/frontend/src/components/dashboard/quick-actions.tsx
    apps/frontend/src/components/dashboard/my-tasks.tsx
    apps/frontend/src/app/(authenticated)/dashboard/page.tsx
  </files>
  <action>
**Dashboard page (`dashboard/page.tsx`):**
- Replace `bg-gray-50` / `bg-white` backgrounds with `bg-background`
- Replace `text-gray-*` headings/text with `text-foreground` / `text-muted-foreground`

**Stats Cards (`stats-cards.tsx`):**
- ~20 hardcoded color occurrences
- Card backgrounds: `bg-white` → `bg-card`
- Stat labels: `text-gray-500` → `text-muted-foreground`
- Icon backgrounds: `bg-blue-50` → `bg-blue-50 dark:bg-blue-900/20`, etc. Use `semanticBgColors` from theme-colors.ts where applicable
- Trend indicators: `text-green-600` → `text-green-600 dark:text-green-400`, `text-red-600` → `text-red-600 dark:text-red-400`
- Stat values: `text-gray-900` → `text-foreground`

**Recent Cases (`recent-cases.tsx`):**
- Table background: `bg-white` → `bg-card`
- Headers: `text-gray-500` → `text-muted-foreground`
- Row text: `text-gray-900` → `text-foreground`, `text-gray-500` → `text-muted-foreground`
- Row hover: `hover:bg-gray-50` → `hover:bg-muted/50`
- Status colors: use `getStatusColor()` from theme-colors.ts

**My Assignments (`my-assignments.tsx`):**
- Same patterns as recent cases
- Card backgrounds, text colors, hover states
- Priority/severity indicators: use `getSeverityColor()` from theme-colors.ts

**Quick Actions (`quick-actions.tsx`):**
- Button backgrounds: `bg-gray-50` → `bg-muted`
- Icon colors: use `semanticTextColors` from theme-colors.ts
- Hover states: `hover:bg-gray-100` → `hover:bg-muted`

**My Tasks (`my-tasks.tsx`):**
- Task item backgrounds and text
- Due date indicators: overdue red, upcoming amber
- Use `semanticColors` for task status indicators
  </action>
  <verify>
  - `cd apps/frontend && npx tsc --noEmit` should compile without errors
  - Dashboard renders with correct colors in both light and dark mode
  </verify>
  <done>All 5 dashboard components + dashboard page render correctly in dark mode. Stat cards, case lists, assignments, tasks, and quick actions all themed.</done>
</task>

<task type="auto">
  <name>Task 2: Theme case layout, header, tabs, and list filters</name>
  <files>
    apps/frontend/src/components/cases/case-detail-header.tsx
    apps/frontend/src/components/cases/case-detail-layout.tsx
    apps/frontend/src/components/cases/case-header.tsx
    apps/frontend/src/components/cases/case-tabs.tsx
    apps/frontend/src/components/cases/case-list-filters.tsx
  </files>
  <action>
**Case Detail Layout (`case-detail-layout.tsx` — ~24 hardcoded colors):**
- Page background: `bg-gray-50` → `bg-background`
- Panel backgrounds: `bg-white` → `bg-card`
- Borders: `border-gray-200` → `border-border`
- Text colors: `text-gray-*` → semantic tokens
- Section headers: `text-gray-900` → `text-foreground`
- Sidebar background: `bg-white` → `bg-card`

**Case Detail Header (`case-detail-header.tsx` — ~33 hardcoded colors):**
- Header background: `bg-white` → `bg-card`
- Case number: `text-gray-500` → `text-muted-foreground`
- Case title: `text-gray-900` → `text-foreground`
- Status badge: use `getStatusColor()` from theme-colors.ts
- Severity badge: use `getSeverityColor()` from theme-colors.ts
- Assignee info: `text-gray-600` → `text-muted-foreground`
- Action buttons: check for hardcoded `bg-gray-*` hover states
- Info chips (category, source, etc.): `bg-gray-100 text-gray-700` → `bg-muted text-muted-foreground`

**Case Header (`case-header.tsx`):**
- Same patterns as detail header — replace hardcoded colors with semantic tokens
- Use theme-colors.ts for status/severity indicators

**Case Tabs (`case-tabs.tsx` — ~22 hardcoded colors):**
- Tab background: `bg-white` → `bg-card`
- Active tab: check indicator color (should use `bg-primary` or `border-primary`)
- Inactive tabs: `text-gray-500` → `text-muted-foreground`
- Tab content area: `bg-white` → `bg-card`
- Borders between tabs: `border-gray-200` → `border-border`

**Case List Filters (`case-list-filters.tsx`):**
- Filter bar background: `bg-white` / `bg-gray-50` → `bg-card` / `bg-muted`
- Filter chips: `bg-gray-100 text-gray-700` → `bg-muted text-foreground`
- Active filters: check highlight colors
- Clear button styling
  </action>
  <verify>
  - `cd apps/frontend && npx tsc --noEmit` should compile without errors
  - Case detail page renders with correct dark mode colors
  - Case list page filters visible and usable in dark mode
  </verify>
  <done>Case layout, headers, tabs, and filters all render correctly in dark mode. Status/severity badges use centralized theme-colors utility.</done>
</task>

</tasks>

<verification>
- `cd apps/frontend && npx tsc --noEmit` passes
- Dashboard page looks correct in dark mode
- Case detail page (header, tabs, layout) looks correct in dark mode
- Case list page with filters looks correct in dark mode
</verification>

<success_criteria>
- Dashboard and case management — the two highest-traffic areas — render correctly in dark mode
- All status/severity indicators use centralized theme-colors
</success_criteria>

<output>
After completion, create `.planning/phases/22-dark-mode-theme/22-05-SUMMARY.md`
</output>
