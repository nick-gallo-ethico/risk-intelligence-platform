---
phase: 31-code-quality-performance
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/rius/services/riu-update.service.ts
  - apps/backend/src/modules/rius/rius.service.ts
  - apps/backend/src/modules/rius/rius.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "RiusService is a thin coordinator under 300 LOC"
    - "Update logic (update, updateStatus, updateAiEnrichment, updateLanguage) extracted to RiuUpdateService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/rius/services/riu-update.service.ts"
      provides: "RIU update operations with immutability enforcement"
      min_lines: 120
    - path: "apps/backend/src/modules/rius/rius.service.ts"
      provides: "Thin coordinator delegating to sub-services"
      max_lines: 300
  key_links:
    - from: "apps/backend/src/modules/rius/rius.service.ts"
      to: "apps/backend/src/modules/rius/services/riu-update.service.ts"
      via: "constructor injection"
      pattern: "private.*riuUpdateService"
---

<objective>
Extract update logic from rius.service.ts (460 LOC) to achieve the thin coordinator pattern target of under 300 LOC.

Purpose: While rius.service.ts already delegates query operations, it still contains 150+ LOC of update logic (update, updateStatus, updateAiEnrichment, updateLanguage) with immutability enforcement and activity logging. Extract this to RiuUpdateService to complete the decomposition.

Output: RiuUpdateService and refactored thin RiusService coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/rius/rius.service.ts
@apps/backend/src/modules/rius/rius.module.ts
@apps/backend/src/modules/rius/services/riu-query.service.ts (reference pattern)
@apps/backend/src/modules/rius/types/riu.types.ts (immutable fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract RiuUpdateService</name>
  <files>
    apps/backend/src/modules/rius/services/riu-update.service.ts
  </files>
  <action>
Create RiuUpdateService in apps/backend/src/modules/rius/services/riu-update.service.ts.

Extract these methods from rius.service.ts:

- `update(id, dto, userId, organizationId)` - General update with immutability enforcement
- `updateStatus(id, newStatus, userId, organizationId)` - Status change (delegates to update)
- `updateAiEnrichment(id, enrichment, organizationId)` - AI field updates
- `updateLanguage(id, languageConfirmed, userId, organizationId)` - Language updates

Inject:

- PrismaService
- ActivityService
- EventEmitter2
- RiuQueryService (for findOne lookups)
- Logger

Key logic to preserve:

- Immutability enforcement via getImmutableFieldsInObject from riu.types.ts
- Activity logging for status_changed, language_updated
- Event emission for riu.status.changed, riu.ai.enriched
- Language effective computation (confirmed > detected > 'en')

Use the same emitEvent helper pattern as the original service.
</action>
<verify>

- File exists at apps/backend/src/modules/rius/services/riu-update.service.ts
- Service class exports RiuUpdateService with @Injectable()
- All 4 methods present with correct signatures
- Immutability check imported from riu.types.ts
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  RiuUpdateService contains all update-related methods extracted from RiusService with preserved signatures and immutability enforcement.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Refactor RiusService to thin coordinator</name>
  <files>
    apps/backend/src/modules/rius/rius.service.ts
    apps/backend/src/modules/rius/rius.module.ts
  </files>
  <action>
**Step 1: Refactor RiusService**

Update apps/backend/src/modules/rius/rius.service.ts:

1. Remove extracted update methods (update, updateStatus, updateAiEnrichment, updateLanguage)
2. Inject RiuUpdateService
3. Delegate update operations to riuUpdateService:
   - update -> riuUpdateService.update
   - updateStatus -> riuUpdateService.updateStatus
   - updateAiEnrichment -> riuUpdateService.updateAiEnrichment
   - updateLanguage -> riuUpdateService.updateLanguage

4. Keep in RiusService:
   - create() - RIU creation with reference number generation
   - createExtension() - Type-specific extension dispatch
   - Query delegations (already done)
   - generateReferenceNumber() - Reference number logic
   - emitEvent() - Event emission helper (used by create)

Target: RiusService under 300 LOC as thin coordinator.

**Step 2: Update rius.module.ts**

Add RiuUpdateService to providers array.
</action>
<verify>

- RiusService is under 300 LOC: `wc -l apps/backend/src/modules/rius/rius.service.ts`
- RiusService injects and delegates to RiuUpdateService
- rius.module.ts includes RiuUpdateService in providers
- npm run lint passes
- npm run typecheck passes
- npm run test -- --testPathPattern="rius" passes (existing tests still work)
  </verify>
  <done>
  RiusService is a thin coordinator under 300 LOC that delegates query operations to RiuQueryService, form data to RiuFormDataService, and update operations to RiuUpdateService.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/rius/rius.service.ts
# Should be < 300

# Verify new service exists

ls apps/backend/src/modules/rius/services/

# Verify delegation

grep "riuUpdateService" apps/backend/src/modules/rius/rius.service.ts

# Verify types

npm run typecheck

# Verify lint

npm run lint

# Verify tests pass

npm run test -- --testPathPattern="rius"

```
</verification>

<success_criteria>
- RiusService is under 300 LOC (down from 460)
- RiuUpdateService extracts ~160 LOC of update logic
- All existing public API signatures preserved
- All tests pass
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-11-SUMMARY.md`
</output>
```
