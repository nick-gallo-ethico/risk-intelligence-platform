---
phase: 31-code-quality-performance
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts
  - apps/backend/src/modules/analytics/dashboard/services/widget-campaign-data.service.ts
  - apps/backend/src/modules/analytics/dashboard/services/widget-metrics-data.service.ts
  - apps/backend/src/modules/analytics/dashboard/widget-data.service.ts
  - apps/backend/src/modules/analytics/dashboard/dashboard.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "WidgetDataService is a thin coordinator under 400 LOC"
    - "Case widget data methods extracted to WidgetCaseDataService"
    - "Campaign/disclosure widget data methods extracted to WidgetCampaignDataService"
    - "Metrics/SLA/activity widget data methods extracted to WidgetMetricsDataService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts"
      provides: "Case-related widget data fetching"
      min_lines: 250
    - path: "apps/backend/src/modules/analytics/dashboard/services/widget-campaign-data.service.ts"
      provides: "Campaign/disclosure widget data fetching"
      min_lines: 200
    - path: "apps/backend/src/modules/analytics/dashboard/services/widget-metrics-data.service.ts"
      provides: "Metrics/SLA/activity widget data fetching"
      min_lines: 150
    - path: "apps/backend/src/modules/analytics/dashboard/widget-data.service.ts"
      provides: "Thin coordinator delegating to sub-services"
      max_lines: 400
  key_links:
    - from: "apps/backend/src/modules/analytics/dashboard/widget-data.service.ts"
      to: "apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts"
      via: "constructor injection"
      pattern: "private.*widgetCaseDataService"
    - from: "apps/backend/src/modules/analytics/dashboard/widget-data.service.ts"
      to: "apps/backend/src/modules/analytics/dashboard/services/widget-campaign-data.service.ts"
      via: "constructor injection"
      pattern: "private.*widgetCampaignDataService"
---

<objective>
Decompose widget-data.service.ts (1240 LOC) into focused sub-services following the thin coordinator pattern.

Purpose: widget-data.service.ts is the largest service in the codebase. It contains data fetching logic for 11+ data sources (cases, rius, campaigns, disclosures, investigations, SLA, etc.). Extract into domain-specific sub-services, leaving WidgetDataService as a thin coordinator that routes to the appropriate sub-service.

Output: WidgetCaseDataService, WidgetCampaignDataService, WidgetMetricsDataService, and refactored thin coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/analytics/dashboard/widget-data.service.ts
@apps/backend/src/modules/analytics/dashboard/dashboard.module.ts
@apps/backend/src/modules/cases/services/case-query.service.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract WidgetCaseDataService</name>
  <files>
    apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts
  </files>
  <action>
Create WidgetCaseDataService in apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts.

Extract these methods from widget-data.service.ts:

**Case data methods:**

- `fetchCaseData(organizationId, userId, widget, queryConfig, dateRange)` - Routes to KPI/Table/List/Chart
- `fetchMyCases(organizationId, userId, widget, queryConfig, dateRange)` - User-assigned cases
- `fetchCaseKpi(organizationId, queryConfig, dateRange)` - Case count KPI
- `fetchCaseTable(organizationId, queryConfig, dateRange)` - Case list table
- `fetchCaseList(organizationId, queryConfig, dateRange)` - Case list
- `fetchCaseChart(organizationId, widget, queryConfig, dateRange)` - Case chart data
- `buildCaseWhereClause(organizationId, filters, dateRange)` - Prisma where builder

**RIU data methods:**

- `fetchRiuData(organizationId, widget, queryConfig, dateRange)` - RIU widget data
- `fetchRiuKpi(organizationId, queryConfig, dateRange)` - RIU count KPI
- `fetchRiuChart(organizationId, widget, queryConfig, dateRange)` - RIU chart data
- `buildRiuWhereClause(organizationId, filters, dateRange)` - Prisma where builder

**Investigation data methods:**

- `fetchInvestigationData(organizationId, userId, dateRange)` - Investigation widget

Inject:

- PrismaService
- CACHE_MANAGER (Cache)

Keep all Prisma query logic, date range handling, and KPI trend calculations intact.
</action>
<verify>

- File exists at apps/backend/src/modules/analytics/dashboard/services/widget-case-data.service.ts
- Service class exports WidgetCaseDataService with @Injectable()
- All case/RIU/investigation methods present with correct signatures
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  WidgetCaseDataService contains all case, RIU, and investigation widget data methods.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract WidgetCampaignDataService and WidgetMetricsDataService</name>
  <files>
    apps/backend/src/modules/analytics/dashboard/services/widget-campaign-data.service.ts
    apps/backend/src/modules/analytics/dashboard/services/widget-metrics-data.service.ts
  </files>
  <action>
**Step 1: Create WidgetCampaignDataService**

Create apps/backend/src/modules/analytics/dashboard/services/widget-campaign-data.service.ts.

Extract these methods from widget-data.service.ts:

- `fetchCampaignData(organizationId, widget, queryConfig, dateRange)` - Campaign widget
- `fetchCampaignKpi(organizationId, queryConfig, dateRange)` - Campaign count KPI
- `fetchCampaignChart(organizationId, widget, queryConfig, dateRange)` - Campaign chart
- `fetchCampaignAssignmentData(organizationId, widget, queryConfig, dateRange)` - Assignments
- `fetchDisclosureData(organizationId, widget, queryConfig, dateRange)` - Disclosures
- `buildCampaignWhereClause(organizationId, filters, dateRange)` - Where builder

**Step 2: Create WidgetMetricsDataService**

Create apps/backend/src/modules/analytics/dashboard/services/widget-metrics-data.service.ts.

Extract these methods:

- `computeComplianceHealth(organizationId, dateRange)` - Compliance health score
- `fetchSlaMetrics(organizationId, userId, queryConfig, dateRange)` - SLA metrics
- `fetchActivityData(organizationId, queryConfig, dateRange)` - Activity feed
- `getQuickActions(widget)` - Quick action buttons config

Inject PrismaService and CACHE_MANAGER in both services.
</action>
<verify>

- widget-campaign-data.service.ts exists with campaign/disclosure methods
- widget-metrics-data.service.ts exists with compliance/SLA/activity methods
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  WidgetCampaignDataService and WidgetMetricsDataService created with domain-specific methods.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Refactor WidgetDataService to thin coordinator</name>
  <files>
    apps/backend/src/modules/analytics/dashboard/widget-data.service.ts
    apps/backend/src/modules/analytics/dashboard/dashboard.module.ts
  </files>
  <action>
**Step 1: Refactor WidgetDataService**

Update apps/backend/src/modules/analytics/dashboard/widget-data.service.ts:

1. Remove all extracted methods (keep only routing and caching logic)
2. Inject the three new sub-services:
   - WidgetCaseDataService
   - WidgetCampaignDataService
   - WidgetMetricsDataService

3. Update fetchWidgetData() to delegate to sub-services:

   ```typescript
   switch (dataSource) {
     case 'cases':
     case 'my_cases':
     case 'rius':
     case 'investigations':
       return this.widgetCaseDataService.fetch(dataSource, ...);
     case 'campaigns':
     case 'campaign_assignments':
     case 'disclosures':
       return this.widgetCampaignDataService.fetch(dataSource, ...);
     case 'compliance_health':
     case 'sla_metrics':
     case 'activity':
     case 'actions':
       return this.widgetMetricsDataService.fetch(dataSource, ...);
   }
   ```

4. Keep in WidgetDataService:
   - `getWidgetData()` - Public entry point with caching
   - `getBatchWidgetData()` - Parallel batch fetch
   - `invalidateWidget()` - Cache invalidation
   - `fetchWidgetData()` - Router to sub-services
   - `buildCacheKey()` - Cache key generation
   - `getCacheTtl()` - TTL lookup

Target: WidgetDataService under 400 LOC as thin coordinator.

**Step 2: Update dashboard.module.ts**

Add all three new services to providers array.
</action>
<verify>

- WidgetDataService is under 400 LOC: `wc -l apps/backend/src/modules/analytics/dashboard/widget-data.service.ts`
- WidgetDataService injects all three sub-services
- dashboard.module.ts includes all new services in providers
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  WidgetDataService is a thin coordinator (~300-400 LOC) that delegates to domain-specific sub-services.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/analytics/dashboard/widget-data.service.ts
# Should be < 400

# Verify new services exist

ls apps/backend/src/modules/analytics/dashboard/services/

# Verify types

npm run typecheck

# Verify lint

npm run lint

```
</verification>

<success_criteria>
- WidgetDataService is under 400 LOC (down from 1240)
- WidgetCaseDataService extracts ~400 LOC of case/RIU/investigation logic
- WidgetCampaignDataService extracts ~300 LOC of campaign/disclosure logic
- WidgetMetricsDataService extracts ~200 LOC of metrics/SLA/activity logic
- All existing public API signatures preserved
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-13-SUMMARY.md`
</output>
```
