---
phase: 31-code-quality-performance
plan: 17
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/src/modules/campaigns/services/audience-query.service.ts
  - apps/backend/src/modules/campaigns/services/audience-description.service.ts
  - apps/backend/src/modules/campaigns/campaign-targeting.service.ts
  - apps/backend/src/modules/campaigns/campaigns.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CampaignTargetingService is a thin coordinator under 400 LOC"
    - "Audience query building extracted to AudienceQueryService"
    - "Human-readable description generation extracted to AudienceDescriptionService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/campaigns/services/audience-query.service.ts"
      provides: "Prisma where clause building for audience targeting"
      min_lines: 300
    - path: "apps/backend/src/modules/campaigns/services/audience-description.service.ts"
      provides: "Human-readable criteria descriptions"
      min_lines: 150
    - path: "apps/backend/src/modules/campaigns/campaign-targeting.service.ts"
      provides: "Thin coordinator for audience preview and targeting"
      max_lines: 400
  key_links:
    - from: "apps/backend/src/modules/campaigns/campaign-targeting.service.ts"
      to: "apps/backend/src/modules/campaigns/services/audience-query.service.ts"
      via: "constructor injection"
      pattern: "private.*audienceQueryService"
---

<objective>
Decompose campaign-targeting.service.ts (1007 LOC) into focused sub-services following the thin coordinator pattern.

Purpose: campaign-targeting.service.ts handles campaign audience targeting with simple/advanced modes, preview generation, and human-readable descriptions. Extract query building and description generation into focused services.

Output: AudienceQueryService, AudienceDescriptionService, and refactored thin coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/campaigns/campaign-targeting.service.ts
@apps/backend/src/modules/campaigns/campaigns.module.ts
@apps/backend/src/modules/cases/services/case-query.service.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AudienceQueryService</name>
  <files>
    apps/backend/src/modules/campaigns/services/audience-query.service.ts
  </files>
  <action>
Create AudienceQueryService in apps/backend/src/modules/campaigns/services/audience-query.service.ts.

Extract these methods from campaign-targeting.service.ts:

**Where clause building:**

- `buildWhereClause(criteria, organizationId)` - Main where builder
- `buildSimpleWhere(simple, organizationId)` - Simple mode where
- `buildAdvancedWhere(advanced, organizationId)` - Advanced mode where
- `buildSegmentWhere(segment, organizationId)` - Segment criteria where

**Department/location filters:**

- `buildDepartmentFilter(departmentIds)` - Department IN clause
- `buildLocationFilter(locationIds)` - Location IN clause
- `buildBusinessUnitFilter(businessUnitIds)` - Business unit filter

**Advanced filters:**

- `buildTenureFilter(minDays, maxDays)` - Tenure range filter
- `buildHierarchyDepthFilter(depth)` - Manager level filter
- `buildJobTitleFilter(titles)` - Job title IN clause
- `buildSubordinateFilter(managerId, organizationId)` - Walk org hierarchy

**Custom attribute filters:**

- `buildCustomAttributeFilter(attribute, operator, value)` - HRIS attribute filter
- `resolveAttributePath(attribute)` - Map attribute to Prisma field

Inject:

- PrismaService
- SegmentQueryBuilder (existing)
- Logger
  </action>
  <verify>
- File exists at apps/backend/src/modules/campaigns/services/audience-query.service.ts
- Service class exports AudienceQueryService with @Injectable()
- All where clause building methods present
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  AudienceQueryService handles all Prisma where clause building for audience targeting.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract AudienceDescriptionService</name>
  <files>
    apps/backend/src/modules/campaigns/services/audience-description.service.ts
  </files>
  <action>
Create AudienceDescriptionService in apps/backend/src/modules/campaigns/services/audience-description.service.ts.

Extract these methods from campaign-targeting.service.ts:

**Description building:**

- `buildCriteriaDescription(criteria, organizationId)` - Main description builder
- `describeSimpleCriteria(simple, organizationId)` - Simple mode description
- `describeAdvancedCriteria(advanced)` - Advanced mode description
- `describeSegmentCriteria(segment)` - Segment description

**Component descriptions:**

- `describeDepartmentFilter(departmentIds, organizationId)` - "Finance, HR, Legal"
- `describeLocationFilter(locationIds, organizationId)` - "US, UK, Germany"
- `describeTenureFilter(minDays, maxDays)` - "90+ days tenure"
- `describeHierarchyFilter(depth)` - "Managers and above"
- `describeJobTitleFilter(titles)` - "Directors, VPs"

**Name resolution:**

- `resolveDepartmentNames(departmentIds, organizationId)` - Lookup department names
- `resolveLocationNames(locationIds, organizationId)` - Lookup location names
- `resolveBusinessUnitNames(businessUnitIds, organizationId)` - Lookup BU names

Inject:

- PrismaService
- Logger
  </action>
  <verify>
- File exists at apps/backend/src/modules/campaigns/services/audience-description.service.ts
- Service class exports AudienceDescriptionService with @Injectable()
- All description methods present
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  AudienceDescriptionService handles human-readable description generation.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Refactor CampaignTargetingService to thin coordinator</name>
  <files>
    apps/backend/src/modules/campaigns/campaign-targeting.service.ts
    apps/backend/src/modules/campaigns/campaigns.module.ts
  </files>
  <action>
**Step 1: Refactor CampaignTargetingService**

Update apps/backend/src/modules/campaigns/campaign-targeting.service.ts:

1. Remove all extracted methods
2. Inject the two new sub-services:
   - AudienceQueryService
   - AudienceDescriptionService

3. Refactor `previewAudience()` to delegate:

   ```typescript
   async previewAudience(criteria, organizationId, options): Promise<AudiencePreviewDto> {
     // Build where clause via sub-service
     const where = await this.audienceQueryService.buildWhereClause(criteria, organizationId);

     // Execute query
     const [totalCount, employees] = await Promise.all([
       this.prisma.employee.count({ where }),
       this.prisma.employee.findMany({ where, ...pagination }),
     ]);

     // Build description via sub-service
     const criteriaDescription = await this.audienceDescriptionService.buildCriteriaDescription(criteria, organizationId);

     return { totalCount, employees, criteriaDescription, ... };
   }
   ```

4. Keep in CampaignTargetingService:
   - `previewAudience()` - Main preview entry point
   - `validateCriteria()` - Criteria validation
   - `getTargetingAttributes()` - HRIS attribute discovery
   - `estimateAudienceSize()` - Quick count without preview

Target: CampaignTargetingService under 400 LOC as thin coordinator.

**Step 2: Update campaigns.module.ts**

Add both new services to providers array.
</action>
<verify>

- CampaignTargetingService is under 400 LOC: `wc -l apps/backend/src/modules/campaigns/campaign-targeting.service.ts`
- CampaignTargetingService injects both sub-services
- campaigns.module.ts includes both new services in providers
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  CampaignTargetingService is a thin coordinator (~300-400 LOC) that delegates query building and description generation.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/campaigns/campaign-targeting.service.ts
# Should be < 400

# Verify new services exist

ls apps/backend/src/modules/campaigns/services/

# Verify types

npm run typecheck

# Verify lint

npm run lint

```
</verification>

<success_criteria>
- CampaignTargetingService is under 400 LOC (down from 1007)
- AudienceQueryService extracts ~400 LOC of where clause building
- AudienceDescriptionService extracts ~250 LOC of description generation
- All existing public API signatures preserved
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-17-SUMMARY.md`
</output>
```
