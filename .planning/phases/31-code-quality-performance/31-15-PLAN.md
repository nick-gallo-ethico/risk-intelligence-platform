---
phase: 31-code-quality-performance
plan: 15
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts
  - apps/backend/src/modules/analytics/migration/services/migration-validator.service.ts
  - apps/backend/src/modules/analytics/migration/services/migration-executor.service.ts
  - apps/backend/src/modules/analytics/migration/migration.service.ts
  - apps/backend/src/modules/analytics/migration/migration.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "MigrationService is a thin coordinator under 400 LOC"
    - "File parsing and format detection extracted to MigrationParserService"
    - "Validation and preview logic extracted to MigrationValidatorService"
    - "Import execution and rollback extracted to MigrationExecutorService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts"
      provides: "File parsing, format detection, field mapping"
      min_lines: 250
    - path: "apps/backend/src/modules/analytics/migration/services/migration-validator.service.ts"
      provides: "Row validation, error collection, preview generation"
      min_lines: 200
    - path: "apps/backend/src/modules/analytics/migration/services/migration-executor.service.ts"
      provides: "Import execution, rollback logic"
      min_lines: 200
    - path: "apps/backend/src/modules/analytics/migration/migration.service.ts"
      provides: "Thin coordinator managing migration workflow"
      max_lines: 400
  key_links:
    - from: "apps/backend/src/modules/analytics/migration/migration.service.ts"
      to: "apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts"
      via: "constructor injection"
      pattern: "private.*migrationParserService"
---

<objective>
Decompose migration.service.ts (1159 LOC) into focused sub-services following the thin coordinator pattern.

Purpose: migration.service.ts handles data import from competitor systems (NAVEX, EQS, CSV). It contains parsing, validation, import execution, and rollback logic. Extract into pipeline-stage services that can be tested and maintained independently.

Output: MigrationParserService, MigrationValidatorService, MigrationExecutorService, and refactored thin coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/analytics/migration/migration.service.ts
@apps/backend/src/modules/analytics/migration/migration.module.ts
@apps/backend/src/modules/cases/services/case-query.service.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MigrationParserService</name>
  <files>
    apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts
  </files>
  <action>
Create MigrationParserService in apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts.

Extract these methods from migration.service.ts:

**Format detection methods:**

- `detectFormat(organizationId, jobId, fileContent)` - Auto-detect file format
- `detectSourceType(headers, sampleRows)` - Identify NAVEX/EQS/CSV
- `detectDelimiter(content)` - CSV delimiter detection
- `detectEncoding(buffer)` - File encoding detection

**Field mapping methods:**

- `generateFieldSuggestions(sourceHeaders, targetEntity)` - AI-assisted field mapping
- `applyFieldMapping(row, mappings)` - Transform row using mappings
- `validateMapping(mappings, targetEntity)` - Validate mapping completeness
- `saveFieldMappings(organizationId, jobId, mappings)` - Persist mappings

**Parsing methods:**

- `parseRow(row, mappings, transforms)` - Parse single row
- `applyTransform(value, transform)` - Apply transform function
- `normalizeValue(value, targetType)` - Type normalization

Inject:

- PrismaService
- Logger
  </action>
  <verify>
- File exists at apps/backend/src/modules/analytics/migration/services/migration-parser.service.ts
- Service class exports MigrationParserService with @Injectable()
- All parsing and mapping methods present
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  MigrationParserService handles file parsing, format detection, and field mapping.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract MigrationValidatorService and MigrationExecutorService</name>
  <files>
    apps/backend/src/modules/analytics/migration/services/migration-validator.service.ts
    apps/backend/src/modules/analytics/migration/services/migration-executor.service.ts
  </files>
  <action>
**Step 1: Create MigrationValidatorService**

Create apps/backend/src/modules/analytics/migration/services/migration-validator.service.ts.

Extract these methods from migration.service.ts:

- `validateJob(organizationId, jobId, data)` - Full validation pass
- `validateRow(row, rowIndex, targetEntity)` - Single row validation
- `validateRequiredFields(row, targetEntity)` - Required field check
- `validateFieldTypes(row, targetEntity)` - Type validation
- `validateForeignKeys(row, targetEntity, orgId)` - Reference validation
- `collectErrors(errors)` - Error aggregation
- `getPreview(organizationId, jobId)` - Generate preview rows

Inject:

- PrismaService
- Logger

**Step 2: Create MigrationExecutorService**

Create apps/backend/src/modules/analytics/migration/services/migration-executor.service.ts.

Extract these methods:

- `executeImport(organizationId, jobId, userId)` - Run import
- `importBatch(batch, jobId, targetEntity)` - Batch insert
- `createMigrationRecord(jobId, entityType, entityId, sourceData)` - Track imported records
- `checkRollbackEligibility(organizationId, jobId)` - Check rollback window
- `executeRollback(organizationId, jobId, userId)` - Rollback import
- `deleteMigratedRecords(jobId)` - Delete imported records

Inject:

- PrismaService
- Logger
- EventEmitter2 (for progress events)
  </action>
  <verify>
- migration-validator.service.ts exists with validation methods
- migration-executor.service.ts exists with import/rollback methods
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  MigrationValidatorService handles validation, MigrationExecutorService handles import/rollback.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Refactor MigrationService to thin coordinator</name>
  <files>
    apps/backend/src/modules/analytics/migration/migration.service.ts
    apps/backend/src/modules/analytics/migration/migration.module.ts
  </files>
  <action>
**Step 1: Refactor MigrationService**

Update apps/backend/src/modules/analytics/migration/migration.service.ts:

1. Remove all extracted methods
2. Inject the three new sub-services:
   - MigrationParserService
   - MigrationValidatorService
   - MigrationExecutorService

3. Keep job management methods (CRUD for MigrationJob):
   - `createJob()` - Create migration job record
   - `getJob()` - Get job by ID
   - `listJobs()` - List jobs with pagination
   - `updateJobStatus()` - Update job status

4. Delegate to sub-services:
   - detectFormat -> migrationParserService.detectFormat()
   - saveFieldMappings -> migrationParserService.saveFieldMappings()
   - validateJob -> migrationValidatorService.validateJob()
   - getPreview -> migrationValidatorService.getPreview()
   - executeImport -> migrationExecutorService.executeImport()
   - checkRollbackEligibility -> migrationExecutorService.checkRollbackEligibility()
   - executeRollback -> migrationExecutorService.executeRollback()

Target: MigrationService under 400 LOC as thin coordinator.

**Step 2: Update migration.module.ts**

Add all three new services to providers array.
</action>
<verify>

- MigrationService is under 400 LOC: `wc -l apps/backend/src/modules/analytics/migration/migration.service.ts`
- MigrationService injects all three sub-services
- migration.module.ts includes all new services in providers
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  MigrationService is a thin coordinator (~300-400 LOC) managing job state and delegating to sub-services.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/analytics/migration/migration.service.ts
# Should be < 400

# Verify new services exist

ls apps/backend/src/modules/analytics/migration/services/

# Verify types

npm run typecheck

# Verify lint

npm run lint

```
</verification>

<success_criteria>
- MigrationService is under 400 LOC (down from 1159)
- MigrationParserService extracts ~350 LOC of parsing logic
- MigrationValidatorService extracts ~300 LOC of validation logic
- MigrationExecutorService extracts ~250 LOC of import/rollback logic
- All existing public API signatures preserved
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-15-SUMMARY.md`
</output>
```
