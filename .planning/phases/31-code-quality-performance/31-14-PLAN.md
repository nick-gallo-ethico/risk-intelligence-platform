---
phase: 31-code-quality-performance
plan: 14
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts
  - apps/backend/src/modules/analytics/exports/services/report-pdf-builder.service.ts
  - apps/backend/src/modules/analytics/exports/services/report-ai-summary.service.ts
  - apps/backend/src/modules/analytics/exports/board-report.service.ts
  - apps/backend/src/modules/analytics/exports/exports.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "BoardReportService is a thin coordinator under 400 LOC"
    - "Data fetching logic extracted to ReportDataFetcherService"
    - "PDF/HTML generation extracted to ReportPdfBuilderService"
    - "AI summary generation extracted to ReportAiSummaryService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts"
      provides: "Report data aggregation from Prisma"
      min_lines: 300
    - path: "apps/backend/src/modules/analytics/exports/services/report-pdf-builder.service.ts"
      provides: "HTML template rendering and PDF generation"
      min_lines: 200
    - path: "apps/backend/src/modules/analytics/exports/services/report-ai-summary.service.ts"
      provides: "AI-powered executive summary generation"
      min_lines: 100
    - path: "apps/backend/src/modules/analytics/exports/board-report.service.ts"
      provides: "Thin coordinator orchestrating report generation"
      max_lines: 400
  key_links:
    - from: "apps/backend/src/modules/analytics/exports/board-report.service.ts"
      to: "apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts"
      via: "constructor injection"
      pattern: "private.*reportDataFetcherService"
---

<objective>
Decompose board-report.service.ts (1189 LOC) into focused sub-services following the thin coordinator pattern.

Purpose: board-report.service.ts handles board report generation including data fetching, PDF/PPTX generation, AI summaries, and storage. Extract into responsibility-focused sub-services: data fetching, PDF building, and AI summaries.

Output: ReportDataFetcherService, ReportPdfBuilderService, ReportAiSummaryService, and refactored thin coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/analytics/exports/board-report.service.ts
@apps/backend/src/modules/analytics/exports/exports.module.ts
@apps/backend/src/modules/cases/services/case-query.service.ts (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract ReportDataFetcherService</name>
  <files>
    apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts
  </files>
  <action>
Create ReportDataFetcherService in apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts.

Extract these methods from board-report.service.ts:

**Data aggregation methods:**

- `fetchReportData(organizationId, config, dateRange)` - Main data fetch orchestrator
- `fetchCaseCounts(organizationId, dateRange, filters)` - Case statistics
- `fetchRiuCounts(organizationId, dateRange, filters)` - RIU statistics
- `fetchTrendData(organizationId, dateRange, filters)` - Monthly trend data
- `fetchCaseBreakdown(organizationId, dateRange, filters)` - Category/severity breakdown
- `fetchSlaMetrics(organizationId, dateRange)` - SLA compliance metrics
- `fetchCampaignMetrics(organizationId, dateRange)` - Campaign statistics
- `fetchRiskAreas(organizationId, dateRange)` - Risk area identification
- `buildDateFilter(startDate, endDate)` - Date range filter builder

Include internal interfaces:

- ReportData
- CaseCounts
- TrendDataPoints
- SlaMetrics
- CampaignMetrics
- RiskArea

Inject:

- PrismaService
- Logger
  </action>
  <verify>
- File exists at apps/backend/src/modules/analytics/exports/services/report-data-fetcher.service.ts
- Service class exports ReportDataFetcherService with @Injectable()
- All data fetching methods present
- Interfaces exported for use by other services
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  ReportDataFetcherService contains all Prisma data fetching logic for board reports.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract ReportPdfBuilderService and ReportAiSummaryService</name>
  <files>
    apps/backend/src/modules/analytics/exports/services/report-pdf-builder.service.ts
    apps/backend/src/modules/analytics/exports/services/report-ai-summary.service.ts
  </files>
  <action>
**Step 1: Create ReportPdfBuilderService**

Create apps/backend/src/modules/analytics/exports/services/report-pdf-builder.service.ts.

Extract these methods from board-report.service.ts:

- `buildHtmlReport(reportData, config)` - Handlebars template rendering
- `renderSection(sectionName, data)` - Individual section rendering
- `formatKpiValue(value, type)` - Number/percentage formatting
- `formatTrendIndicator(trend)` - Up/down arrow formatting
- `getReportTemplate()` - Handlebars template loading
- `registerHandlebarsHelpers()` - Custom Handlebars helpers

Inject:

- PdfGeneratorService (existing)
- Logger

**Step 2: Create ReportAiSummaryService**

Create apps/backend/src/modules/analytics/exports/services/report-ai-summary.service.ts.

Extract these methods:

- `generateExecutiveSummary(reportData, config)` - AI summary generation
- `buildSummaryPrompt(reportData)` - Prompt construction
- `formatAiResponse(response)` - Response parsing and formatting
- `generateRecommendations(reportData)` - AI recommendations

Inject:

- AiClientService
- Logger
  </action>
  <verify>
- report-pdf-builder.service.ts exists with HTML/template methods
- report-ai-summary.service.ts exists with AI summary methods
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  ReportPdfBuilderService handles templating, ReportAiSummaryService handles AI summaries.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Refactor BoardReportService to thin coordinator</name>
  <files>
    apps/backend/src/modules/analytics/exports/board-report.service.ts
    apps/backend/src/modules/analytics/exports/exports.module.ts
  </files>
  <action>
**Step 1: Refactor BoardReportService**

Update apps/backend/src/modules/analytics/exports/board-report.service.ts:

1. Remove all extracted methods
2. Inject the three new sub-services:
   - ReportDataFetcherService
   - ReportPdfBuilderService
   - ReportAiSummaryService

3. Refactor `generate()` to orchestrate sub-services:

   ```typescript
   async generate(organizationId, userId, config): Promise<BoardReportResult> {
     // 1. Fetch data
     const reportData = await this.reportDataFetcherService.fetchReportData(...);

     // 2. Generate AI summary (optional)
     if (config.sections?.includes('executive_summary')) {
       reportData.executiveSummary = await this.reportAiSummaryService.generateExecutiveSummary(...);
     }

     // 3. Build PDF
     const html = await this.reportPdfBuilderService.buildHtmlReport(...);
     const pdfBuffer = await this.pdfGeneratorService.generate(html);

     // 4. Build PPTX (if requested)
     // 5. Upload to storage
     // 6. Audit log
     // 7. Return result
   }
   ```

4. Keep in BoardReportService:
   - `generate()` - Public orchestration method
   - `uploadToStorage()` - Storage upload logic
   - `buildMetadata()` - Result metadata construction
   - `auditGeneration()` - Audit logging

Target: BoardReportService under 400 LOC as thin coordinator.

**Step 2: Update exports.module.ts**

Add all three new services to providers array.
</action>
<verify>

- BoardReportService is under 400 LOC: `wc -l apps/backend/src/modules/analytics/exports/board-report.service.ts`
- BoardReportService injects all three sub-services
- exports.module.ts includes all new services in providers
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  BoardReportService is a thin coordinator (~300-400 LOC) that delegates to focused sub-services.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/analytics/exports/board-report.service.ts
# Should be < 400

# Verify new services exist

ls apps/backend/src/modules/analytics/exports/services/

# Verify types

npm run typecheck

# Verify lint

npm run lint

```
</verification>

<success_criteria>
- BoardReportService is under 400 LOC (down from 1189)
- ReportDataFetcherService extracts ~400 LOC of data fetching
- ReportPdfBuilderService extracts ~250 LOC of templating
- ReportAiSummaryService extracts ~150 LOC of AI logic
- All existing public API signatures preserved
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-14-SUMMARY.md`
</output>
```
