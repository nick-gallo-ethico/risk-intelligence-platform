---
phase: 31-code-quality-performance
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/cases/services/case-query.service.ts
  - apps/backend/src/modules/cases/services/case-status.service.ts
  - apps/backend/src/modules/cases/cases.service.ts
  - apps/backend/src/modules/cases/cases.module.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CasesService is a thin coordinator under 300 LOC"
    - "Query logic (findAll, fullTextSearch, buildWhereClause) extracted to CaseQueryService"
    - "Status logic (validateStatusTransition, updateStatus, close) extracted to CaseStatusService"
    - "All existing public method signatures preserved - API unchanged"
  artifacts:
    - path: "apps/backend/src/modules/cases/services/case-query.service.ts"
      provides: "Case query operations with full-text search"
      min_lines: 200
    - path: "apps/backend/src/modules/cases/services/case-status.service.ts"
      provides: "Case status transitions and closure"
      min_lines: 80
    - path: "apps/backend/src/modules/cases/cases.service.ts"
      provides: "Thin coordinator delegating to sub-services"
      max_lines: 300
  key_links:
    - from: "apps/backend/src/modules/cases/cases.service.ts"
      to: "apps/backend/src/modules/cases/services/case-query.service.ts"
      via: "constructor injection"
      pattern: "private.*caseQueryService"
    - from: "apps/backend/src/modules/cases/cases.service.ts"
      to: "apps/backend/src/modules/cases/services/case-status.service.ts"
      via: "constructor injection"
      pattern: "private.*caseStatusService"
---

<objective>
Decompose cases.service.ts (795 LOC) into focused sub-services following the thin coordinator pattern.

Purpose: cases.service.ts is the last monolithic service in the codebase. Extract query operations and status logic into dedicated services, leaving CasesService as a thin coordinator (~200 LOC) that delegates to sub-services.

Output: CaseQueryService, CaseStatusService, and refactored thin CasesService coordinator.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@apps/backend/src/modules/cases/cases.service.ts
@apps/backend/src/modules/cases/cases.module.ts
@apps/backend/src/modules/rius/rius.service.ts (reference pattern - already decomposed)
@apps/backend/src/modules/rius/services/riu-query.service.ts (reference implementation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CaseQueryService</name>
  <files>
    apps/backend/src/modules/cases/services/case-query.service.ts
  </files>
  <action>
Create CaseQueryService in apps/backend/src/modules/cases/services/case-query.service.ts.

Extract these methods from cases.service.ts:

- `findAll(query, organizationId)` - Paginated list with filtering
- `findAllWithFullTextSearch(query, organizationId)` - PostgreSQL tsvector search
- `buildWhereClause(query, organizationId)` - Prisma where clause builder
- `applyFilterCondition(where, condition)` - HubSpot-style filter application
- `buildOrderByClause(sortBy, sortOrder)` - Relation-aware sorting
- `findOne(id, organizationId)` - Single case lookup
- `findByReferenceNumber(referenceNumber, organizationId)` - Lookup by reference

Inject:

- PrismaService
- Logger

Follow RiuQueryService as reference pattern:

- Preserve exact method signatures (return types, parameters)
- Keep all Prisma includes and query logic
- Maintain full-text search with ts_rank ordering
- Keep HubSpot-style filter operator mapping
  </action>
  <verify>
- File exists at apps/backend/src/modules/cases/services/case-query.service.ts
- Service class exports CaseQueryService with @Injectable()
- All 7 methods present with correct signatures
- npm run lint passes
- npm run typecheck passes
  </verify>
  <done>
  CaseQueryService contains all query-related methods extracted from CasesService with preserved signatures.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Extract CaseStatusService and refactor CasesService</name>
  <files>
    apps/backend/src/modules/cases/services/case-status.service.ts
    apps/backend/src/modules/cases/cases.service.ts
    apps/backend/src/modules/cases/cases.module.ts
  </files>
  <action>
**Step 1: Create CaseStatusService**

Create apps/backend/src/modules/cases/services/case-status.service.ts.

Extract these methods from cases.service.ts:

- `updateStatus(id, status, rationale, userId, organizationId)` - Status change with validation
- `close(id, rationale, userId, organizationId)` - Soft-close to CLOSED status
- `validateStatusTransition(current, next)` - Status transition rules

Inject:

- PrismaService
- ActivityService
- EventEmitter2
- CaseQueryService (for findOne lookups)

Keep the event emission pattern (CaseStatusChangedEvent).

**Step 2: Refactor CasesService to thin coordinator**

Update apps/backend/src/modules/cases/cases.service.ts:

1. Remove extracted methods (query and status logic)
2. Inject CaseQueryService and CaseStatusService
3. Delegate to sub-services:
   - findAll, findOne, findByReferenceNumber -> caseQueryService
   - updateStatus, close -> caseStatusService
4. Keep in CasesService:
   - create() - Case creation with reference number generation
   - update() - General case updates (not status-specific)
   - generateReferenceNumber() - Reference number logic
   - emitEvent() - Event emission helper

Target: CasesService under 300 LOC as thin coordinator.

**Step 3: Update cases.module.ts**

Add CaseQueryService and CaseStatusService to providers.

Update exports if needed.
</action>
<verify>

- apps/backend/src/modules/cases/services/case-status.service.ts exists
- CasesService is under 300 LOC: `wc -l apps/backend/src/modules/cases/cases.service.ts`
- CasesService injects and delegates to CaseQueryService
- CasesService injects and delegates to CaseStatusService
- cases.module.ts includes both new services in providers
- npm run lint passes
- npm run typecheck passes
- npm run test -- --testPathPattern="cases" passes (existing tests still work)
  </verify>
  <done>
  CasesService is a thin coordinator (~200-300 LOC) that delegates query operations to CaseQueryService and status operations to CaseStatusService. All public method signatures preserved.
  </done>
  </task>

</tasks>

<verification>
```bash
# Verify LOC reduction
wc -l apps/backend/src/modules/cases/cases.service.ts
# Should be < 300

# Verify new services exist

ls apps/backend/src/modules/cases/services/

# Verify types

npm run typecheck

# Verify lint

npm run lint

# Verify tests pass

npm run test -- --testPathPattern="cases"

```
</verification>

<success_criteria>
- CasesService is under 300 LOC (down from 795)
- CaseQueryService extracts ~400 LOC of query logic
- CaseStatusService extracts ~100 LOC of status logic
- All existing public API signatures preserved
- All tests pass
- No runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-code-quality-performance/31-09-SUMMARY.md`
</output>
```
