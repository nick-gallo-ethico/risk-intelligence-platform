---
phase: 30-test-coverage-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/auth/auth.service.spec.ts
  - apps/backend/src/modules/auth/mfa/mfa.service.spec.ts
  - apps/backend/src/modules/auth/services/token-refresh.service.spec.ts
  - apps/backend/src/modules/auth/mfa/recovery-codes.service.spec.ts
autonomous: true

must_haves:
  truths:
    - "AuthService login tests verify valid credentials return tokens"
    - "AuthService login tests verify invalid password throws UnauthorizedException"
    - "AuthService login tests verify inactive organization throws UnauthorizedException"
    - "AuthService login tests verify withBypassRLS is called for cross-tenant lookup"
    - "MfaService tests verify TOTP setup and verification flow"
    - "TokenRefreshService tests verify token refresh returns new access token"
    - "RecoveryCodesService tests verify code generation and verification"
  artifacts:
    - path: "apps/backend/src/modules/auth/auth.service.spec.ts"
      provides: "AuthService unit tests"
      min_lines: 150
    - path: "apps/backend/src/modules/auth/mfa/mfa.service.spec.ts"
      provides: "MfaService unit tests"
      min_lines: 100
    - path: "apps/backend/src/modules/auth/services/token-refresh.service.spec.ts"
      provides: "TokenRefreshService unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/mfa/recovery-codes.service.spec.ts"
      provides: "RecoveryCodesService unit tests"
      min_lines: 60
  key_links:
    - from: "auth.service.spec.ts"
      to: "auth.service.ts"
      via: "Jest Test.createTestingModule"
      pattern: "describe.*AuthService"
---

<objective>
Create unit tests for the core auth services: AuthService, MfaService, TokenRefreshService, and RecoveryCodesService.

Purpose: These services handle security-critical operations (login, token management, MFA) and require comprehensive test coverage to ensure tenant isolation and correct behavior.

Output: 4 spec files with tests covering login, MFA setup/verify, token refresh, and recovery code generation.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-coverage-foundation/30-RESEARCH.md

# Reference patterns

@apps/backend/examples/test-pattern.spec.ts
@apps/backend/src/modules/investigations/investigations.service.spec.ts
@apps/backend/src/modules/auth/auth.service.ts
@apps/backend/src/modules/auth/mfa/mfa.service.ts
@apps/backend/src/modules/auth/services/token-refresh.service.ts
@apps/backend/src/modules/auth/mfa/recovery-codes.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthService unit tests</name>
  <files>apps/backend/src/modules/auth/auth.service.spec.ts</files>
  <action>
Create comprehensive unit tests for AuthService following the canonical pattern in test-pattern.spec.ts.

**Mock dependencies:**

- PrismaService with `withBypassRLS: jest.fn((cb) => cb())` pattern
- JwtService with `signAsync: jest.fn().mockResolvedValue('mock-token')`
- ConfigService with mock expiry values
- Mock bcrypt: `jest.mock('bcrypt')` at top of file

**Test cases to implement:**

describe('login')

- should return tokens and user on valid credentials
- should throw UnauthorizedException on invalid password (bcrypt.compare returns false)
- should throw UnauthorizedException on user not found
- should throw UnauthorizedException on inactive organization
- should throw UnauthorizedException on inactive user
- should bypass RLS for login (verify withBypassRLS called)
- should create session after successful login
- should update lastLoginAt

describe('refreshTokens')

- should return new access token for valid refresh token
- should throw UnauthorizedException for expired session
- should throw UnauthorizedException for revoked session
- should bypass RLS for refresh (verify withBypassRLS called)

describe('revokeSession')

- should mark session as revoked
- should throw NotFoundException for session not found

describe('revokeAllSessions')

- should revoke all sessions for user

**Structure:**

```typescript
import { Test, TestingModule } from "@nestjs/testing";
import { UnauthorizedException } from "@nestjs/common";
import { JwtService } from "@nestjs/jwt";
import { ConfigService } from "@nestjs/config";
import * as bcrypt from "bcrypt";
import { AuthService } from "./auth.service";
import { PrismaService } from "../prisma/prisma.service";

jest.mock("bcrypt");

describe("AuthService", () => {
  // ... setup with mockPrismaService.withBypassRLS = jest.fn((cb) => cb())
});
```

  </action>
  <verify>
Run `cd apps/backend && npm test -- --testPathPattern="auth.service.spec" --coverage` and verify:
- All tests pass
- No skipped tests
- Coverage report shows auth.service.ts lines tested
  </verify>
  <done>
AuthService has unit tests covering login (valid/invalid credentials, inactive org/user), token refresh (valid/expired/revoked), session revocation, and withBypassRLS verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create MfaService unit tests</name>
  <files>apps/backend/src/modules/auth/mfa/mfa.service.spec.ts</files>
  <action>
Create unit tests for MfaService following the canonical pattern.

**Mock dependencies:**

- PrismaService
- Mock otplib: `jest.mock('otplib')` - mock `authenticator.generateSecret()` and `authenticator.verify()`
- Mock qrcode: `jest.mock('qrcode')` - mock `toDataURL()`

**Test cases to implement:**

describe('initiateMfaSetup')

- should generate secret and return QR code data URL
- should store pending MFA secret on user
- should throw if user already has MFA enabled

describe('verifyAndEnableMfa')

- should enable MFA when TOTP code is valid
- should throw BadRequestException when TOTP code is invalid
- should throw BadRequestException when no pending secret
- should generate recovery codes on enable

describe('verifyMfa')

- should return true for valid TOTP code
- should return false for invalid TOTP code
- should throw if MFA not enabled

describe('disableMfa')

- should clear MFA fields on user
- should throw if MFA not enabled

describe('regenerateRecoveryCodes')

- should generate new recovery codes
- should hash codes before storage
- should return plaintext codes to user

describe('getMfaStatus')

- should return enabled:true when MFA configured
- should return enabled:false when MFA not configured

**Mock pattern for otplib:**

```typescript
jest.mock("otplib", () => ({
  authenticator: {
    generateSecret: jest.fn().mockReturnValue("MOCKSECRET"),
    verify: jest.fn().mockReturnValue(true),
  },
}));
```

  </action>
  <verify>
Run `cd apps/backend && npm test -- --testPathPattern="mfa.service.spec" --coverage` and verify all tests pass.
  </verify>
  <done>
MfaService has unit tests covering MFA setup initiation, TOTP verification, MFA enable/disable, recovery code regeneration, and status checking.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create TokenRefreshService and RecoveryCodesService tests</name>
  <files>
apps/backend/src/modules/auth/services/token-refresh.service.spec.ts
apps/backend/src/modules/auth/mfa/recovery-codes.service.spec.ts
  </files>
  <action>
Create unit tests for TokenRefreshService and RecoveryCodesService.

**TokenRefreshService tests (token-refresh.service.spec.ts):**

Mock dependencies:

- PrismaService with withBypassRLS pattern
- JwtService
- ConfigService

describe('refreshAccessToken')

- should return new access token for valid session
- should throw UnauthorizedException for invalid session
- should throw UnauthorizedException for expired session
- should throw UnauthorizedException for revoked session
- should verify JWT before processing

describe('refreshWebSocketToken')

- should return WebSocket token for valid session
- should throw UnauthorizedException for invalid credentials

describe('revokeAllUserSessions')

- should revoke all sessions for specified user
- should use withBypassRLS for cross-tenant operation

**RecoveryCodesService tests (recovery-codes.service.spec.ts):**

Mock dependencies:

- PrismaService
- Mock bcrypt for hashing

describe('generateRecoveryCodes')

- should generate 10 codes by default
- should generate codes in correct format (XXXXX-XXXXX)
- should return plaintext codes

describe('hashRecoveryCodes')

- should hash all codes using bcrypt
- should return array of hashes

describe('verifyRecoveryCode')

- should return true when code matches
- should return false when code does not match
- should mark used code (decrement remaining uses)
- should throw when no recovery codes exist
  </action>
  <verify>
  Run:
- `cd apps/backend && npm test -- --testPathPattern="token-refresh.service.spec"` - all pass
- `cd apps/backend && npm test -- --testPathPattern="recovery-codes.service.spec"` - all pass
  </verify>
  <done>
  TokenRefreshService has tests for access token refresh, WebSocket token refresh, and session revocation. RecoveryCodesService has tests for code generation, hashing, and verification.
  </done>
  </task>

</tasks>

<verification>
Run full auth module test suite:
```bash
cd apps/backend
npm test -- --testPathPattern="auth" --coverage
```

Expected:

- 4 new spec files created
- All tests pass
- No regressions in existing tests
  </verification>

<success_criteria>

1. auth.service.spec.ts exists with 8+ test cases covering login, refresh, revoke
2. mfa.service.spec.ts exists with 10+ test cases covering MFA lifecycle
3. token-refresh.service.spec.ts exists with 6+ test cases
4. recovery-codes.service.spec.ts exists with 5+ test cases
5. All tests pass with `npm test -- --testPathPattern="auth"`
6. withBypassRLS pattern verified in auth tests (login, refresh operations)
   </success_criteria>

<output>
After completion, create `.planning/phases/30-test-coverage-foundation/30-01-SUMMARY.md`
</output>
