---
phase: 30-test-coverage-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend/src/modules/auth/sso/sso.service.spec.ts
  - apps/backend/src/modules/auth/sso/sso-config.service.spec.ts
  - apps/backend/src/modules/auth/domain/domain.service.spec.ts
  - apps/backend/src/modules/auth/domain/domain-verification.service.spec.ts
autonomous: true

must_haves:
  truths:
    - "SsoService tests verify findOrCreateSsoUser creates user if not exists"
    - "SsoService tests verify JIT provisioning respects domain-organization mapping"
    - "SsoConfigService tests verify CRUD operations on SSO configuration"
    - "DomainService tests verify domain lookup for organization routing"
    - "DomainVerificationService tests verify DNS TXT record verification"
  artifacts:
    - path: "apps/backend/src/modules/auth/sso/sso.service.spec.ts"
      provides: "SsoService unit tests"
      min_lines: 100
    - path: "apps/backend/src/modules/auth/sso/sso-config.service.spec.ts"
      provides: "SsoConfigService unit tests"
      min_lines: 60
    - path: "apps/backend/src/modules/auth/domain/domain.service.spec.ts"
      provides: "DomainService unit tests"
      min_lines: 80
    - path: "apps/backend/src/modules/auth/domain/domain-verification.service.spec.ts"
      provides: "DomainVerificationService unit tests"
      min_lines: 60
  key_links:
    - from: "sso.service.spec.ts"
      to: "sso.service.ts"
      via: "Jest Test.createTestingModule"
      pattern: "describe.*SsoService"
---

<objective>
Create unit tests for SSO and domain services: SsoService, SsoConfigService, DomainService, and DomainVerificationService.

Purpose: These services handle enterprise SSO integration, JIT user provisioning, and domain-based organization routing. Tests ensure secure tenant routing and proper SSO configuration.

Output: 4 spec files with tests covering SSO user provisioning, configuration management, domain lookup, and DNS verification.
</objective>

<execution_context>
@C:\Users\cu0718\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\cu0718\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-test-coverage-foundation/30-RESEARCH.md

# Reference patterns

@apps/backend/examples/test-pattern.spec.ts
@apps/backend/src/modules/auth/sso/sso.service.ts
@apps/backend/src/modules/auth/sso/sso-config.service.ts
@apps/backend/src/modules/auth/domain/domain.service.ts
@apps/backend/src/modules/auth/domain/domain-verification.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SsoService and SsoConfigService unit tests</name>
  <files>
apps/backend/src/modules/auth/sso/sso.service.spec.ts
apps/backend/src/modules/auth/sso/sso-config.service.spec.ts
  </files>
  <action>
Create unit tests for SsoService and SsoConfigService following the canonical pattern.

**SsoService tests (sso.service.spec.ts):**

Mock dependencies:

- PrismaService with withBypassRLS pattern (SSO lookups cross tenant boundaries)
- DomainService (for organization lookup)

Test data fixtures:

```typescript
const mockOrgId = "org-test-123";
const mockSsoProfile = {
  provider: "azure",
  providerId: "azure-user-123",
  email: "user@company.com",
  firstName: "Test",
  lastName: "User",
};
const mockUser = {
  id: "user-123",
  email: "user@company.com",
  organizationId: mockOrgId,
  ssoProvider: "azure",
  ssoProviderId: "azure-user-123",
};
```

describe('findOrCreateSsoUser')

- should return existing user when SSO profile matches
- should create new user when SSO profile not found (JIT provisioning)
- should associate SSO provider with existing email user (linking)
- should throw if domain not mapped to organization
- should use withBypassRLS for cross-tenant lookup

describe('linkSsoToExistingUser')

- should link SSO provider to existing user
- should throw if user already has different SSO provider

describe('jitProvisionUser')

- should create user with correct organizationId from domain
- should set default role from organization settings
- should set SSO fields (ssoProvider, ssoProviderId)

**SsoConfigService tests (sso-config.service.spec.ts):**

Mock dependencies:

- PrismaService

describe('getSsoConfig')

- should return SSO config for organization
- should return null if no config exists

describe('updateSsoConfig')

- should create config if not exists
- should update existing config
- should validate provider type (azure, google, saml)
- should filter by organizationId (tenant isolation)
  </action>
  <verify>
  Run:
- `cd apps/backend && npm test -- --testPathPattern="sso.service.spec"` - all pass
- `cd apps/backend && npm test -- --testPathPattern="sso-config.service.spec"` - all pass
  </verify>
  <done>
  SsoService has tests for findOrCreateSsoUser, linkSsoToExistingUser, and JIT provisioning with withBypassRLS verification. SsoConfigService has tests for SSO configuration CRUD operations.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Create DomainService and DomainVerificationService unit tests</name>
  <files>
apps/backend/src/modules/auth/domain/domain.service.spec.ts
apps/backend/src/modules/auth/domain/domain-verification.service.spec.ts
  </files>
  <action>
Create unit tests for DomainService and DomainVerificationService.

**DomainService tests (domain.service.spec.ts):**

Mock dependencies:

- PrismaService

Test data fixtures:

```typescript
const mockOrgId = "org-test-123";
const mockDomain = {
  id: "domain-123",
  domain: "company.com",
  organizationId: mockOrgId,
  verified: true,
  verificationToken: "ethico-verify-abc123",
  createdAt: new Date(),
};
```

describe('getDomain')

- should return domain by id
- should filter by organizationId (tenant isolation)
- should throw NotFoundException if not found

describe('addDomain')

- should create domain with verification token
- should throw BadRequestException if domain already exists (any org)
- should set verified: false initially

describe('removeDomain')

- should delete domain
- should throw NotFoundException if not found or wrong org

describe('findOrganizationByEmailDomain')

- should return organizationId for verified domain
- should return null for unverified domain
- should return null for unknown domain
- should use withBypassRLS (cross-tenant lookup)

**DomainVerificationService tests (domain-verification.service.spec.ts):**

Mock dependencies:

- PrismaService
- Mock dns.resolveTxt: `jest.mock('dns/promises')` or mock the DNS lookup method

describe('initiateDnsVerification')

- should return verification token and instructions
- should throw if domain not found

describe('checkDnsVerification')

- should mark domain as verified when TXT record matches
- should return false when TXT record not found
- should return false when TXT record wrong value
- should handle DNS lookup errors gracefully

**DNS mocking pattern:**

```typescript
jest.mock("dns/promises", () => ({
  resolveTxt: jest.fn(),
}));
import * as dns from "dns/promises";

// In test
(dns.resolveTxt as jest.Mock).mockResolvedValue([["ethico-verify-abc123"]]);
```

  </action>
  <verify>
Run:
- `cd apps/backend && npm test -- --testPathPattern="domain.service.spec"` - all pass
- `cd apps/backend && npm test -- --testPathPattern="domain-verification.service.spec"` - all pass
  </verify>
  <done>
DomainService has tests for domain CRUD and organization lookup with tenant isolation. DomainVerificationService has tests for DNS TXT record verification flow.
  </done>
</task>

</tasks>

<verification>
Run SSO and domain test suite:
```bash
cd apps/backend
npm test -- --testPathPattern="sso|domain" --coverage
```

Expected:

- 4 new spec files created
- All tests pass
- No regressions in existing tests
  </verification>

<success_criteria>

1. sso.service.spec.ts exists with 7+ test cases covering findOrCreateSsoUser, linking, JIT provisioning
2. sso-config.service.spec.ts exists with 4+ test cases for config CRUD
3. domain.service.spec.ts exists with 6+ test cases for domain management
4. domain-verification.service.spec.ts exists with 4+ test cases for DNS verification
5. All tests pass with `npm test -- --testPathPattern="sso|domain"`
6. withBypassRLS pattern verified in SSO/domain cross-tenant operations
   </success_criteria>

<output>
After completion, create `.planning/phases/30-test-coverage-foundation/30-02-SUMMARY.md`
</output>
