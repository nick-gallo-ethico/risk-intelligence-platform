/**
 * Ethico Risk Intelligence Platform - Shared Types
 *
 * This package provides TypeScript types shared between frontend and backend.
 *
 * Usage:
 * import { User, UserRole, LoginRequest } from '@ethico/types';
 * import type { PaginatedResponse } from '@ethico/types';
 */

// User types
export * from './user';

// Auth types
export * from './auth';

// API types
export * from './api';
